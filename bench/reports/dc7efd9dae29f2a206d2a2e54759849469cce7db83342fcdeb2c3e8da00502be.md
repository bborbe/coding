dc7efd9dae29f2a206d2a2e54759849469cce7db83342fcdeb2c3e8da00502be

## Configuration
- model: sonnet
- effort: medium
- mode: short
- config_hash: dc7efd9dae29f2a206d2a2e54759849469cce7db83342fcdeb2c3e8da00502be
- rules_commands_hash: 05f95877eb4833244b45fd716709dda56217b27235b0c1ec5fb36e81169196ce
- prs_version: dev-1
- coding version: 0.37.0
- golden baseline coding version: v0.35.6
- golden version: golden-dev-2
- runner_version: 1
- rows skipped: 0
- cost: not recorded — the ledger carries no cost field.

*golden-dev-2* carries 1 `rejected` entry, so precision is measurable: reporting one costs precision. With so few adjudicated, a single hit still moves the number a long way — read it as directional, not calibrated.

36 of the 44 signatures embed a line reference, so a re-report of the same issue at a different line does not match and is surfaced as a gap-triage candidate rather than a hit. On this golden set, `recall` measures whether a configuration cited the same line, not whether it found the issue.

## Runs
| run | span | PRs | complete | golden in scope | findings | hits | misses | matched rejected | gap candidates | recall | precision | wall time (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-08-08T09:36:49.617686+00:00 … 2026-08-08T09:40:36.060691+00:00 | 5 | complete | 44 | 6 | 3 | 40 | 0 | 3 | 0.070 | 1.000 | 260 |
| 2 | 2026-08-08T09:45:45.518306+00:00 … 2026-08-08T09:50:02.643382+00:00 | 5 | complete | 44 | 9 | 2 | 41 | 0 | 8 | 0.047 | 1.000 | 303 |
| 3 | 2026-08-08T09:50:49.866567+00:00 … 2026-08-08T09:55:49.073512+00:00 | 5 | complete | 44 | 9 | 4 | 39 | 0 | 6 | 0.093 | 1.000 | 349 |
| 4 | 2026-08-08T09:56:40.374196+00:00 … 2026-08-08T12:52:27.573435+00:00 | 5 | complete | 44 | 5 | 1 | 42 | 0 | 4 | 0.023 | 1.000 | 352 |
| 5 | 2026-08-08T12:47:58.459085+00:00 … 2026-08-08T20:24:03.919434+00:00 | 5 | complete | 44 | 1 | 1 | 42 | 0 | 0 | 0.023 | 1.000 | 300 |
| 6 | 2026-08-08T20:14:21.017519+00:00 … 2026-08-08T20:16:55.705463+00:00 | 3 | partial | 25 | 1 | 0 | 24 | 0 | 1 | 0.000 | n/a | 202 |
| 7 | 2026-08-08T20:19:32.316923+00:00 … 2026-08-08T20:25:14.175907+00:00 | 3 | partial | 25 | 1 | 0 | 24 | 0 | 1 | 0.000 | n/a | 223 |
| 8 | 2026-08-08T20:24:54.224851+00:00 … 2026-08-08T20:24:54.224851+00:00 | 1 | partial | 1 | 0 | 0 | 1 | 0 | 0 | 0.000 | n/a | 20 |

## Per-PR
| run | pr_id | golden in scope | hits | misses | findings | gap candidates | duration (s) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | tts-mcp#20 | 1 | 0 | 1 | 0 | 0 | 30 |
| 1 | github-pr-review-agent#11 | 14 | 1 | 12 | 1 | 0 | 56 |
| 1 | quant#109 | 6 | 0 | 6 | 1 | 1 | 57 |
| 1 | node-skeleton#2 | 10 | 1 | 9 | 2 | 1 | 82 |
| 1 | python-skeleton#3 | 13 | 1 | 12 | 2 | 1 | 34 |
| 2 | tts-mcp#20 | 1 | 0 | 1 | 0 | 0 | 22 |
| 2 | github-pr-review-agent#11 | 14 | 0 | 13 | 0 | 0 | 62 |
| 2 | quant#109 | 6 | 0 | 6 | 0 | 0 | 74 |
| 2 | node-skeleton#2 | 10 | 0 | 10 | 6 | 6 | 98 |
| 2 | python-skeleton#3 | 13 | 2 | 11 | 3 | 2 | 47 |
| 3 | tts-mcp#20 | 1 | 0 | 1 | 0 | 0 | 25 |
| 3 | github-pr-review-agent#11 | 14 | 0 | 13 | 5 | 5 | 81 |
| 3 | quant#109 | 6 | 0 | 6 | 0 | 0 | 47 |
| 3 | node-skeleton#2 | 10 | 1 | 9 | 1 | 0 | 146 |
| 3 | python-skeleton#3 | 13 | 3 | 10 | 3 | 1 | 51 |
| 4 | tts-mcp#20 | 1 | 0 | 1 | 0 | 0 | 29 |
| 4 | github-pr-review-agent#11 | 14 | 0 | 13 | 0 | 0 | 75 |
| 4 | node-skeleton#2 | 10 | 0 | 10 | 3 | 3 | 108 |
| 4 | quant#109 | 6 | 0 | 6 | 0 | 0 | 85 |
| 4 | python-skeleton#3 | 13 | 1 | 12 | 2 | 1 | 54 |
| 5 | tts-mcp#20 | 1 | 0 | 1 | 0 | 0 | 21 |
| 5 | github-pr-review-agent#11 | 14 | 0 | 13 | 0 | 0 | 87 |
| 5 | node-skeleton#2 | 10 | 0 | 10 | 0 | 0 | 72 |
| 5 | quant#109 | 6 | 0 | 6 | 0 | 0 | 70 |
| 5 | python-skeleton#3 | 13 | 1 | 12 | 1 | 0 | 50 |
| 6 | tts-mcp#20 | 1 | 0 | 1 | 0 | 0 | 24 |
| 6 | github-pr-review-agent#11 | 14 | 0 | 13 | 1 | 1 | 64 |
| 6 | node-skeleton#2 | 10 | 0 | 10 | 0 | 0 | 114 |
| 7 | tts-mcp#20 | 1 | 0 | 1 | 0 | 0 | 20 |
| 7 | node-skeleton#2 | 10 | 0 | 10 | 1 | 1 | 134 |
| 7 | github-pr-review-agent#11 | 14 | 0 | 13 | 0 | 0 | 69 |
| 8 | tts-mcp#20 | 1 | 0 | 1 | 0 | 0 | 20 |

## Gap-triage candidates
These findings matched no golden entry. They are **not a precision failure** — the golden set is a bootstrap from one strong-model run, and a finding it does not describe is evidence the set is incomplete.

### run 1 — quant#109 — None

`task/recurring-schedules/prod/plan-next-week.yaml`: bundles two unrelated changes in one file — the `due_date` field addition (consistent with the rest of the PR) plus a rewrite of the Success Criteria/Tasks/Definition of Done checklist text (Must/Should/Could → priority groups + explicit deferrals). Consider splitting into a separate commit/PR for traceability, though not blocking. Precommit skipped (short mode) — CI covers lint+test. No worktree was created (checkout was already at `origin/bench-pr-109` HEAD, clean tree), so no cleanup needed. **Summary**: mechanical, low-risk change — adds `due_date` to 21 weekend/prod recurring-schedule YAMLs, correctly differentiated between same-day (`{{current_date}}`) and next-Sunday (`{{next_sun_date}}`) based on each schedule's weekday. No functional issues found.

### run 1 — node-skeleton#2 — None

None beyond the above — the TS module boundaries (`import type` + `require('*.ts')` split to avoid `as`), `noUncheckedIndexedAccess` handling in `log.ts`/`test/health.test.ts`, and the `config.check()` fail-fast redesign are all clean and match the documented invariants. precommit skipped (short mode) — CI covers lint+test. No worktree was used (already at PR head, tree clean, so no cleanup needed).

### run 1 — python-skeleton#3 — None

`Makefile.precommit` `trivy` target has no pinned Trivy version (unlike `pip-audit`, which pins via `PIP_AUDIT_VERSION`), so results can silently drift as Trivy/its DB update — minor inconsistency with the audit target's pinning discipline. Everything else — the hatchling migration, Dockerfile `README.md` copy fix, ruff `S` ruleset with reasoned suppressions, `.trivyignore` scaffold, CHANGELOG entries, and the `test_create_app_includes_routers` rewrite to use `TestClient` instead of introspecting `app.routes` — is correct, well-justified, and consistent with the stated go-skeleton/node-skeleton parity goal. precommit skipped (selector/short mode) — CI covers lint+test. No worktree was created (diff generated directly against `origin/bench-base-3` in the current checkout), so no cleanup needed.

### run 2 — node-skeleton#2 — None

`tsconfig.json`/`eslint.config.js`/`Makefile.precommit` wiring is consistent (`typecheck` added to `make check`, matches README and CLAUDE.md updates).

### run 2 — node-skeleton#2 — None

`config.check()` replacing throw-on-import is a genuine improvement — logs structured errors before `process.exit(1)` instead of an uncaught throw, and adds a previously-missing `SHUTDOWN_TIMEOUT_MS` validation.

### run 2 — node-skeleton#2 — None

`require('./x.ts')` + `import type` split is the correct idiom for this repo's no-build-step constraint (`moduleResolution: bundler`).

### run 2 — node-skeleton#2 — None

Verified `node:22-slim` + `ENTRYPOINT ["node", "src/index.ts"]` works unflagged — type-stripping went default in Node 22.18 (backported from 23.6), and today's `node:22-slim` pulls latest 22.x.

### run 2 — node-skeleton#2 — None

LICENSE file present (README addition is consistent, not dangling).

### run 2 — node-skeleton#2 — None

precommit skipped (short mode) — CI covers lint+test+typecheck. No findings. Nothing to fix before merge.

### run 2 — python-skeleton#3 — None

`Makefile.precommit` — `trivy` target has no version pin for the `trivy` binary itself (only `PIP_AUDIT_VERSION` is pinned); CI installs whatever `apt-get install trivy` resolves to, so local and CI runs can silently drift.

### run 2 — python-skeleton#3 — None

`.trivyignore` is empty (comment-only) — fine for now, just confirming it's intentional and not an oversight. precommit skipped (short mode) — CI covers lint+test. No worktree was created (already at PR head), so no cleanup needed.

### run 3 — github-pr-review-agent#11 — None

precommit skipped (short mode) — CI covers lint+test. Ran `go build ./...` manually: clean.

### run 3 — github-pr-review-agent#11 — None

Step 4 (dispatcher) skipped per Short mode.

### run 3 — github-pr-review-agent#11 — None

LICENSE present; CHANGELOG `## Unreleased` correctly appended (both the compress bump and the `--skip-post` bullet); doc mis-attributions (`CLAUDE.md`, `README.md`, `docs/pr-post-back.md`) fully corrected — verified no `run-task` line still mentions `comment-only`/`positional`.

### run 3 — github-pr-review-agent#11 — steps_checkout_execution.go:316

Verified the nil-interface contract end-to-end: `ResolvePosters` (`pkg/factory/runner.go`) returns interface-typed `nil, nil` under `SkipPost`, exported directly (no stale `export_test.go` alias), and both real consumers guard correctly — `steps_checkout_execution.go:316` (`s.prPoster == nil`) and `steps_review.go:278` (`s.poster == nil`, new). The new `pkg/skip_post_boundary_test.go` drives the real `ResolvePosters` return value through all three consumer paths (execution, review-dismiss, review-verify), which is the one thing a literal-`nil` test can't prove.

### run 3 — github-pr-review-agent#11 — None

`overrideStep` correctly left unguarded per its documented constraint (only reachable via the pod path where poster is always non-nil). This is a clean, well-scoped, fully self-consistent diff — no findings to report.

### run 3 — python-skeleton#3 — Makefile.precommit:41

`Makefile.precommit:41-42` — the `audit` target's comment promises `--ignore-vuln` entries for unreachable findings, but no such flag exists in the actual command yet; fine as forward-looking documentation, just note it's aspirational, not implemented.

### run 4 — node-skeleton#2 — None

precommit skipped (selector mode/short mode) — CI covers lint+test+typecheck

### run 4 — node-skeleton#2 — None

`npm ci` was not run in this environment, so `tsc --noEmit` / `node --test` were not executed live; static review of all touched `.ts` files found no type errors, no `any`/`as`/`!`/`@ts-ignore`, and correct `.ts`-extension `require()` usage matching the documented `moduleResolution: "bundler"` invariant. Recommend confirming CI's `make check` is green before merge.

### run 4 — node-skeleton#2 — None

LICENSE file present; README gained a License section pointing to it — harmless, unrelated to the TS conversion but fine. Worktree cleanup: not applicable — review ran directly in the current checkout (already at PR head), no worktree was created.

### run 4 — python-skeleton#3 — Makefile.precommit:52

No `ROOTDIR` variable is visibly defined in the diff, but `Makefile.precommit:52` references `$(ROOTDIR)` for the fallback `.trivyignore`/`.trivy-secret.yaml` lookup — verify it's defined elsewhere (e.g. `Makefile.variables`) since this repo's own `.trivyignore` is at the project root, so the fallback branch may be dead code here but presumably reused for sibling skeletons via includes. Both are minor/non-blocking. Otherwise: build-backend switch to hatchling is correctly paired with the Dockerfile fix (README.md now copied before build), the `S104`/`S101` ruff suppressions are properly reasoned, and the `test_create_app_includes_routers` fix correctly moves off fragile `app.routes` introspection to behavioral `TestClient` assertions. CHANGELOG entries are well-formed with conventional prefixes. precommit skipped (short mode) — CI covers lint+test.

### run 6 — github-pr-review-agent#11 — pkg/factory/runner.go:166

`pkg/factory/runner.go:166` — `ResolvePosters` is exported (capital `R`), while the originating prompt (`prompts/completed/001-run-task-skip-post-flag.md`) specified an unexported `resolvePosters` surfaced only through the `export_test.go` seam convention used elsewhere in this package. Not a bug — it matches the existing `ResolveBotLogin` precedent and is exercised directly by `factory_test`, and the nil-interface contract (the actual risk here) is correctly upheld either way. Flagging only as a spec/convention drift, not something to change. precommit skipped (short mode) — CI covers lint+test.

### run 7 — node-skeleton#2 — README.md:109

`README.md:109-111` adds a "## License" section — unrelated to the stated TS-conversion scope of this PR (harmless, and `LICENSE` does exist at repo root, so no factual issue, just scope creep worth calling out). precommit skipped (short mode) — CI covers lint+test. No further action needed on this review; worktree cleanup not applicable (session was already at PR head).
