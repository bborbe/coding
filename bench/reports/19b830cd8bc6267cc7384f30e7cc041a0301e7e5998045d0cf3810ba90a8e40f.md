19b830cd8bc6267cc7384f30e7cc041a0301e7e5998045d0cf3810ba90a8e40f

## Configuration
- model: fable
- effort: xhigh
- mode: full
- config_hash: 19b830cd8bc6267cc7384f30e7cc041a0301e7e5998045d0cf3810ba90a8e40f
- rules_commands_hash: ecc803331f860b845a6a1b8a103e889bce02520e8cc04ea88102de74c8d5600d
- prs_version: curated-1
- coding version: 0.51.0
- golden baseline coding version: v0.35.6
- golden version: golden-curated-4
- runner_version: 1
- rows skipped: 0
- cost: not recorded — the ledger carries no cost field.

*golden-curated-4* carries 21 `rejected` entries, so precision is measurable: reporting one costs precision. With so few adjudicated, a single hit still moves the number a long way — read it as directional, not calibrated.

No signature embeds a line reference, so a re-report of the same issue at a different line still matches. `recall` measures issue detection.

## Runs
| run | span | PRs | complete | golden in scope | findings | hits | misses | matched rejected | gap candidates | recall | precision | wall time (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-09-08T08:53:24.738928+00:00 … 2026-09-08T10:13:08.915744+00:00 | 11/20 | partial | 84 | 84 | 6 | 63 | 1 | 77 | 0.087 | 0.857 | 5288 |

## Per-PR
| run | pr_id | golden in scope | hits | misses | findings | gap candidates | dropped items | missing sections | duration (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | bw#41 | 3 | 0 | 2 | 1 | 1 | 0 | — | 295 |
| 1 | dark-factory#71 | 2 | 0 | 2 | 3 | 3 | 0 | — | 774 |
| 1 | tts-mcp#16 | 6 | 0 | 5 | 8 | 8 | 0 | — | 352 |
| 1 | tts-mcp#3 | 6 | 0 | 5 | 6 | 6 | 0 | — | 365 |
| 1 | distill#6 | 10 | 1 | 6 | 10 | 9 | 0 | — | 545 |
| 1 | vault-ui#39 | 6 | 1 | 5 | 5 | 4 | 0 | — | 343 |
| 1 | recurring-task-creator#30 | 6 | 0 | 4 | 3 | 3 | 0 | — | 546 |
| 1 | tts-mcp#10 | 9 | 1 | 6 | 10 | 9 | 0 | — | 508 |
| 1 | discord-assistant#5 | 13 | 1 | 12 | 11 | 10 | 0 | — | 524 |
| 1 | tts-mcp#13 | 10 | 2 | 7 | 18 | 15 | 0 | — | 529 |
| 1 | github-releaser-agent#8 | 13 | 0 | 9 | 9 | 9 | 0 | — | 505 |

*Effective fixture: **11 of 20** PRs produced a scored row.* The 9 absent rows are not a zero-finding result — they were rejected before scoring, so they lower confidence without lowering recall.

## Gap-triage candidates
These findings matched no golden entry. They are **not a precision failure** — the golden set is a bootstrap from one strong-model run, and a finding it does not describe is evidence the set is incomplete.

### run 1 — bw#41 — groups/meta/k3s.py:19

**`groups/meta/k3s.py:19-23`** — only the `net.ipv4.neigh.default.*` thresholds are raised. The IPv6 neighbour table is a separate kernel table with the same 128/512/1024 defaults. No k3s node or netplan config in the repo enables IPv6, so this is future-proofing only. Add the `net.ipv6.neigh.default.gc_thresh1..3` trio if the cluster ever goes dual-stack.

### run 1 — dark-factory#71 — agents/prompt-auditor.md:494

**`agents/prompt-auditor.md:494-496`** — "Give each entry an exact find/replace pair" is mandatory for every Critical Issue, but several DoD-checklist Critical failures are not text edits: wrong inbox location (line 455, a file move), global-number filename prefix (line 456, a rename), filename/content mismatch (line 457, a rename), and a missing required XML section (an insertion with no old text). The auditor will either contort the pair or break the template. Suggested wording: "When the fix is a text edit, give an exact find/replace pair. Otherwise give the exact command, such as `git mv old new`, or the insertion anchor plus the new text." Fixing a shape for the pair, for example labelled `Find:` and `Replace:` fenced blocks, would also let callers apply it mechanically.

### run 1 — dark-factory#71 — agents/prompt-auditor.md:510

**`agents/prompt-auditor.md:510-518`** — the new `<final_step>` restates the find/replace requirement that lines 494 to 499 already carry, so two places must now stay in sync. Shrink it to the prohibition plus a pointer to the report sections. `agents/spec-verifier.md:362` already has house wording for this ("Do NOT offer additional options or follow-ups") that could be reused.

### run 1 — dark-factory#71 — CHANGELOG.md:15

**`CHANGELOG.md:15`** — says "for each Blocker/Critical issue", but the agent has no Blocker severity. Its sections are "Critical Issues" and "Recommendations". Say "Critical issue". *(rule: `changelog/conventional-prefix-required`)* is satisfied; this is wording only.

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:8

**`skills/voice/SKILL.md:8`** — Asserts a fact about a file the plugin does not own: "there is no competing always-on rule in `~/.claude/CLAUDE.md`". The claim is already false on the author's own install, whose CLAUDE.md carries a "TTS Voice Default Narrate" rule. Same overreach in the frontmatter at line 3 ("no always-on rule competes with it"). Fix: state only what the skill owns, e.g. "this skill defines what each mode speaks; a CLAUDE.md rule may select a default mode but should not restate volume policy". Adjust line 94 to match, since a rule that only picks a default mode does not stop this file being the source of truth for what the modes mean. *(rule: `agent-cmd/single-source-of-truth`)*

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:87

**`skills/voice/SKILL.md:87`** — The "never speak, in any mode" list ends with "anything at all when the user is clearly sitting there watching". That carve-out is undecidable from the model's side and directly contradicts the `narrate` contract at line 15 ("gist of every substantive answer"). It hands the model a license to skip narration whenever it guesses the user is present. Fix: scope the exception to attention signals in `on`, or state that in `narrate` the gist is unconditional.

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:8

**`skills/voice/SKILL.md:8`** — Reframe the always-on claim as a caveat ("if your own CLAUDE.md sets a voice default, keep it to naming a mode") rather than a guarantee. *(rule: `agent-cmd/gap-driven-feedback`)*

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — Frontmatter description is one 616-character run-on sentence with five clauses. Split into a trigger sentence and a caveat sentence so the discovery signal stays scannable.

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:89

**`skills/voice/SKILL.md:89`** — "`narrate` is the mode to reach for when the user asks a question and turns voice on in the same breath" leaves the actor ambiguous. Say whether Claude should treat that answer as `narrate` or merely suggest `/voice narrate`, since line 8 says the chosen arg sets the level.

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:69

**`skills/voice/SKILL.md:69-74`** — `narrate` and `interview` are mutually exclusive in the table, yet their stated use cases overlap (away from screen vs away from keyboard). Either let `interview` imply `narrate` or document that modes do not compose.

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:78

**`skills/voice/SKILL.md:78`** — Example `say("Build failed in capitalcom-gateway.")` leaks a private project name into a public MIT plugin. Use a generic name.

### run 1 — tts-mcp#16 — CHANGELOG.md:12

**`CHANGELOG.md:12-17`** — Bullets are multi-sentence paragraphs. The entry style is one line per bullet with the what and brief context. Move the rationale into the skill or commit body. Prefixes are valid and the `### Added` / `### Changed` grouping matches the repo's existing Keep a Changelog format.

### run 1 — tts-mcp#3 — commands/voice-test.md:1

**`commands/voice-test.md:1`** — frontmatter has only `description`. The steps call `mcp__tts__say`, `mcp__tts__get_voices`, `mcp__tts__get_status` and invoke `/tts-mcp:voice restart`, yet no `allowed-tools` bounds them. Add `allowed-tools` listing the three MCP tools plus `Skill(tts-mcp:voice)`. `argument-hint` is not needed since the command takes no args. *(rule: `agent-cmd/command-frontmatter`)*

### run 1 — tts-mcp#3 — CHANGELOG.md:12

**`CHANGELOG.md:12`** — Unreleased bullet starts with a backticked path, not a conventional prefix. It describes a new slash command, so it should be `feat:`. With `.maintainer.yaml` at `autoRelease: true`, the bump detector keys on that prefix, so this PR risks shipping as a patch instead of a minor. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — tts-mcp#3 — CHANGELOG.md:16

**`CHANGELOG.md:16`** — same issue on the `### Changed` bullet. Prefix it `refactor:` (behaviour moved, no new capability). The `### Added` / `### Changed` sub-headings are pre-existing repo style and were not flagged; prefixing the two bullets is the minimal fix. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — tts-mcp#3 — commands/voice-test.md:22

**`commands/voice-test.md:22`** — restart loop. SKILL.md restart step 3 (line 32) runs this command to verify. This command's step 4 ends with "run `/tts-mcp:voice restart`, then re-run this test", and the only terminal condition (step 5, "after a restart") sits after that bullet. A literal pass entered from restart can restart a second time, which drops in-flight messages for every session per SKILL.md's own caveat. Gate the restart bullet with "only if no restart happened yet this round; otherwise go to step 5", or have SKILL.md step 4 say "do not restart again".

### run 1 — tts-mcp#3 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — description now embeds an anti-trigger clause ("Activation does not run a selftest — use /tts-mcp:voice-test"). The body already says this at lines 20–24. Keeping the frontmatter to positive trigger phrases keeps the discovery signal dense. Rule `skill-writing/skill-md-frontmatter-required` verified compliant: `name` matches the directory, `description` present.

### run 1 — tts-mcp#3 — skills/voice/SKILL.md:24

**`skills/voice/SKILL.md:24`** — the skill delegates to a command by literal name. The guide documents the reverse (command wraps skill). It works at runtime and the command is 23 lines, so `agent-cmd/command-thin` is satisfied. Fine for a two-file plugin; revisit only if `voice-test` grows.

### run 1 — distill#6 — hooks/deny-generated-file-edits.sh:36

**`hooks/deny-generated-file-edits.sh:36`** — `Source:` extraction uses `\S+`, so any source path containing a space is truncated at the first space. The project's own example banner in `docs/spec.md:279` (`.../50 Knowledge Base/CLAUDE Rules`) yields `source_dir=/Users/bborbe/Documents/Obsidian/Personal/50`, and the denial then points the agent at a nonexistent directory. Fix: use `.+` like the `Regenerate:` line below it.

### run 1 — distill#6 — hooks/deny-generated-file-edits.sh:37

**`hooks/deny-generated-file-edits.sh:37-49`** — the `Regenerate:` value is read from arbitrary file content and relayed to the agent as `Then regenerate: <cmd>`, giving file-supplied text a trusted hook voice. The attacker must already control a file in the repo, so this is amplification rather than a new vector, which is why it is not Critical. Cheap fix: distill always writes `distill --source … --output …` (`pkg/distill/driver.go:315`), so allowlist `^distill --source .+ --output .+$` and fall back to the generic message otherwise.

### run 1 — distill#6 — README.md:128

**`README.md:128`** — `cp hooks/deny-generated-file-edits.sh ~/.claude/hooks/` fails when `~/.claude/hooks/` does not exist. Add `mkdir -p ~/.claude/hooks/` before the `cp`.

### run 1 — distill#6 — docs/adopting-distill.md:108

**`docs/adopting-distill.md:108`** — same missing `mkdir -p`, plus the relative `hooks/…` path assumes the reader's cwd is a distill checkout. The runbook's audience is standing in their own `$ROOT`, often with distill installed via `go install`, where no `hooks/` dir exists. Fix: mkdir, then either an absolute path to the checkout or a `curl -o` from the repo's raw URL.

### run 1 — distill#6 — CHANGELOG.md:7

**`CHANGELOG.md:7`** — `feat(hooks):` is a scoped prefix; the rule's regex `^- ([a-z]+:)` does not match it. The repo's earlier `chore(dev):` entries were harmless because a parse miss still yields a patch bump, but a mis-parsed `feat` silently downgrades minor to patch. Fix: `- feat: ship \`hooks/deny-generated-file-edits.sh\` …` and put the scope in the text, unless the bump detector is confirmed to accept scopes. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — distill#6 — hooks/deny-generated-file-edits.sh:10

**`hooks/deny-generated-file-edits.sh:10-12`** — malformed stdin or missing `jq` aborts under `set -e` with exit 5 or 127 and no JSON. Non-2 exit codes are non-blocking in the hook protocol, so this fails open. Guard with `jq -e . <<<"$input" >/dev/null 2>&1 || passthrough`.

### run 1 — distill#6 — hooks/deny-generated-file-edits.sh:33

**`hooks/deny-generated-file-edits.sh:33`** — `head -c 2048 -- "$path"` so a dash-prefixed filename cannot be parsed as an option and silently bypass the check.

### run 1 — distill#6 — hooks/deny-generated-file-edits.sh:12

**`hooks/deny-generated-file-edits.sh:12`** — `.tool_name // empty` for symmetry with line 27.

### run 1 — distill#6 — README.md:166

**`README.md:166`** — the new link line is label-colon-link while the sentence above it is prose. Reword as `See … for converting an existing hand-written CLAUDE.md.`

### run 1 — vault-ui#39 — src/vault_ui/activity.py:51

**`src/vault_ui/activity.py:51`** - `transcript_mtime` uses `claude_session_id` verbatim as a filename, but the field is documented as "session UUID or display name" and the PATCH session endpoint stores an unresolved display name as-is. For those values the transcript is never found, the badge silently degrades to file mtime, and every poll pays the full projects glob first. Fix: bail out unless `is_uuid(session_id)` (already exported by `session_resolver`, already imported in `tasks.py`). This also keeps glob metacharacters from vault frontmatter out of `root.glob`. Add a test for the display-name case.

### run 1 — vault-ui#39 — src/vault_ui/vault_cli_client.py:316

**`src/vault_ui/vault_cli_client.py:316-319`** - byte-for-byte copy of the `modified_date` block in `_parse_task` (lines 248-251). Extract a shared `_parse_iso_datetime` helper.

### run 1 — vault-ui#39 — tests/test_activity.py:36

**`tests/test_activity.py:36-47`** - the glob-fallback test only asserts `result is not None`; sibling tests bound the age. Add the same window check so it proves the right file's mtime came back.

### run 1 — vault-ui#39 — src/vault_ui/activity.py:1

**`src/vault_ui/activity.py:1`** - module docstring still says "task cards"; the module serves goal cards too since the second commit.

### run 1 — recurring-task-creator#30 — CHANGELOG.md:14

**`CHANGELOG.md:14`** — `fix(deps):` is not in the guide's allowed prefix set and fails its `^- ([a-z]+:)` regex. The guide says dependency bumps use `chore:`. Change to `- chore: bump google.golang.org/grpc to v1.82.1 ...`. Practical risk is low since the sibling `fix:` bullet already forces a patch bump, but the last commit was explicitly meant to make this line conventional and missed. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — recurring-task-creator#30 — pkg/publisher/period_token_test.go:60

**`pkg/publisher/period_token_test.go:60`** — the 2026-08-31 entry sits under the "Discriminating entries" comment, but old code also yields `2026-07` for it (Aug 31 minus one month is Jul 31, no overflow). Move it to the control block and fix its description, which currently talks about a nonexistent "August target".

### run 1 — recurring-task-creator#30 — pkg/publisher/period_token.go:105

**`pkg/publisher/period_token.go:105`** — the same `time.Date(base.Year(), base.Month(), 1, ...)` clamp is repeated at lines 105, 109, 113 with no comment. Extract a `firstOfMonth(base time.Time)` helper that preserves `base.Location()`, and add a one-line "why" comment about `AddDate` day-carry. The file otherwise comments all non-obvious date math.

### run 1 — tts-mcp#10 — mcp/package-lock.json

**`mcp/package-lock.json`** — Already behind the advisory database. Running `npm audit` on this exact lockfile today reports three open advisories (fast-uri high, hono moderate, qs moderate), and `@hono/node-server` 1.19.15 is no longer flagged at all. A plain `npm audit fix` without `--force` lands fast-uri 3.1.7, hono 4.13.7, qs 6.16.0, keeps `@modelcontextprotocol/sdk` at 1.29.0, and brings audit to zero. Verified in a temp copy, not in the repo. Re-run before merge.

### run 1 — tts-mcp#10 — CHANGELOG.md:17

**`CHANGELOG.md:17`** — The "two remaining advisories are `@hono/node-server <2.0.5`, left unfixed deliberately, Windows-only" bullet was likely accurate when written but is wrong now. It will become the permanent release note asserting a benign residual while the actual residual is a high-severity fast-uri SSRF/host-confusion set. Rewrite after the re-bump and avoid enumerating a point-in-time audit state.

### run 1 — tts-mcp#10 — Makefile.precommit:30

**`Makefile.precommit:30`** — `check` now installs the `mcp/` dependency tree but has no `npm audit` gate. The drift above happened silently within this PR's lifetime; a gate would have caught it. Add an `mcp-audit` target (`cd mcp && npm audit --audit-level=high`) as a `check` prerequisite. Tension to resolve: v0.1.0 deliberately dropped pip-audit for a lean toolchain. If that stance stands, state it in the changelog instead. *(rule: `node/make/security-gates-in-check`)*

### run 1 — tts-mcp#10 — Makefile.precommit:26

**`Makefile.precommit:26`** — `make check` and `make precommit` now hard-require Node, npm and network access, but README "Prerequisites" still says "Node.js 18+ (optional)", the README Makefile table and CLAUDE.md "Makefile Conventions" still describe `make check` as Python lint plus typecheck, and CI pins Node 22 while README says 18+. Update both docs and pin one Node version locally (`engines` or `.nvmrc`) matching CI.

### run 1 — tts-mcp#10 — CHANGELOG.md:16

**`CHANGELOG.md:16-21`** — The three new `## Unreleased` bullets carry no conventional prefix. The rule owner rated this Important with a caveat: the whole file uses Keep-a-Changelog subsections and the maintainer bot already cut v0.1.0 from that format, so this is an inherited repo convention, not a PR regression. Decide repo-wide. If adopting prefixes, both dependency and tooling bullets are `chore:`, not `fix:`. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — tts-mcp#10 — CHANGELOG.md:14

**`CHANGELOG.md:14-17`** — A dependency/security bump is filed under `### Fixed`. This file's own precedent for npm-audit bumps is `### Security` (the 2026-06-13 section), and bullet 17 describes a non-fix. Move both to `### Security` or fold 17 into 16.

### run 1 — tts-mcp#10 — CHANGELOG.md:16

**`CHANGELOG.md:16`** — No before/after versions for the bumped packages; the changelog guide asks for versions on dependency updates. From the lockfile diff: ``` fast-uri 3.1.0→3.1.4 hono 4.12.9→4.12.32 qs 6.15.0→6.15.3 ip-address 10.1.0→10.3.1 express-rate-limit 8.3.2→8.6.0 esbuild 0.27.4→0.28.1 body-parser 2.2.2→2.3.0 tsx 4.21.0→4.23.1 ```

### run 1 — tts-mcp#10 — Makefile.precommit:26

**`Makefile.precommit:26`** — `npm ci` lives inside `mcp-typecheck`, so every `make check` wipes and reinstalls `mcp/node_modules`. Split into `mcp-install` (a `precommit` prerequisite next to `sync`) and a pure `mcp-typecheck`, matching the Node Makefile guide's install/check separation.

### run 1 — tts-mcp#10 — .github/workflows/ci.yml:39

**`.github/workflows/ci.yml:39`** — CI pins Node 22 while `@types/node` is `^25` (moved 25.5.0 to 25.9.5 in this lockfile), so `tsc` accepts Node 25 APIs that Node 22 lacks at runtime. Align `@types/node` to `^22` or move CI to the runtime the types describe.

### run 1 — discord-assistant#5 — src/config.js:182

**`src/config.js:182`** — Bot and shim wake regexes disagree on non-ASCII input: `\W` is ASCII-only in JS but Unicode-aware in Python. Probed: "Ähm hey bot, was ist meine Aufgabe?" is addressed on the bot side and not on the shim side. That direction arms `answering`, the shim answers with silence, nothing clears the flag, and typed turns are `busy` for the full five-minute cap. The comment at lines 135-136 calls drift "cosmetic"; the PR's own changelog explains why it is not. Fix: pick one semantic (`re.ASCII` in Python, or `u` flag with `[^\p{L}\p{N}_]` in JS) and add a parity test that runs both implementations over one shared utterance list.

### run 1 — discord-assistant#5 — config.example.yaml:65

**`config.example.yaml:65`** — `voice.wake_phrases` is read by the shim only. The bot never opens this file; it reads `SHIM_WAKE_PHRASES` from env alone. A YAML-only custom list therefore diverges the two sides on every utterance, including the wedge direction above for every bot-side match the shim rejects. Fix: state in the comment that the env var is the only setting both processes see, or make the bot read the same file.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:110

**`shim/claude_openai_shim.py:110`** — Boolean parsing is still a blacklist: any unrecognised token is true, while the Node `flag()` falls back to the default. `SHIM_UNSAFE=fasle` yields `UNSAFE=True`, which the startup banner reports as tools UNRESTRICTED. Strip order is also wrong: whitespace is trimmed before quotes, so a Make-quoted `"false "` reads true while Node reads false. The diff comment claims agreement "on every spelling". Fix: mirror the allowlist plus fallback, and strip quotes before whitespace.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:813

**`shim/claude_openai_shim.py:813`** — Comment lists a "so," lead-in among the three live failures the lead-skip fixes, but "so" is deliberately not skippable (test and changelog both say "so, hey bot" stays quiet). Reword so the third failure is clearly left by design.

### run 1 — discord-assistant#5 — local.env.example:62

**`local.env.example:62`** — Default list ships "hey but" and "hey bought", both ordinary English openers. "Hey but that's not what I meant" said to a colleague gets answered aloud by a session with vault access. Consider defaulting to "hey bot" only and letting deployments add mishearings from their own transcripts.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:832

**`shim/claude_openai_shim.py:832`** — No Python tests exist for `is_addressed`, `strip_wake_phrase` or `setting()`. Pre-existing gap, but this PR adds ~100 lines of regex logic that already failed three times live.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:799

**`shim/claude_openai_shim.py:799`** — `WAKE_PHRASES` relies on `str(raw)`; a YAML list stringifies into garbage phrases instead of erroring.

### run 1 — discord-assistant#5 — test/voice.test.js:543

**`test/voice.test.js:543`** — Barge-in test mutates the shared config singleton. `Session` could accept `interruptResponse` as an option. *(rule: `node/architecture/inject-dependencies`)*

### run 1 — discord-assistant#5 — CHANGELOG.md:3

**`CHANGELOG.md:3`** — Preamble ("All notable changes…" plus SemVer bullets) is absent. Pre-existing on the base branch, not moved by this PR. *(rule: `changelog/preamble-frozen`)*

### run 1 — discord-assistant#5 — CHANGELOG.md:5

**`CHANGELOG.md:5-88`** — Bullets run 10-15 lines each. The guide's format is one line per entry, with rationale in commit bodies.

### run 1 — tts-mcp#13 — src/tts/engine.py:212

**`src/tts/engine.py:212-225`** — `Qwen3Engine.validate_model` never checks the configured `language` against the model. mlx-audio silently sets `language_id = None` when `language.lower()` is not in `codec_language_id` (verified in `qwen3_tts.py:392-395`), so a typo like `language: Germann` generates unconditioned audio with no error. Call `model.get_supported_languages()` in `validate_model` and raise on mismatch.

### run 1 — tts-mcp#13 — src/server.py:353

**`src/server.py:353`** — `instruct` is queued for every engine; on voxtral the request returns 202 and only fails later inside the worker, visible via `GET /status`. The handler already rejects an unknown voice with 400 one line above. Reject up front with 422 via `state.engine.kind` or a `supports_instruct` capability on `TTSEngine`, and normalize `body.instruct or None` so `""` behaves like the empty-voice case.

### run 1 — tts-mcp#13 — README.md:133

**`README.md:133-139`** — "Download a Model" still says "Presents three Voxtral 4B variants" and lists only those three. The script now offers five options and prints `engine:` / `language:` / `default_voice:` hints for qwen3. Same for **`README.md:146`** ("downloads a Voxtral model").

### run 1 — tts-mcp#13 — src/tts/engine.py:249

**`src/tts/engine.py:249`** — `cast(CustomVoiceModel, model)` hides that `TTSEngine.generate(model: TTSModel)` admits a model that is not a `TTSModel`. Safety depends on `validate_model` having run on a separate path. Consider a generic `TTSEngine[M]` or folding the capability check into `generate`.

### run 1 — tts-mcp#13 — src/tts/generate.py:17

**`src/tts/generate.py:17-24`** — the `(engine, model, text, voice, instruct, stream, streaming_interval)` clump repeats across five signatures with two adjacent `str` params. A `GenerationRequest` dataclass would remove transposition risk before a third engine arrives.

### run 1 — tts-mcp#13 — src/main.py:299

**`src/main.py:299-307`** and **`src/server.py:693-701`** — byte-identical engine/language parsing blocks. Extract one helper in `src/tts/config.py`.

### run 1 — tts-mcp#13 — src/server.py:284

**`src/server.py:284-285`** — `StatusResponse` gained `= None` defaults. Harmless and only constructed with all fields, but unrelated to this PR.

### run 1 — tts-mcp#13 — src/server.py:357

**`src/server.py:357`** — the `/say` debug log omits `instruct`.

### run 1 — tts-mcp#13 — mcp/tts-mcp.ts:226

**`mcp/tts-mcp.ts:226`** — `instruct` logged untruncated while `text` is sliced to 80 chars. Mirror the slice.

### run 1 — tts-mcp#13 — src/tts/protocols.py:36

**`src/tts/protocols.py:36-38`** — docstring says CustomVoice models "do not take a `voice` argument on `generate`". The real `Model.generate()` does accept `voice` and forwards it as `speaker`. Reword to "uses `speaker`/`language` names on `generate_custom_voice`".

### run 1 — tts-mcp#13 — src/tts/engine.py:92

**`src/tts/engine.py:92-104`** vs **`src/tts/engine.py:212-225`** — `hasattr` + `is None` in one engine, `getattr(..., None)` in the other. Pick one.

### run 1 — tts-mcp#13 — src/tts/engine.py:153

**`src/tts/engine.py:153`** and **`src/tts/engine.py:286`** — missing and empty `language` raise the same message from two places. Have `build_engine` reject falsy so the constructor check is dead, or differentiate messages.

### run 1 — tts-mcp#13 — scripts/download-model.sh:101

**`scripts/download-model.sh:101`** — inline `speech_tokenizer` file list. Extract to a named array like the other two. The list itself matches the HF repo exactly (verified via the HF API for both quantizations).

### run 1 — tts-mcp#13 — tests/test_server.py:92

**`tests/test_server.py:92-93`** — test-double attribute rename to `_sample_rate` / `_lead_silence_ms` is a stray cosmetic edit.

### run 1 — tts-mcp#13 — README.md:28

**`README.md:28`**, **`README.md:210`**, **`README.md:186`** — "Voxtral model weights", "Different Voxtral voices", "24000 for Voxtral" are now engine-agnostic statements. **`README.md:57`** diagram still names `src/tts.py`, inconsistent with the tree the PR updated below it.

### run 1 — github-releaser-agent#8 — pkg/steps_planning.go:44

**`pkg/steps_planning.go:44-91`** — `isHexString`, `sameCommit`, `releaseTagVerdict` are new business logic (the safety bound of the auto-complete path) as same-package private helpers called from a method, with `export_test.go` aliases as the test seam. Rule asks for a small interface + constructor + struct so the matcher is injectable and mockable. Note the file already does this for `classifyValidationFailure`, so this follows existing convention; your call whether to hold the line here. *(rule: `go-composition/no-same-package-private-helper-for-business-logic`)*

### run 1 — github-releaser-agent#8 — pkg/githubtags/tags.go:33

**`pkg/githubtags/tags.go:33-35`** — the interface comment restructure dropped "Implementations MUST be safe for concurrent use." and the `LatestSemverTag` contract paragraph, and the summary now describes only the highest-semver lookup. `CommitSHAForTag` has a rich method doc while `LatestSemverTag` has none. Restore the concurrency sentence and add a short `LatestSemverTag` method comment.

### run 1 — github-releaser-agent#8 — pkg/steps_planning.go:86

**`pkg/steps_planning.go:86`** — `releaseTagVerdict` encodes a binary decision as a string token compared against `PlanOutcomeNothingToRelease`. A bool or a package-local typed string would remove one moving part.

### run 1 — github-releaser-agent#8 — pkg/steps_planning.go:750

**`pkg/steps_planning.go:750-753`** — `handleValidationFailure` takes six consecutive string parameters. A small struct for owner/name/ref would cut positional-argument risk.

### run 1 — github-releaser-agent#8 — pkg/steps_planning.go:761

**`pkg/steps_planning.go:761`** — the `release_tag_check` summary log prints `tag_commit=` empty on the fail-open path. Consider a literal placeholder so the line is self-contained.

### run 1 — github-releaser-agent#8 — pkg/steps_planning.go:718

**`pkg/steps_planning.go:718`** — on the P1 path `CommitSHAForTag` re-paginates the full tag list that `LatestSemverTag` just paginated in the same run. Rare path, negligible cost, but a cached tag list would halve it.

### run 1 — github-releaser-agent#8 — pkg/steps_planning_test.go:2296

**`pkg/steps_planning_test.go:2296`** — `NotTo(Equal(NeedsInput))` directly after `To(Equal(Done))` on the same field is redundant.

### run 1 — github-releaser-agent#8 — pkg/steps_planning_test.go:2604

**`pkg/steps_planning_test.go:2604`** — the `sameCommit` table has no whitespace-padded entry to prove the `TrimSpace`, and no Run-level test feeds an uppercase ref end to end.

### run 1 — github-releaser-agent#8 — pkg/steps_planning_test.go:2256

**`pkg/steps_planning_test.go:2256`** — the spec-002 block rebuilds the same four-line fixture in most specs. A `BeforeEach` would remove most of it. Matches the file's existing style, so purely optional.
