id: node/http/explicit-body-limit-ts
valid:
  - |
    app.use(express.json({ limit: '1mb' }));
  - |
    app.use(express.urlencoded({ extended: true, limit: '100kb' }));
invalid:
  - |
    app.use(express.json());
  - |
    app.use(express.urlencoded());
