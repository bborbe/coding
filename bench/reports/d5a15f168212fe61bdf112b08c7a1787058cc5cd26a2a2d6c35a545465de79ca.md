d5a15f168212fe61bdf112b08c7a1787058cc5cd26a2a2d6c35a545465de79ca

## Configuration
- model: m3
- effort: high
- mode: full
- config_hash: d5a15f168212fe61bdf112b08c7a1787058cc5cd26a2a2d6c35a545465de79ca
- rules_commands_hash: ecc803331f860b845a6a1b8a103e889bce02520e8cc04ea88102de74c8d5600d
- prs_version: curated-1
- coding version: 0.45.0
- golden baseline coding version: v0.35.6
- golden version: golden-curated-3
- runner_version: 1
- rows skipped: 0
- cost: not recorded — the ledger carries no cost field.

*golden-curated-3* carries 20 `rejected` entries, so precision is measurable: reporting one costs precision. With so few adjudicated, a single hit still moves the number a long way — read it as directional, not calibrated.

No signature embeds a line reference, so a re-report of the same issue at a different line still matches. `recall` measures issue detection.

## Runs
| run | span | PRs | complete | golden in scope | findings | hits | misses | matched rejected | gap candidates | recall | precision | wall time (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-08-17T19:13:33.467258+00:00 … 2026-08-17T21:15:08.486823+00:00 | 20/20 | complete | 158 | 80 | 10 | 128 | 0 | 70 | 0.072 | 1.000 | 7368 |

## Per-PR
| run | pr_id | golden in scope | hits | misses | findings | gap candidates | dropped items | missing sections | duration (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | tts-mcp#20 | 1 | 0 | 1 | 1 | 1 | 0 | — | 60 |
| 1 | github-pr-review-agent#11 | 16 | 0 | 14 | 0 | 0 | 0 | — | 958 |
| 1 | quant#109 | 10 | 2 | 8 | 7 | 5 | 0 | — | 251 |
| 1 | node-skeleton#2 | 12 | 1 | 11 | 5 | 4 | 0 | — | 385 |
| 1 | python-skeleton#3 | 17 | 0 | 16 | 2 | 2 | 2 | — | 244 |
| 1 | vault-cli#68 | 1 | 0 | 1 | 0 | 0 | 0 | — | 157 |
| 1 | backup#15 | 4 | 0 | 2 | 2 | 2 | 0 | — | 293 |
| 1 | bw#41 | 3 | 0 | 2 | 0 | 0 | 0 | — | 260 |
| 1 | dark-factory#71 | 2 | 0 | 2 | 0 | 0 | 0 | — | 253 |
| 1 | tts-mcp#16 | 6 | 0 | 5 | 4 | 4 | 0 | — | 217 |
| 1 | tts-mcp#3 | 6 | 0 | 5 | 10 | 10 | 0 | — | 386 |
| 1 | distill#6 | 10 | 0 | 7 | 1 | 1 | 0 | — | 542 |
| 1 | vault-ui#39 | 6 | 0 | 6 | 8 | 8 | 0 | — | 566 |
| 1 | recurring-task-creator#30 | 6 | 1 | 3 | 10 | 9 | 0 | — | 426 |
| 1 | tts-mcp#10 | 9 | 0 | 7 | 2 | 2 | 0 | — | 417 |
| 1 | discord-assistant#5 | 13 | 3 | 10 | 8 | 5 | 0 | — | 703 |
| 1 | tts-mcp#13 | 10 | 1 | 9 | 1 | 0 | 0 | — | 319 |
| 1 | github-releaser-agent#8 | 13 | 0 | 9 | 7 | 7 | 0 | — | 456 |
| 1 | discord-assistant#1 | 9 | 2 | 7 | 8 | 6 | 0 | — | 382 |
| 1 | helm#3 | 4 | 0 | 3 | 4 | 4 | 0 | — | 94 |

*Effective fixture: **20 of 20** PRs produced a scored row.* **2** findings carried no matching key and never reached scoring.

## Gap-triage candidates
These findings matched no golden entry. They are **not a precision failure** — the golden set is a bootstrap from one strong-model run, and a finding it does not describe is evidence the set is incomplete.

### run 1 — tts-mcp#20 — mcp/package-lock.json

**`mcp/package-lock.json`** — `fast-uri` bumped 3.1.4 → 3.1.5. `make precommit` still reports `1 moderate severity vulnerability` from `npm audit` in `mcp/`; consider running `cd mcp && npm audit` to confirm whether the bump resolves it or whether a separate advisory is open against a different transitive dep.

### run 1 — quant#109 — task/recurring-schedules/prod/renew-gmail-oauth-tokens.yaml:64

**`task/recurring-schedules/prod/renew-gmail-oauth-tokens.yaml:64`** — `due_date: "{{next_sun_date}}"` can land *after* the deadline it tracks. The body states "Google's tokens expire **every 7 days** for unverified apps", and the schedule is Saturday-to-Saturday — so the 7-day window closes on the next Saturday. A Sunday due date means the task is not flagged overdue until a day past expiry, i.e. the overdue signal arrives only once the tokens are already dead. This is the one file in the `next_sun_date` group with a hard external expiry rather than soft slack; it should be `{{current_date}}` like the other same-day-critical Saturday entries.

### run 1 — quant#109 — task/recurring-schedules/prod/plan-weekend.yaml:65

**`task/recurring-schedules/prod/plan-weekend.yaml:65`** — `due_date: "{{next_sun_date}}"` contradicts the file's own body, which says to plan "as the first thing **Saturday morning**" and exists to sequence Sat+Sun before the weekend starts. Due Sunday means the task is still "on time" when the weekend it plans is nearly over. It is also the only `priority: 1` task in the batch, yet it got a looser deadline than `priority: 2` same-day chores (`shutdown-k3s`, `opnsense-update`, `turn-on-hell`, all `{{current_date}}`) — a priority/deadline inversion.

### run 1 — quant#109 — task/recurring-schedules/prod/plan-next-week.yaml:67

**`task/recurring-schedules/prod/plan-next-week.yaml:67`** — the correctness of all 7 `{{next_sun_date}}` due dates rests on unverified boundary semantics. Every one of these files is scheduled on **Saturday**, so "next Sunday" is only *tomorrow* if the resolver treats "next `<weekday>`" inclusively/as the upcoming occurrence; under a strict "skip to next week" reading it resolves to **Sat+8 days**, making these due dates a week too late. The resolver lives in `recurring-task-creator` (separate repo) and cannot be verified from here. The pre-existing body usage at `plan-weekend.yaml:18` ("Sat `{{next_sat_date}}` → Sun `{{next_sun_date}}`", rendered *on* a Saturday) implies the inclusive reading, but it shifts consistently under either interpretation, so it is not proof. Worth confirming against the resolver before this ships to prod — a `{{current_date}}` mistake is visible immediately, this one is silent.

### run 1 — quant#109 — task/recurring-schedules/prod/monthly-review.yaml

**`task/recurring-schedules/prod/monthly-review.yaml`** — partial rollout within one family. 51 other prod schedules keep `planned_date` with no `due_date`, which is fine as a staged first batch, but the periodic-planning family is now split down the middle: `weekly-review.yaml` and `plan-next-week.yaml` got `due_date` while their direct siblings `monthly-review.yaml`, `quarter-review.yaml`, `plan-month.yaml`, `plan-year.yaml` did not. Consider finishing that family together so the cadence set stays uniform.

### run 1 — quant#109 — task/recurring-schedules/prod/plan-next-week.yaml:40

**`task/recurring-schedules/prod/plan-next-week.yaml:40`** — the new pre-check bundles four separate conditions (previous week `reviewed: true`, empty `vault-cli decision list`, remediation via `/complete-week`, boss ack) into one checkbox. As a single `- [ ]` it can only be ticked all-or-nothing; splitting into two checkboxes would make a partial failure visible.

### run 1 — node-skeleton#2 — src/handlers/health.ts:6

**`src/handlers/health.ts:6,40-42`** — `healthRouter` reads `config.build` at module scope; the rest of the file already injects `isReady` via `HealthRouterOptions`. Add `build` to `HealthRouterOptions` and pass `config.build` from `createApp` so the factory reads no module state. *(rule: `node/architecture/inject-dependencies`)* — **Pre-existing**, not introduced by this PR; flagged because the PR is the natural place to fix it and the conversion touches every line of this file.

### run 1 — node-skeleton#2 — package.json:6

**`package.json:6`** — `"main": "src/index.ts"` points at a TypeScript source file. `npm start` uses the `scripts` field, so `main` is unused today, but it is a latent footgun: tooling that reads `main` (older bundlers, a future clone consumed as a library, Node < 22.18 doing `require('node-skeleton')`) resolves a `.ts` entrypoint and breaks. Drop the field, or point it at a real JS entry.

### run 1 — node-skeleton#2 — src/config.ts:28

**`src/config.ts:28-56` / `src/index.ts:8-12`** — `config.check()` is now opt-in (called only from `index.ts`). The PR replaces the old throw-at-import pattern with collected-errors-then-exit, which is the guide's Good pattern — but a clone that imports `config` from another module without calling `check()` boots with invalid config. Document `config.check()` as a mandatory startup step in `node-service-guide.md` / CLAUDE.md so clones preserve the call.

### run 1 — node-skeleton#2 — README.md:64

**`README.md:64-99`** — README carries internal-rationale sections ("Liveness vs readiness", "Graceful shutdown ordering", "Security gates") that now duplicate the agent-operational invariants added to CLAUDE.md. Defensible because the skeleton's product is the reference architecture, but worth tightening as it grows so README and CLAUDE.md don't drift into two sources of truth. *(rule: `readme/user-facing-not-agent-context`)*

### run 1 — python-skeleton#3 — CHANGELOG.md:18

**`CHANGELOG.md:18`** — `- ci: install trivy in CI` uses the prefix `ci:`, which is NOT in the recognised conventional prefix set (`feat:`, `fix:`, `refactor:`, `test:`, `docs:`, `chore:`, `perf:`). Per the changelog guide, build/tooling bullets map to `chore:`; dark-factory's version-bump parser will fail to classify this bullet and break automated bump detection. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — python-skeleton#3 — README.md:76

**`README.md:76-94`** — the new `## Security gates` section is agent-operational content rather than user-facing content. It explains internal rationale (uv.lock gitignore, pip-audit severity threshold, why `osv-scanner` is excluded, ruff `S104`/`S101` suppression reasoning, `.trivyignore` conventions) and includes a cross-skeleton maintainer comparison table. Per the README guide, build commands/architecture internals belong in CLAUDE.md or docs/, and README should carry only brief Development/usage notes. *(rule: `readme/user-facing-not-agent-context`)*

### run 1 — backup#15 — Makefile:127

**`Makefile:127`** — The diff edits the Go `build` target but only wires `NPM_REGISTRY`. The Go build path is still missing all three canonical build args (`BUILD_GIT_VERSION`, `BUILD_GIT_COMMIT`, `BUILD_DATE`); `Dockerfile:1-4` uses `-ldflags "-s"` with no `-X` injection and no `ARG` declarations, and `main.go` has no matching fields. This is the right place to add the three args alongside `NPM_REGISTRY` since the Makefile line is being touched anyway. *(rule: `go-build-args/three-args-required`)*

### run 1 — backup#15 — Makefile:47

**`Makefile:47`** — Root `check` target is Go-only (`vet vulncheck osv-scanner trivy`) and never reaches the frontend's TypeScript type-checker. `frontend/tsconfig.json` exists and `frontend/package.json` has a `build:check` script (`vue-tsc && vite build`), but no Makefile target invokes it; `vite build` strips types without validating them. Pre-existing, but surfaced by reviewing the Makefile this diff edits. *(rule: `node/make/typecheck-for-typescript`)*

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — The frontmatter `description:` is 101 words. Per `claude-code-skill-writing-guide.md`, the description is Claude's pattern-match signal for auto-activation; this length dilutes the headline trigger (the new `narrate` mode). Suggested: keep the first sentence (what + when), move the full `Args:` list out of the description, soften the "sole authority" claim to one short clause.

### run 1 — tts-mcp#16 — skills/voice/SKILL.md

**`skills/voice/SKILL.md`** — No explicit `## Success Criteria` section. The guide checklist expects one. For a mode-setting skill the natural shape is: confirmation printed (and, for `on`/`narrate`/`interview`, spoken), `status` reports the intended mode, post-`restart` `/health` + `/tts-mcp:voice-selfcheck` pass, subsequent turns respect the "How much to speak" table.

### run 1 — tts-mcp#16 — skills/voice/SKILL.md

**`skills/voice/SKILL.md`** — No explicit `## Prerequisites` section. The skill assumes a launchd-managed `com.bborbe.tts-mcp`, the `mcp__tts__say` tool, and an `engine` (`qwen3` or `voxtral`); these are scattered across the body. One block at the top would make the assumption contract explicit.

### run 1 — tts-mcp#16 — CHANGELOG.md:10

**`CHANGELOG.md:10,14`** — The new `## Unreleased` section uses `### Added` and `### Changed` subheadings. `docs/changelog-guide.md` prose says "Flat list — no `### Added` / `### Fixed` categories" — but every existing versioned section in this repo (`v0.4.0`, `v0.3.0`, `v0.2.0`, ...) already uses the same subheading pattern, and the agent-auditor confirmed the rule `changelog/preamble-frozen` does not trigger here (preamble untouched). Treat as guide-vs-repo-convention drift, not a defect; flagged only for awareness.

### run 1 — tts-mcp#3 — commands/voice-test.md:1

**`commands/voice-test.md:1-3`** — The new slash command declares only `description:`; the guide's canonical frontmatter always includes `allowed-tools`, and missing it means "unbounded permissions" — the agent invoking this command retains its full default tool set when only three MCP tools (`mcp__tts__say`, `mcp__tts__get_voices`, `mcp__tts__get_status`) are needed. Add the bounded `allowed-tools` block. *(rule: `agent-cmd/command-frontmatter`)*

### run 1 — tts-mcp#3 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — The `description:` plain scalar ends with `Args: on | off | status | interview | restart.`; `Args:` followed by a space terminates the scalar and `yaml.safe_load` fails with "mapping values are not allowed here" (verified). This is **pre-existing** on `bench-base-3`, but this PR edits line 3 and is the right place to fix it — quote the description (`description: "..."`) or drop the redundant `Args:` suffix (the body already documents args in `## Args`). *(rule: `skill-writing/skill-md-frontmatter-required`)*

### run 1 — tts-mcp#3 — commands/voice-test.md

**`commands/voice-test.md` (new)** — `voice-test` appears in zero of `README.md`, `AGENTS.md`, `.claude-plugin/*`, or any agent/command registry. This PR introduces the first command and the first `/tts-mcp:` namespace surface, making the gap user-facing. Add a "Slash commands" / "Skills" section to `README.md` documenting `/tts-mcp:voice-test` (what it does, when to run, how it differs from `/voice on`), and an `llms.txt` entry. *(rule: `agent-cmd/gap-driven-feedback`)*

### run 1 — tts-mcp#3 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — The added sentence (`Activation does not run a selftest — use /tts-mcp:voice-test to verify the audio path.`) is "what changed" prose, not a when-to-activate trigger. The trigger cases (silence, device switch, restart/fix) are already covered earlier in the same description, and the "No selftest on activation" body section (`SKILL.md:20-24`) documents the behavior change. Worse, the word "selftest" can pattern-match a user request for a selftest and incorrectly activate `/voice`. Delete the sentence. *(rule: `skill-writing/skill-md-frontmatter-required`)*

### run 1 — tts-mcp#3 — commands/voice-test.md:15

**`commands/voice-test.md:15,20`** — Step 4 references `mcp__tts__get_status` with the test `message_id`, but step 1 never instructs the agent to *capture* the `message_id` returned by `mcp__tts__say`. An agent following the steps top-down may not retain the ID, forcing a re-say or guess. Add "capture the returned `message_id`" to step 1.

### run 1 — tts-mcp#3 — commands/voice-test.md:23

**`commands/voice-test.md:23` vs `skills/voice/SKILL.md:33`** — Failure-mode taxonomy drift: the command lists `server unreachable / stuck queue / device error` (line 23) and `error = synth or device failure` (line 20); the skill lists `server unreachable / stuck queue / synth error` (line 33). Pick one canonical list (the command is the source of truth) and reduce the skill to a pointer. *(rule: `agent-cmd/single-source-of-truth`)*

### run 1 — tts-mcp#3 — commands/voice-test.md:15

**`commands/voice-test.md:15`** — Restates the throwaway-word rationale ("lead throwaway word per the playbook — CoreAudio clips the first word") that already lives in `skills/voice/SKILL.md:41-42`. The pointer is correct; the re-explanation is what can drift. Trim to "lead throwaway word per the playbook". *(rule: `agent-cmd/single-source-of-truth`)*

### run 1 — tts-mcp#3 — CHANGELOG.md:12

**`CHANGELOG.md:12,16`** — New `## Unreleased` bullets start with backticked identifiers (`- `commands/voice-test.md` ships ...`, `- `/tts-mcp:voice on` and ...`), not `feat:`/`fix:` etc. Pre-existing repo convention is Keep-a-Changelog categorized sections (`### Added`, `### Changed`) as declared in lines 5-6 and used throughout `## v0.1.0` — the new block is internally consistent. Either adopt conventional prefixes or document the deviation explicitly. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — tts-mcp#3 — skills/voice/SKILL.md

**`skills/voice/SKILL.md`** (whole file) — No explicit "Success Criteria" section. The guide's checklist lists one. For a session-mode toggle + playbook, a 2-3 bullet "Success = mode confirmed on screen, spoken rules applied" line would make completion unambiguous. Pre-existing, not introduced by this PR.

### run 1 — tts-mcp#3 — commands/voice-test.md

**`commands/voice-test.md`** — The new command carries the full inline procedure and its frontmatter has no `allowed-tools: Skill(tts-mcp:voice-test)`. It deviates from the guide's thin-wrapper convention. Optional — either accept the inline-procedure design deliberately or convert to a `Skill()` wrapper. *(rule: `agent-cmd/command-thin`)*

### run 1 — distill#6 — CHANGELOG.md:7

**`CHANGELOG.md:7`** — `- feat(hooks): ship ...` uses a scope-suffixed conventional prefix. The rule's enforcement regex `^- ([a-z]+:)` requires a `:` immediately after `[a-z]+`, so the `(hooks)` scope causes the regex to fail and `feat(hooks):` is not in the allowed prefix set (`{feat:, fix:, refactor:, test:, docs:, chore:, perf:}`). The other two new entries use bare `docs:` and are fine. **Fix:** drop the scope → `- feat: ship \`hooks/deny-generated-file-edits.sh\` ...`. Severity is Important (not Critical) because the semantic intent is preserved for any parser that tolerates scoped conventional commits; the practical risk is a missed/patch bump rather than broken structure. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — vault-ui#39 — src/vault_ui/api/tasks.py:705

**`src/vault_ui/api/tasks.py:705-709` and `:1794-1798`** — Both `_goal_to_response` and `_task_to_response` carry the identical five-line block `compute_activity_date(modified, claude_session_id, derive_claude_project_dir(vault_config.vault_path, vault_config.session_project_dir))`, and the Claude-project-dir derivation is now recomputed at four sites. Add `VaultConfig.claude_project_dir` as a property (move the encoding from `cleanup.py` into `config.py`) and collapse both callsites to `compute_activity_date(modified, session_id, vault_config.claude_project_dir)`. Removes the copy-paste block and centralises the seam on the object that owns its inputs.

### run 1 — vault-ui#39 — src/vault_ui/activity.py:22

**`src/vault_ui/activity.py:22`** — `_claude_projects_root()` hard-codes `Path.home() / ".claude" / "projects"` independently of `cleanup.py:30` (where the same root is implied). The two are consistent today but if the Claude config root ever relocates (e.g. `CLAUDE_CONFIG_DIR`) the direct path and the glob fallback would drift apart — and the fallback exists precisely to catch sessions that landed outside the vault's own encoded-cwd dir. Share one source of truth (a `_claude_projects_root()` helper from `cleanup.py`, or derive the fallback from `project_dir.parent` and drop the parameter).

### run 1 — vault-ui#39 — src/vault_ui/activity.py:61

**`src/vault_ui/activity.py:61-64`** — `transcript_mtime` returns the first glob match, not the freshest. If the same session id ever lives under multiple project directories (a session resumed from different cwds), directory iteration order decides the answer rather than the function name's contract. `return max((... for ... ), default=None, key=lambda p: p.stat().st_mtime)` (or collect-then-max) restores the implied semantics. *(rule: `python-architecture/constructor-injection-only` — single source of truth for the abstraction seam)*

### run 1 — vault-ui#39 — src/vault_ui/activity.py:32

**`src/vault_ui/activity.py:32-35`** — `_as_utc` assumes naive datetimes from vault-cli are UTC. The pre-existing `tasks.py:553-555` convention makes the same assumption, so this PR isn't a new divergence — but the assumption is unverified and a naive-local parse would skew displayed activity age by the UTC offset. Confirm vault-cli's emission format; if it is indeed naive-UTC, leave a code comment to lock the contract for future readers; if it is naive-local, fix the helper.

### run 1 — vault-ui#39 — tests/test_activity.py

**`tests/test_activity.py`** — All 15 tests exercise success paths; the OSError branch at `activity.py:65-66` (the only defensive-IO path in the module) and the "aware datetime passed through unchanged" branch at `activity.py:35` are untested. Add a `monkeypatch`-driven `Path.glob` raising test for the OSError branch and an explicit aware-input test for `_as_utc`. *(rule: `python-logging/configure-once-in-main` is satisfied — this is a separate test-coverage concern)*

### run 1 — vault-ui#39 — src/vault_ui/activity.py:59

**`src/vault_ui/activity.py:59-64`** — Per-card glob over `~/.claude/projects/*/` on every list request. The glob fallback is functionally justified (sessions can start outside the vault), but a per-request cache of the resolved `transcript_mtime` lookup (or a request-scoped dict) would avoid re-scanning on every UI poll. Acceptable now; revisit if list sizes grow.

### run 1 — vault-ui#39 — src/vault_ui/vault_cli_client.py:316

**`src/vault_ui/vault_cli_client.py:316-319`** — `_parse_goal`'s `modified_date` parsing duplicates `_parse_task`'s verbatim 4-line block (guard → `fromisoformat` → `suppress(ValueError, TypeError)`). Extract a private `_parse_modified_date(data)` helper used by both.

### run 1 — vault-ui#39 — src/vault_ui/activity.py:59

**`src/vault_ui/activity.py:59` and `:75`** — `projects_root` is a parameter on both `transcript_mtime` and `compute_activity_date`, but neither production callsite (`tasks.py:705`, `tasks.py:1794`) passes it — they rely on the hard-coded default. The seam exists for tests but is uncoupled from the callsite that knows the actual projects root (`vault_config`). Fold it into the `VaultConfig.claude_project_dir` refactor above.

### run 1 — recurring-task-creator#30 — pkg/publisher/period_token.go:60

**`pkg/publisher/period_token.go:60-65`** — The `PeriodTokenBuilder` interface GoDoc describes the token formula (Recurrence, PeriodOffset, weekday suffix from fire date) but does not document the new day-1 anchor invariant this very PR introduces. The comment is no longer wrong, but it under-specifies the behavior the fix deliberately guarantees. Add one sentence, e.g.: *"Monthly, Quarterly, and Yearly tokens anchor the PeriodOffset shift to the first of the fire date's month before calling AddDate, so a late-month firing date (e.g. the 31st) cannot overflow into the adjacent period."* *(rule: `go-doc/exported-item-must-have-comment` — completeness of documented contract)*

### run 1 — recurring-task-creator#30 — pkg/publisher/period_token_test.go:68

**`pkg/publisher/period_token_test.go:68-73`** — The `Entry("fire on 31st, offset=-1 → July (not August) — July has 31 days but August target does too", ...)` sits in the `// Discriminating entries` comment block but is actually a control/regression entry (both old and new code produce `2026-07`; July has 31 days so `AddDate` cannot overflow into August). Move it into the control block or reword its description to *"control — July has 31 days so old code also yields July."* The inline parenthetical already hints at this, but the section header is misleading.

### run 1 — recurring-task-creator#30 — pkg/publisher/period_token.go:105

**`pkg/publisher/period_token.go:105,109,113`** — The first-of-month construction `time.Date(base.Year(), base.Month(), 1, 0, 0, 0, 0, base.Location())` is duplicated across three cases. A shared `firstOfMonth(base time.Time) time.Time` helper alongside the existing `firstOfPreviousMonth` in `render.go:54` would deduplicate, though the prompt explicitly accepts inline-per-case as well.

### run 1 — recurring-task-creator#30 — pkg/publisher/render.go:54

**`pkg/publisher/render.go:54`** — The sibling `firstOfPreviousMonth` hardcodes `time.UTC`, while the new code uses `base.Location()`. Outputs are identical today (`Date.Time()` is always UTC via `date.go:32-43`), so this is purely cosmetic. A shared helper would unify them.

### run 1 — recurring-task-creator#30 — pkg/publisher/period_token_test.go

**`pkg/publisher/period_token_test.go`** — Coverage is excellent for negative offsets (the bug's core) but does not exercise positive `PeriodOffset` cases, nor `RecurrenceWeekday` / `RecurrenceOnDate` in the dedicated file (those are still covered in `publisher_test.go`). Low priority given the discriminating entries fully prove the fix.

### run 1 — recurring-task-creator#30 — pkg/publisher/doc.go

**`pkg/publisher/doc.go`** — The `publisher` package has no package-level GoDoc anywhere (no `doc.go`, no `// Package publisher` block). Pre-existing — not introduced by this PR — but worth noting since the new test file is the natural moment to fix it. `pkg.go.dev` renders the package with no summary.

### run 1 — recurring-task-creator#30 — go.mod:95

**`go.mod:95`** — Pre-existing tidy drift: `github.com/maxbrunsfeld/counterfeiter/v6 v6.12.2 // indirect` is referenced only via `//go:generate go run -mod=mod ...` directives, never imported. `go mod tidy -diff` would drop it. Intentional pin — move it into the `tool` block alongside `k8s.io/code-generator` and `controller-tools` in a separate follow-up.

### run 1 — recurring-task-creator#30 — pkg/publisher/period_token.go:104

**`pkg/publisher/period_token.go:104-115`** — `tdd/failing-test-before-implementation` process note: fix and test landed in commit `23d403d`, no standalone "red" commit. Discriminating entries (`Jul 31 → 2026-06`, `Mar 31 → 2026-02`, `Dec 31 → 2026Q3`) structurally prove the old code failed, so this is a dark-factory process nuance rather than a defect.

### run 1 — recurring-task-creator#30 — pkg/publisher/period_token_test.go:20

**`pkg/publisher/period_token_test.go:20`** — `context.Background()` used directly in the `build` closure. Consistent with existing `publisher_test.go` (41 occurrences), exempt per project DoD, no change needed.

### run 1 — tts-mcp#10 — Makefile.precommit:30

**`Makefile.precommit:30`** — `check: lint typecheck mcp-typecheck` still omits dependency-vulnerability scanning and secret detection. The diff rewrites this target line, so closing the gap here would have been in-scope; instead, `npm ci` now installs the lockfile but nothing scans it. Concrete fix: add an `audit` target (`cd mcp && npm audit --audit-level=high`) and a `trivy fs --scanners vuln,secret .` step, then append both to `check`. Pre-existing, not introduced, but the rule's contract is "reachable from `check`", and this PR is the first one to actually add a Node-side gate. *(rule: `node/make/security-gates-in-check`)*

### run 1 — tts-mcp#10 — None

**`c6f136a` subject line** — 62 characters, over the 50-char soft cap. The subject lumps two concerns (lockfile bump + new CI gate) into one line. Splitting them — e.g. a lockfile bump commit followed by the CI/relay typecheck wiring — would also produce a cleaner history; or shorten to `Bump mcp/ deps; cover relay in CI` (35 chars). *(rule: `git-commit/subject-under-50-chars`)*

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:799

**`shim/claude_openai_shim.py:799`** — `WAKE_PHRASES` will silently fail if `voice.wake_phrases` is written as a YAML list. `setting()` returns `str(raw)` for non-scalar values, so a natural `wake_phrases: [hey bot, hey bought]` becomes the literal string `"['hey bot', 'hey bought']"`; `.split(',')` then produces phrases that never match, `is_addressed()` returns False for everything, and the bot is **permanently silent in voice calls**. The `WAKE_PHRASES.strip()` non-empty branch defeats the "empty list disables the gate" escape at line 838. This is a silent permanent failure, not the documented "one repeat" cost of a missed trigger — and the code comment at line 780 calling this "A LIST, not a phrase" invites exactly that shape. Fix in `setting()` (line ~115): handle sequences before `return str(raw)` — `if isinstance(raw, (list, tuple)): return ','.join(str(x) for x in raw)`.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:799

**`shim/claude_openai_shim.py:799`** — Module-level composition (`WAKE_PHRASES = setting(...)` at line 799 + `_WAKE_RE = re.compile(...)` at line 822) bakes parsed phrases into a compiled regex at import time, outside the `if __name__ == "__main__":` block at line 2081. Because `_WAKE_RE` is precompiled with the original phrases, a test cannot vary the phrase list by patching `WAKE_PHRASES` alone — it must also rebuild the module-level regex. That is order-dependent global state exactly covered by the rule. Fix: parse `SHIM_WAKE_PHRASES` and compile the regex in the `__main__` block, and pass the matcher into `is_addressed(text, wake_re)` / `strip_wake_phrase(text, wake_re)` as a parameter (module-level default only for the `npm run shim` entry path). *(rule: `python-architecture/main-py-composition-root`)*

### run 1 — discord-assistant#5 — src/voice.js:555

**`src/voice.js:555-575`** — The new `input_audio_buffer.speech_started` branch and `input_audio_buffer.transcription_completed` branch read `config.interruptResponse` and `config.isAddressed` from module-level mutable state instead of constructor args. This is the shared-fixture-ordering test coupling the rule exists to prevent — the new tests `finally`-restore `config.interruptResponse` (test/voice.test.js:543-557) and `delete require.cache[require.resolve('../src/config')]` to re-read env. Inject via constructor (`Session({ ..., interruptResponse, isAddressed })`) so each test constructs the state it needs. *(rule: `node/architecture/inject-dependencies`)*

### run 1 — discord-assistant#5 — src/config.js:15

**`src/config.js:15`** — `flag()` returns the fallback silently for any unrecognized value (`INTERRUPT_RESPONSE=flase`, `TRANSCRIBE=banana`), so a typo'd boolean reverts to default with no warning at boot — the same "switch that looks set and isn't" failure mode the JSDoc identifies as worst case. The rest of the module is fail-fast (`config.check()` exits on a missing `DISCORD_TOKEN` and is invoked at `src/index.js:20` before serving); the newly parsed booleans should route through it. Extend `config.check()` to collect `'TRANSCRIBE has unrecognized value "banana"'` as a problem so a typo surfaces as a loud boot failure alongside the existing checks. *(rule: `node/config/validate-before-serving`)*

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:832

**`shim/claude_openai_shim.py:832`** — Strict `applies_when` is not met (no service class, no method parameter), but the matcher these functions depend on (`WAKE_PHRASES`, `_WAKE_RE`) is sourced from module globals rather than injected — covered by the same composition-root finding above; resolves automatically once that is fixed. Standalone fix: model the matcher as a `WakeGate` class (`__init__(self, phrases: str)`), built once in `__main__` and exposed via a module-level singleton for `Handler`. *(rule: `python-architecture/constructor-injection-only`)*

### run 1 — github-releaser-agent#8 — pkg/githubtags/tags.go:34

**`pkg/githubtags/tags.go:34-35`** — Diff-introduced interface-doc regression. The refactor trimmed the 7-line `TagsFetcher` interface-level prose (which documented `LatestSemverTag`'s contract: `ErrNoTags` fallback semantics, wrapped-error cases, and the "Implementations MUST be safe for concurrent use" requirement) down to a single line, while inline-documenting the new `CommitSHAForTag` method. The result is asymmetric: `LatestSemverTag` is now the only bare method in the exported 2-method interface. Behavior is still documented on `*httpTagsFetcher.LatestSemverTag`, so this is *coverage drift at the API surface*, not lost information — interface consumers (the counterfeiter fake, future alternate implementations) lose the contract. **Both `godoc-assistant` and `go-architecture-assistant` flagged this independently** — strong two-agent signal. *(rule: `go-doc/exported-item-must-have-comment`)*

### run 1 — github-releaser-agent#8 — pkg/githubtags/tags.go:89

**`pkg/githubtags/tags.go:89`** — Sole implementing struct still named `httpTagsFetcher` while the interface contract grew to two methods in this PR. Pre-existing naming, in scope only because the PR touched the interface. Cosmetic divergence from the package convention. *(rule: `go-architecture/private-struct-matches-interface`)*

### run 1 — github-releaser-agent#8 — pkg/steps_planning.go:750

**`pkg/steps_planning.go:750` (`handleValidationFailure`)** — Data clump: 8 positional arguments, two of which carry subtly-different version concepts (`effectiveVersion` — remote-resolved tag used for the GitHub lookup — vs `snapshotVersion` — frontmatter `current_version` preserved verbatim on the escalation path). The Run call site at line 193 passes them adjacent; an inversion is the load-bearing subtlety of the whole feature. Suggest a named value type so the two roles cannot be transposed on a future edit.

### run 1 — github-releaser-agent#8 — CHANGELOG.md:10

**`CHANGELOG.md:10-11`** — Both new `## Unreleased` entries use scoped conventional prefixes (`feat(githubtags):`, `feat(planning):`) rather than the bare `feat:` listed in the rule doc. Judged **COMPLIANT in spirit** (the `feat` token is in the recognised set; minor bump is unambiguous) and consistent with the repo's shipped precedent (`v0.3.0 feat(planning):`, `v0.3.1 docs(readme):`, `v0.3.2 fix(deps):`). The rule's enforcement regex (`^- ([a-z]+:)`) would not match these, however — so any future mechanical checker built to that regex would false-positive on the repo's own convention. No PR change required; consider tightening the rule regex to `^- [a-z]+(\([a-z]+\))?:`. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — github-releaser-agent#8 — None

**`commit 04f55d7` (subject line)** — `specs: spec 002 transitioned to verifying (both prompts completed)` uses past-tense `transitioned` instead of the bare imperative `transition`. Dark-factory-generated bookkeeping commit, not authored prose — falls outside the rule's grep-ability intent. *(rule: `git-commit/imperative-mood`)*

### run 1 — github-releaser-agent#8 — specs/completed/002-planning-close-nothing-to-release.md:48

**`specs/completed/002-planning-close-nothing-to-release.md:48-62`** — All 15 acceptance-criteria checkboxes remain `- [ ]` (unchecked) despite `status: completed`. Not a `markdown-todo/lowercase-x-for-complete` violation (lowercase unchecked is legal), but inconsistent with the sibling `specs/completed/001-bug-stale-current-version-collision.md` which marks its ACs `- [x]`. Governance / audit-trail-only.

### run 1 — github-releaser-agent#8 — specs/completed/002-planning-close-nothing-to-release.md:123

**`specs/completed/002-planning-close-nothing-to-release.md:123` (`## Suggested Decomposition`)** — The spec plans THREE prompts (with prompt 3 docs-only); execution produced TWO (`004-spec-002-tag-commit-seam`, `005-spec-002-planning-nothing-to-release`), with the docs work folded into prompt 2 and the first CHANGELOG bullet added by prompt 1. All ACs are demonstrably satisfied (AC13/14 greps pass), but the table no longer maps 1:1 to real prompts. Update for completed-spec self-consistency.

### run 1 — discord-assistant#1 — src/index.js:267

**`src/index.js:267-280`** — The post-login `uncaughtException` swallow is unbounded: once `connectedOnce` is true, the handler survives every `RECOVERABLE_NET` exception forever, and the process never exits. Discord.js's internal reconnect can stall (failed resume, revoked token, sustained outage — exactly the case the backoff comment at `scripts/supervise.sh:48-49` worries about). In that state `/healthz` returns 200 by design and `/readiness` returns 503, so traffic drains but `supervise.sh` cannot help — the silent-outage failure this PR exists to fix reappears in a form with no restart path. Add a bounded reconnect watchdog: a `.unref()`-ed timer that starts when this branch clears `gatewayReady` and calls `process.exit(1)` once `gatewayReady` has been false for `RECONNECT_GRACE_MS` (default ~300s), reset on `shardResume`/`shardReady`. *(general, judgment-tier — gap between the swallow and the supervise halves)*

### run 1 — discord-assistant#1 — scripts/supervise.sh:21

**`scripts/supervise.sh:21`** — `stop()` calls `kill "$child"` but never `wait`s for it, then `exit 0`s. A child with a graceful SIGTERM/SIGINT handler (drains Discord gateway, flushes state) is reparented to init while still running — orphaned bot, silent failure, exactly the scenario the supervisor was written to prevent. After the `kill`, add `wait "$child" 2>/dev/null`. Tradeoff to document: if the child ignores SIGTERM the `wait` blocks forever, so the fully robust version loops on `kill -0` up to N seconds then `kill -9`. *(general — bash correctness / signal handling)*

### run 1 — discord-assistant#1 — CHANGELOG.md:5

**`CHANGELOG.md:5`** — The new `## Unreleased` bullet starts with `- Survive…` instead of a recognised conventional prefix, so it fails the rule's regex `^- ([a-z]+:)` and dark-factory / `/coding:commit` will not parse the bump at release time. This change is a bug fix (survive a fault that killed the bot), so prefix with `fix:`. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — discord-assistant#1 — src/index.js:259

**`src/index.js:259-260`** — `RECOVERABLE_NET` is broader than the gateway it claims to describe. `socket hang up`, `ECONNRESET`, `ETIMEDOUT` are generic transport errors that also surface from the voice UDP socket and HTTP calls to the endpoint. A matching non-gateway fault would flip `gatewayReady = false` and drain readiness for a subsystem that is fine, while hiding the real error behind a "survived a gateway network fault" log. Narrow the regex to gateway/websocket-specific messages (the observed `Opening handshake has timed out`, `WebSocket was closed`), or gate the branch on the fault's source rather than its message.

### run 1 — discord-assistant#1 — scripts/supervise.sh:22

**`scripts/supervise.sh:22`** — Trap always exits 0, so an external `kill` or system shutdown reports success and masks the signal source in orchestration logs. Exit with the conventional code: `exit $((128 + sig))`, passing the signal name as an arg (`trap 'stop INT' INT` / `trap 'stop TERM' TERM`).

### run 1 — discord-assistant#1 — scripts/supervise.sh:40

**`scripts/supervise.sh:40-43`** — Hard-coded `exit 2 = bad config, give up` couples this generic supervisor to one app's exit-code convention. Make it configurable: `FATAL_CODES="${SUPERVISE_FATAL_CODES:-2}"`, then case-match against that list — keeps the bad-config hot-loop protection while not silently killing the bot on a transient 2 from some future code.

### run 1 — helm#3 — charts/github-pr-reviewer/values.yaml:61

**`charts/github-pr-reviewer/values.yaml:61`** — `TRUSTED_AUTHORS` passes through to `bborbe/github-pr-watcher` (out-of-repo) verbatim. The new comment asserts case-sensitive exact match against the literal `ben-s-go-updater[bot]` login form the watcher sees from GitHub, but the watcher README does not document that contract for `TRUSTED_AUTHORS` (only for `BOT_ALLOWLIST`, where `dependabot[bot]` is shown as a default). If the watcher splits on comma but does not trim or does case-insensitive compare, the bot gets silently dropped into human_review — exactly the wrong direction for a "trust" toggle. *(rule: `helm/cross-repo-config-contract`)*

### run 1 — helm#3 — charts/github-pr-reviewer/Chart.yaml:9

**`charts/github-pr-reviewer/Chart.yaml:9`** — version `0.1.0` → `0.2.0` is the right semver shape (additive trust-list change, backward compatible). There is no repo-level or chart-level `CHANGELOG.md` to record the rationale; the trust-decision prose lives only in the `values.yaml` comment, which means git log + grep is the only path to discover "why 0.2.0?". Consider a one-line CHANGELOG note (per-chart or repo-wide) so future bumps accumulate context.

### run 1 — helm#3 — charts/github-pr-reviewer/values.yaml:51

**`charts/github-pr-reviewer/values.yaml:51-60`** — the 10-line inline comment mixes two distinct concerns: (a) the format contract (`[bot]` literal, exact case-sensitive login match), and (b) the trust rationale for this specific bot. Splitting them (format contract =2 lines at the top; trust rationale = a paragraph after) would make future additions to the trust list easier to review for "did the format rule still apply?". Style only.

### run 1 — helm#3 — charts/github-pr-reviewer/values.yaml:57

**`charts/github-pr-reviewer/values.yaml:57`** — `[[Auto-Merge Trust-Gated PRs]]` is a wiki-link-style token. If the host system resolves `[[...]]` automatically, fine; if not, it will rot as a comment once that work ships under a different name.
