8a720c0be85af085beac5f93da0753aae2a982398e612ff63d293efa6bcf61db

## Configuration
- model: fable
- effort: xhigh
- mode: full
- config_hash: 8a720c0be85af085beac5f93da0753aae2a982398e612ff63d293efa6bcf61db
- rules_commands_hash: ecc803331f860b845a6a1b8a103e889bce02520e8cc04ea88102de74c8d5600d
- prs_version: curated-1
- coding version: 0.51.0
- golden baseline coding version: v0.35.6
- golden version: golden-curated-4
- runner_version: 1
- rows skipped: 0
- cost: not recorded — the ledger carries no cost field.

*golden-curated-4* carries 21 `rejected` entries, so precision is measurable: reporting one costs precision. With so few adjudicated, a single hit still moves the number a long way — read it as directional, not calibrated.

No signature embeds a line reference, so a re-report of the same issue at a different line still matches. `recall` measures issue detection.

## Runs
| run | span | PRs | complete | golden in scope | findings | hits | misses | matched rejected | gap candidates | recall | precision | wall time (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-09-08T10:42:25.674406+00:00 … 2026-09-08T11:44:18.173323+00:00 | 11/20 | partial | 78 | 51 | 8 | 62 | 0 | 43 | 0.114 | 1.000 | 4077 |
| 2 | 2026-09-08T11:58:55.190667+00:00 … 2026-09-08T12:34:58.229479+00:00 | 5/20 | partial | 44 | 50 | 6 | 31 | 0 | 42 | 0.162 | 1.000 | 2695 |
| 3 | 2026-09-08T17:58:52.511392+00:00 … 2026-09-08T18:29:57.187576+00:00 | 4/20 | partial | 36 | 46 | 3 | 27 | 1 | 42 | 0.100 | 0.750 | 2338 |

## Per-PR
| run | pr_id | golden in scope | hits | misses | findings | gap candidates | dropped items | missing sections | duration (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | tts-mcp#20 | 1 | 0 | 1 | 1 | 1 | 0 | — | 217 |
| 1 | github-pr-review-agent#11 | 16 | 4 | 10 | 10 | 6 | 0 | — | 720 |
| 1 | quant#109 | 10 | 0 | 10 | 0 | 0 | 0 | — | 276 |
| 1 | node-skeleton#2 | 12 | 1 | 11 | 12 | 11 | 0 | — | 631 |
| 1 | python-skeleton#3 | 17 | 3 | 13 | 8 | 5 | 0 | — | 434 |
| 1 | vault-cli#68 | 1 | 0 | 1 | 1 | 1 | 0 | — | 244 |
| 1 | backup#15 | 4 | 0 | 2 | 3 | 3 | 0 | — | 282 |
| 1 | bw#41 | 3 | 0 | 2 | 3 | 3 | 0 | — | 260 |
| 1 | dark-factory#71 | 2 | 0 | 2 | 3 | 3 | 0 | — | 325 |
| 1 | tts-mcp#16 | 6 | 0 | 5 | 6 | 6 | 0 | — | 321 |
| 1 | tts-mcp#3 | 6 | 0 | 5 | 4 | 4 | 0 | — | 367 |
| 2 | distill#6 | 10 | 1 | 6 | 8 | 7 | 0 | — | 659 |
| 2 | vault-ui#39 | 6 | 0 | 6 | 6 | 6 | 0 | — | 573 |
| 2 | recurring-task-creator#30 | 6 | 1 | 3 | 5 | 4 | 0 | — | 580 |
| 2 | tts-mcp#10 | 9 | 2 | 5 | 6 | 4 | 0 | — | 351 |
| 2 | discord-assistant#5 | 13 | 2 | 11 | 25 | 21 | 0 | — | 533 |
| 3 | tts-mcp#13 | 10 | 1 | 8 | 21 | 19 | 0 | — | 718 |
| 3 | github-releaser-agent#8 | 13 | 0 | 9 | 8 | 8 | 0 | — | 517 |
| 3 | discord-assistant#1 | 9 | 1 | 8 | 14 | 13 | 0 | — | 630 |
| 3 | helm#3 | 4 | 1 | 2 | 3 | 2 | 0 | — | 473 |

*Effective fixture: **20 of 60** PRs produced a scored row.* The 40 absent rows are not a zero-finding result — they were rejected before scoring, so they lower confidence without lowering recall.

## Gap-triage candidates
These findings matched no golden entry. They are **not a precision failure** — the golden set is a bootstrap from one strong-model run, and a finding it does not describe is evidence the set is incomplete.

### run 1 — tts-mcp#20 — mcp/package-lock.json:948

**`mcp/package-lock.json:948`** — `3.1.5` is already a known-vulnerable version. Four high-severity advisories published 2026-09-02 cover `>= 3.0.0, < 3.1.6` ([GHSA-5jgf](https://github.com/advisories/GHSA-5jgf-p345-68v8), [GHSA-f65p](https://github.com/advisories/GHSA-f65p-4m7j-42xc), [GHSA-fph4](https://github.com/advisories/GHSA-fph4-wmhf-6fwf), [GHSA-jqff](https://github.com/advisories/GHSA-jqff-g426-hqxp)); SSRF and host-confusion in URI normalization. Patched in `3.1.6`; current 3.x is `3.1.7` (npm `three` tag). Fix: `cd mcp && npm update fast-uri` and commit the lockfile, or merge upstream PR #27 (`3.1.5 → 3.1.7`, open since 2026-09-03) right after this one.

### run 1 — github-pr-review-agent#11 — pkg/factory/runner.go:106

**`pkg/factory/runner.go:106`** - `--skip-post` nils only the Go `PrPoster`/`ReviewVerifier`; `RunAgent` still injects the write-capable App installation token as `GH_TOKEN` into the Claude subprocess env for every phase. The usage text at `cmd/run-task/main.go:76` and the `RunConfig.SkipPost` doc promise "suppress all GitHub write calls", but that holds only because the unchanged tool allowlist has no write verbs. Fix: reword usage and doc to "suppresses the agent's own review/approve/dismiss posts", or additionally omit or replace the token under `SkipPost` (the review phase's `gh pr view/diff` needs auth, so a read-only token would be required).

### run 1 — github-pr-review-agent#11 — pkg/factory/runner_test.go:32

**`pkg/factory/runner_test.go:32`** - `Expect(poster).To(BeNil())` also passes for a typed-nil pointer boxed in an interface (Gomega `BeNil` uses reflect `IsNil`), so the "returns nil interfaces" test cannot catch the regression the boundary file's header describes. Use `Expect(poster == nil).To(BeTrue())`. Flagged independently by two owners.

### run 1 — github-pr-review-agent#11 — pkg/skip_post_boundary_test.go:97

**`pkg/skip_post_boundary_test.go:97`** - case 3 mirrors the existing verification-success test; the pass path never reads the poster, so the nil-poster framing is misleading.

### run 1 — github-pr-review-agent#11 — pkg/skip_post_boundary_test.go:224

**`pkg/skip_post_boundary_test.go:224`** - absence of an `ai_review verify:` diagnostic is a weak discriminator; assert on the guard outcome directly.

### run 1 — github-pr-review-agent#11 — pkg/skip_post_boundary_test.go:1

**`pkg/skip_post_boundary_test.go:1`** - file added after the implementation and its tests landed in 9eabea9; coverage backfill, not red-green. *(rule: `tdd/failing-test-before-implementation`)*

### run 1 — github-pr-review-agent#11 — README.md:29

**`README.md:29`** - "Local CLI" vs "Local review" rows read alike. Rename, e.g. "Local task runner" / "Ad-hoc PR review".

### run 1 — node-skeleton#2 — CLAUDE.md:38

**`CLAUDE.md:38`** — The `moduleResolution: "bundler"` invariant's stated reason is false. `tsc --noEmit` with `module`/`moduleResolution` set to `nodenext` (and `node16`) passes with zero errors on this exact tree, because tsc never inspects `require()` string arguments. Recommend: switch tsconfig to `nodenext` (TypeScript's mode for code Node runs directly) and drop the bullet, or keep `bundler` and rewrite the bullet with a true reason. A wrong invariant here is inherited by every future service.

### run 1 — node-skeleton#2 — src/index.ts:8

**`src/index.ts:8-10`** — Typed `require()` is an unchecked boundary: `require` returns `any`, so `const config: Config = require('./config.ts')` is a silent cast. Verified: changing `module.exports` in `config.ts` to `{ wrongShape: true }` still typechecks. Same pattern in `src/server.ts:6-9`, `src/handlers/health.ts:5-6`, `src/log.ts:5`, `test/health.test.ts:5-7`. Real safety lives at the export site (`const x: T = {...}; module.exports = x`). Fix: state this in the CLAUDE.md invariants as the cost of the no-build-step design and name the export-site annotation as the mandatory pattern. The current wording ("the only gate that makes the types mean anything") overclaims.

### run 1 — node-skeleton#2 — package.json:8

**`package.json:8`** — `"node": ">=22"` understates the floor. Unflagged type stripping, which `node src/index.ts` and every `.ts` require depend on, arrived in Node 22.18.0; 22.0 to 22.17 fail at boot with an unknown-extension error. `node:22-slim` and CI's `'22'` float to recent patches and mask it. Fix: `">=22.18"` with a one-line comment.

### run 1 — node-skeleton#2 — CLAUDE.md:11

**`CLAUDE.md:11`** — Claims no `as` in source, but lint accepts `x as number` (probed: exit 0). `!`, `any`, `@ts-ignore` are enforced. Fix in `eslint.config.js:47`: add `'@typescript-eslint/consistent-type-assertions': ['error', { assertionStyle: 'never' }]`.

### run 1 — node-skeleton#2 — README.md:7

**`README.md:7-17`** — Intro and "What you get" never say the skeleton is TypeScript run by Node with no build step; only the `make typecheck` row hints at it. Fix: one table row, e.g. "TypeScript sources, run directly by Node, no build step, no `dist/`".

### run 1 — node-skeleton#2 — eslint.config.js:44

**`eslint.config.js:44-46`** — The `erasableSyntaxOnly` comment sits above `no-explicit-any` and `no-non-null-assertion`, which have nothing to do with erasability. Move or reword.

### run 1 — node-skeleton#2 — src/server.ts:69

**`src/server.ts:69`** — `err: Error` overstates what Express passes (`next('string')` is legal). Type `unknown` and narrow before reading `.message` and `.stack`.

### run 1 — node-skeleton#2 — src/server.ts:31

**`src/server.ts:31,70-72`** — Inline `import('express').X` in signatures; `import type { Request, Response, NextFunction, Express, Router } from 'express'` matches the `import type` style already used for local modules. Same in `src/handlers/health.ts:12`.

### run 1 — node-skeleton#2 — src/config.ts:47

**`src/config.ts:47-55`** — `check()` promises "every problem" but skips `LOG_LEVEL`; an unknown value silently becomes `info`. Validating it would also let `src/log.ts:24` drop the `Record<string, number>` widening for a `LogLevel` type guard.

### run 1 — node-skeleton#2 — package.json:31

**`package.json:31`** — `typescript ^6.0.3` exceeds typescript-eslint's peer ceiling `<6.1.0`; `~6.0.3` prevents `npm update` drift.

### run 1 — node-skeleton#2 — tsconfig.json:11

**`tsconfig.json:11-14`** — `esModuleInterop` and `forceConsistentCasingInFileNames` are TS 6 defaults, `resolveJsonModule` is unused. Trim or keep deliberately.

### run 1 — python-skeleton#3 — Makefile.precommit:47

**`Makefile.precommit:47`** — `uv export --all-extras` audits dev tooling too, while the comment says "the exact transitive tree that gets installed" and the Dockerfile installs `--no-dev`. Auditing dev deps is stricter and fine; adjust the comment to say so.

### run 1 — python-skeleton#3 — Makefile.precommit:31

**`Makefile.precommit:31`** — `PIP_AUDIT_VERSION ?= 2.9.0`; PyPI latest is 2.10.1 (2026-06-10). Consider bumping.

### run 1 — python-skeleton#3 — pyproject.toml:38

**`pyproject.toml:38`** — `requires = ["hatchling"]` is unpinned, whereas the removed setuptools line had a floor. Add a lower bound such as `hatchling>=1.25`.

### run 1 — python-skeleton#3 — README.md:78

**`README.md:78`** — `make check` now hard-requires a local `trivy` binary, but the README never says how to install it. Add a one-line prerequisite (e.g. `brew install trivy`).

### run 1 — python-skeleton#3 — README.md:86

**`README.md:86`** — "over ground pip-audit and trivy already cover" reads awkwardly. Suggest "beyond what pip-audit and trivy already cover".

### run 1 — vault-cli#68 — CHANGELOG.md:11

**`CHANGELOG.md:11`** — the single bullet packs the fix, the quoted error string, the updater-tool history, the tidy claim, and the dod.md remark into one run-on line. Guide examples are one concise clause. Consider trimming to the fix plus the `go install` symptom and moving the narrative to the PR description. Not a rule violation, style only.

### run 1 — backup#15 — Makefile:6

**`Makefile:6`** — default registry URL duplicated with `Dockerfile:8`; via `make build` the Dockerfile default is dead code. Either keep one source of truth (drop the Makefile `?=` and pass `$(if $(NPM_REGISTRY),--build-arg NPM_REGISTRY=$(NPM_REGISTRY))`) or add a comment that the Dockerfile default only serves plain `docker build`.

### run 1 — backup#15 — Dockerfile:11

**`Dockerfile:11`** — `${NPM_REGISTRY}` unquoted; an empty override (`NPM_REGISTRY= make build`) runs `npm set registry` with no value and fails late. Suggest `RUN npm set registry "${NPM_REGISTRY:?NPM_REGISTRY must be set}"`. Optionally note in a comment that only https internal mirrors are intended.

### run 1 — backup#15 — Makefile:6

**`Makefile:6`** — new `NPM_REGISTRY` knob is undocumented; the README Building section still shows only `BRANCH=develop make build`. Add one example such as `NPM_REGISTRY=https://registry.npmjs.org make build`.

### run 1 — bw#41 — groups/meta/k3s.py:6

**`groups/meta/k3s.py:6-19`** - The 14-line dated incident narrative is far longer than any other comment in `groups/`. Consider trimming to the root cause (veth-per-pod inflates the single global ARP table past kernel defaults, causing silent drops) and linking the incident note or runbook for the details. Non-rule-derived, style only.

### run 1 — bw#41 — groups/meta/k3s.py:22

**`groups/meta/k3s.py:22-24`** - Only the IPv4 neighbour table is raised. If the nodes carry IPv6 pod or node addresses, the NDP table has separate `net.ipv6.neigh.default.gc_thresh1/2/3` limits with the same 128/512/1024 defaults and the same overflow failure mode. Add the IPv6 trio for symmetry if IPv6 is in play, otherwise ignore. Non-rule-derived.

### run 1 — bw#41 — groups/meta/k3s.py:3

**`groups/meta/k3s.py:3`** - The pre-existing `.*k3s.*` member pattern now carries real config. I verified the inventory: all 13 name matches are `hm.nuke-k3s-*` hosts, so nothing unintended is affected today. Anchoring the pattern would guard against future look-alike node names. Non-rule-derived.

### run 1 — dark-factory#71 — CHANGELOG.md:15

**`CHANGELOG.md:15`** — entry promises find/replace pairs "for each Blocker/Critical issue", but the agent defines no Blocker tier. The report template only has `## Critical Issues` and `## Recommendations`. Reword to "each Critical issue".

### run 1 — dark-factory#71 — agents/prompt-auditor.md:514

**`agents/prompt-auditor.md:514-518`** — the find/replace requirement is restated in full here after already being spelled out in the template placeholders at lines 494-500. Keep the detail in the template and have `<final_step>` point at it in one sentence, so future wording changes happen in one place.

### run 1 — dark-factory#71 — agents/prompt-auditor.md:493

**`agents/prompt-auditor.md:493-496`** — the Critical Issues placeholder is now hard-wrapped over three lines while the sibling placeholders stay single-line, and the rest of the file does not wrap at 80 columns either. Cosmetic only.

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:8

**`skills/voice/SKILL.md:8`** — Asserts as fact that "there is no competing always-on rule in `~/.claude/CLAUDE.md`". A shipped plugin cannot see or control the consumer's personal config, and the claim is already false in this session's loaded `~/.claude/CLAUDE.md`, whose "TTS Voice Default Narrate" rule acts as if `/voice narrate` ran every session. That memory rule still outranks the selected mode, which is the exact failure the changelog says this PR removes. Reword as intent: the skill is the intended sole authority, an always-on rule in the user's CLAUDE.md sets the default, and the arg overrides it per session. Line 94 already hedges this way.

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — The frontmatter description repeats the same unverifiable guarantee ("no always-on rule competes with it") inside the discovery signal. Drop the clause and keep trigger phrases only.

### run 1 — tts-mcp#16 — CHANGELOG.md:17

**`CHANGELOG.md:17`** — "there is no always-on default any more" documents a change to the maintainer's personal config, not to this repo. Consumers read a promise the plugin cannot keep. Reword to what actually changed in-tree: the persistence note now documents that the skill sets the mode and no default ships.

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — Description is a single ~617-character line packing six args, a self-authority claim, and a selftest caveat. Trim to what/when triggers; move the rest into "What this does".

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:87

**`skills/voice/SKILL.md:87`** — "Anything at all when the user is clearly sitting there watching" sits under "Never speak, in any mode" and can cancel `narrate`'s core promise on an unverifiable judgment. Scope it to `on` / `interview`, or add "in `narrate`, still speak the gist".

### run 1 — tts-mcp#16 — CHANGELOG.md:12

**`CHANGELOG.md:12`** and **`CHANGELOG.md:16`** — 66- and 76-word paragraphs. Guide format is `- <prefix>: <what> [context]`. Keep the what, cut the rationale.

### run 1 — tts-mcp#3 — commands/voice-test.md:1

**`commands/voice-test.md:1`** — Frontmatter has only `description`. The command invokes three MCP tools plus `/tts-mcp:voice restart`, so it runs with unbounded permissions. Add an `allowed-tools` line listing `mcp__tts__say`, `mcp__tts__get_voices`, and `mcp__tts__get_status`. No `argument-hint` needed since the command takes no arguments. *(rule: `agent-cmd/command-frontmatter`)*

### run 1 — tts-mcp#3 — CHANGELOG.md:12

**`CHANGELOG.md:12-16`** — The two new `## Unreleased` bullets carry no conventional prefix. The agent-auditor rated this Optional because the whole file already uses Keep a Changelog `### Added` / `### Changed` sections. I raised it: the rule is MUST tier, this repo has `autoRelease: true`, and v0.1.0 was an initial release, so it gives no evidence that the bot maps `### Added` to a minor bump. The new command is a feature and a wrong patch bump would under-ship it. Prefix the bullets with `feat:` and `refactor:`, which satisfies the parser without restructuring the file. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — tts-mcp#3 — commands/voice-test.md:22

**`commands/voice-test.md:22`** — The "still silent, run restart, then re-run this test" bullet has no guard. When this command is entered from the skill's restart step and still fails, the reader hits this bullet before reaching step 5's "after a restart" stop, so a second restart is the literal reading. Each restart reloads the model and drops in-flight messages for every session. Make the bullet conditional on no restart having happened yet in this pass. The skill's own step at `skills/voice/SKILL.md:33` restates what this command already does and could collapse into step 3.

### run 1 — tts-mcp#3 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — The description is now a single run-on of roughly 430 characters. The Args list and the new selftest caveat sit at the end where a skimmer misses them. Split into two sentences or move the caveat into the body section that already covers it.

### run 2 — distill#6 — README.md:128

**`README.md:128`** — The install command `cp hooks/deny-generated-file-edits.sh ~/.claude/hooks/` assumes a source checkout, but the README's own Install section is `go install github.com/bborbe/distill@latest`, which ships no `hooks/` dir. For a go-install user the hook is still uninstallable, which is exactly what the CHANGELOG entry claims to fix. Same line at `docs/adopting-distill.md:110`. Fix: add a `curl -fsSL https://raw.githubusercontent.com/bborbe/distill/main/hooks/deny-generated-file-edits.sh -o ~/.claude/hooks/deny-generated-file-edits.sh` variant, or say "from a clone of this repo".

### run 2 — distill#6 — README.md:128

**`README.md:128`** — Missing `mkdir -p ~/.claude/hooks` before the `cp`. Verified: `cp file dir/` fails with "No such file or directory" when the dir does not exist, and `~/.claude/hooks/` is not guaranteed for a first-time installer. Same at `docs/adopting-distill.md:110`.

### run 2 — distill#6 — CHANGELOG.md:7

**`CHANGELOG.md:7`** — `feat(hooks):` uses a scope. The guide's enforcement regex `^- ([a-z]+:)` does not match scoped prefixes. The repo already has a released `chore(dev):` precedent, so this is style only, but dropping the scope guarantees parser compatibility. *(rule: `changelog/conventional-prefix-required`)*

### run 2 — distill#6 — hooks/deny-generated-file-edits.sh:36

**`hooks/deny-generated-file-edits.sh:36-37`** — `\S` is a GNU ERE extension. Verified working on macOS BSD grep, GNU grep, and BusyBox grep, so no breakage today. `[^[:space:]]+` is strict POSIX with identical semantics.

### run 2 — distill#6 — hooks/deny-generated-file-edits.sh:33

**`hooks/deny-generated-file-edits.sh:33-37`** — A multibyte char straddling byte 2048 makes BSD grep silently drop the `Regenerate:` match and GNU grep append a garbled byte. Graceful fallback message either way, and the real banner is ASCII, so low risk. `LC_ALL=C` on `head` and `grep` removes the divergence.

### run 2 — distill#6 — hooks/deny-generated-file-edits.sh:11

**`hooks/deny-generated-file-edits.sh:11-12`** — Malformed or empty stdin makes `jq` fail under `set -e` before any JSON is emitted. What the harness does with a non-zero, silent hook is undefined here. `|| passthrough` on the `tool=` line keeps it fail-open explicitly.

### run 2 — distill#6 — docs/adopting-distill.md:95

**`docs/adopting-distill.md:95`** — Step 5 recommends `--title`, but the banner's `Regenerate:` line (`pkg/distill/driver.go:315`, unchanged) omits it. A user who obeys the hook's denial message regenerates without the H1. One sentence pointing to the Makefile target as the canonical regenerate command closes the gap.

### run 2 — vault-ui#39 — src/vault_ui/activity.py:52

**`src/vault_ui/activity.py:52-61`** — `claude_session_id` comes from vault frontmatter and is used unvalidated to build a filename, a path, and a glob pattern. Pathlib's `/` operator discards `project_dir` when the right side is absolute (verified: `Path("/x") / "/etc/passwd.jsonl"` → `/etc/passwd.jsonl`), `..` traverses, and `*`/`?`/`[` widen the glob. The resulting mtime is returned to the browser as `activity_date`, so a crafted id becomes an existence-plus-mtime oracle for any `*.jsonl`. The rest of the codebase already gates this value: `cleanup.py:51-59` rejects `/` and non-UUIDs, `factory.py:92/140` and `tasks.py:1728` check `is_uuid()`. Exposure is capped by the default `127.0.0.1` bind. Fix: add `if not session_id or not is_uuid(session_id): return None` at the top of `transcript_mtime`, importing `is_uuid` from `session_resolver`. Non-UUID ids never have a transcript named after them anyway (live check: 0 of 775 task ids and 0 goal ids are non-UUID), so this loses nothing.

### run 2 — vault-ui#39 — src/vault_ui/activity.py:59

**`src/vault_ui/activity.py:59-64`** — The glob fallback runs once per task/goal per list call with no per-request memo. Measured cost here is 0.35 ms per miss across 60 project dirs, with a 60 s poll, so not a problem today. If it grows, build one `{filename: path}` index per request.

### run 2 — vault-ui#39 — src/vault_ui/activity.py:25

**`src/vault_ui/activity.py:25-29`** — `_mtime_or_none` swallows every `OSError` silently (permission errors look identical to "missing"), while the sibling branch at line 65 logs at debug. Narrow to `FileNotFoundError` or log the rest.

### run 2 — vault-ui#39 — src/vault_ui/api/tasks.py:705

**`src/vault_ui/api/tasks.py:705-710`** and **`src/vault_ui/api/tasks.py:1794-1799`** — Filesystem probing inside the response mappers. Consistent with the existing `discover_hierarchy_folders` call in the same function, so not a regression, but a small injected resolver would keep the mappers pure if I/O keeps accreting here.

### run 2 — vault-ui#39 — src/vault_ui/static/app.js:1135

**`src/vault_ui/static/app.js:1135`** — The hover title shows the raw UTC ISO string (`2026-09-08T09:01:37Z`). `new Date(iso).toLocaleString()` would read as local time.

### run 2 — vault-ui#39 — tests/test_activity.py:10

**`tests/test_activity.py:10-11`** — Module-level reads of app.js/style.css plus source-text assertions (lines 140-169) follow the existing repo convention but are brittle to renames. Timing-window assertions at lines 33, 73, 86 compare against live `datetime.now()`; windows are ±1 min so flake risk is low.

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token.go:105

**`pkg/publisher/period_token.go:105`** — the clamp `time.Date(base.Year(), base.Month(), 1, …, base.Location())` is copy-pasted verbatim at lines 105, 109, and 113. Extract a `firstOfMonth(base)` leaf helper next to the existing `firstOfPreviousMonth` in `render.go`, which is the same shape but hardcodes UTC.

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token.go:105

**`pkg/publisher/period_token.go:105`** — no inline rationale for the day-1 anchor, while the sibling OnDate branch explains itself. One comment noting that `AddDate` rolls a nonexistent "June 31" forward into July would save the next reader a trip to the test file.

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token_test.go:68

**`pkg/publisher/period_token_test.go:68`** — the Aug 31 entry sits under the "Discriminating entries" comment, but old code already returns `2026-07` for it since July 31 exists. Its description ("July has 31 days but August target does too") is also garbled. Move it to the control group or replace it with a real discriminator such as Dec 31 with offset -1 expecting `2026-11`.

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token_test.go:25

**`pkg/publisher/period_token_test.go:25`** — no positive-offset overflow case. May 31 with offset +1 overflowed to `2026-07` on old code and should now yield `2026-06`.

### run 2 — tts-mcp#10 — CHANGELOG.md:17

**`CHANGELOG.md:17`** — the "two remaining advisories" bullet is stale. The `@hono/node-server` advisory GHSA-frvp-7c67-39w9 was revised on 2026-08-12 and no longer matches the 1.19.15 in this lockfile, so `npm audit` does not list it anymore. The advisories that do fire are the three above, which opened after the commit date of 2026-07-25. Re-run `npm update` in `mcp/` and rewrite this bullet before merge, since `autoRelease: true` will publish it verbatim.

### run 2 — tts-mcp#10 — CHANGELOG.md:16

**`CHANGELOG.md:16-21`** — the three new `## Unreleased` bullets carry no conventional prefix. With `autoRelease: true` the bump detector has to infer from prose. Downgraded from the rule's MUST tier because the file header opts into Keep a Changelog and every pre-existing bullet uses the same style. Decide once repo-wide: add `chore:` prefixes, or accept the convention. *(rule: `changelog/conventional-prefix-required`)*

### run 2 — tts-mcp#10 — CHANGELOG.md:17

**`CHANGELOG.md:17`** — a bullet describing advisories deliberately left unfixed sits under `### Fixed`. Fold it as a caveat into line 16 or move it under a `### Security` note.

### run 2 — tts-mcp#10 — Makefile.precommit:26

**`Makefile.precommit:26`** — `npm ci` wipes and reinstalls `node_modules` on every `make check`. Deterministic, but slow locally. Acceptable as is.

### run 2 — discord-assistant#5 — None

**`@commits`** — Commit `d0b0695` — `fix: no typing dots for an utterance that was not addressed` — subject lacks an imperative verb after the prefix; "no typing dots..." is a noun phrase, not an action. Rephrase e.g. `fix: skip typing dots for an unaddressed utterance`. *(rule: `git-commit/imperative-mood`)*

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:821

**`shim/claude_openai_shim.py:821`** — `_WAKE_LEAD` joins `sorted(_FILLER_WORDS | {"hey"})` without `re.escape`. Safe today since every entry is a hardcoded plain-letter literal (`_FILLER_WORDS` at line 764 is not env/config-derived), but worth a one-line comment noting the assumption so a future entry with regex metacharacters doesn't silently break the pattern.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:773

**`shim/claude_openai_shim.py:773-825`** — `_WAKE_RE.search` returns only the first match, so an accumulated turn like "Hey bot. Hey bot, what's up" strips only through the first occurrence, leaving `"what's up"` (correct in this example) but in general leaves any interstitial re-addressing text (e.g. "Hey bot. Never mind. Hey bot, what's up") folded into the prompt. Acceptable given the file's own "keep only from the phrase onward" comment covers the common case; not worth changing without a concrete failure.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:775

**`shim/claude_openai_shim.py:775-899` vs `src/config.js:142-181`** — the wake-phrase detection (default phrase list, quote-stripping, sentence-anchoring, filler-lead skip) is hand-duplicated between the Python shim and the JS bot's `config.isAddressed`, with only a comment ("Mirrors the endpoint's rule") holding them in sync. Given the two run in different runtimes with no shared import boundary, duplicating the *decision logic* is a reasonable trade-off versus building an IPC/shared-config mechanism for one predicate — but if this drifts again, prefer having the shim expose its decision (e.g. a response header/marker on the empty completion) so only one side owns the rule, rather than adding more parallel regex logic to the JS side.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:107

**`shim/claude_openai_shim.py:107`** — `setting()`'s bool branch does `.strip()` then `.strip("\"'")` then `.lower()`, the opposite order from the JS `flag()` in `src/config.js` (quotes stripped first, then trimmed). They disagree on values where a quote is immediately followed by trailing whitespace, e.g. `X='"1" '` (quote, `1`, quote, space): Python strips the outer whitespace first, exposing the quote pair for `strip("\"'")` to remove, landing on `"1"` → truthy → `True`; JS's regex only strips a quote that is literally the last character, so the embedded trailing quote survives, `v` becomes `1"`, and the value falls through to the fallback default instead of `True`. The comment at lines 104-106 explicitly claims this now "agrees with the bot's `flag()` … on every spelling," which this case falsifies. Fix by mirroring JS's order (strip quotes first, then whitespace) or add a code comment noting the known residual mismatch: `str(raw).strip("\"'").strip().lower()` — verify this reproduces JS behavior for `'"1" '` before landing it, since swapping order changes other edge cases too. *(rule: `python-logging/configure-once-in-main`)* — not this rule, drop the citation, this is standalone.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:1831

**`shim/claude_openai_shim.py:1831`** — the new `QUIET` diagnostic truncates the prompt at `[:60]` while the sibling `VOICE/TEXT` log two lines above truncates at `[:70]`; harmless but an easy one-line normalize (`[:70]`) if touching this again, since every other `->`-prefixed log in the file uses a single truncation length per call site by convention (40/60/70 vary elsewhere too, so this is genuinely minor). Note the "should fix" item above mistakenly included a rule citation that doesn't apply — restating without it:

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:107

**`shim/claude_openai_shim.py:107`** — order-of-operations mismatch between Python's `setting()` and JS's `flag()` for quoted values with trailing whitespace (e.g. `'"1" '`): Python returns `True` unconditionally, JS falls through to the caller's fallback. The comment claims full parity with `flag()`; this input breaks that claim. Recommend matching JS's order (`strip("\"'")` before `.strip()`) or documenting the residual gap.

### run 2 — discord-assistant#5 — src/config.js:8

**`src/config.js:8-17`** — `flag()` strips quotes before trimming (`replace(...).trim()`), but the strip regex is anchored to the literal string start/end (`^`/`$`), so it only fires when the quote is the very first/last character. A value with whitespace *outside* the quotes (e.g. `' "1"'` or `'"1" '`) leaves one quote mark attached, `.trim()` can't remove it, and the parse falls through to `fallback` silently — exactly the "fails quietly" failure mode this function's own docstring says it exists to prevent. Verified: `flag(' "1"', X)` and `flag('"1" ', X)` both return the fallback, not `true`. Fix: trim first, then strip quotes (or strip with a regex that also eats surrounding whitespace, e.g. `/^\s*["']?|["']?\s*$/g`).

### run 2 — discord-assistant#5 — src/config.js:150

**`src/config.js:150-155`** — `wakePhrases` strips quotes on the *whole* env value before splitting, which only handles `export X="a,b"` (one pair of outer quotes). If phrases are individually quoted per Make/shell convention (`"hey bot","hey but"`), the strip removes only the outermost pair and leaves the inner `","` untouched, so `.split(',')` yields `'hey bot"'` and `'"hey but'` — items with embedded quote characters that will never match any transcript. Verified with `SHIM_WAKE_PHRASES='"hey bot","hey but"'`. Either document that only whole-value quoting is supported, or strip quotes per-item after splitting: `.split(',').map(p => p.replace(/^["']|["']$/g, '').trim().toLowerCase())`.

### run 2 — discord-assistant#5 — src/config.js:170

**`src/config.js:170-186`** — `config.isAddressed` calls `new RegExp(...)` inside `.some()` on every invocation, rebuilding up to `wakePhrases.length` regex objects per transcribed utterance. `wakePhrases` and `WAKE_LEAD_WORDS` are both fixed after config load; precompile the regex array once (e.g. immediately after `wakePhrases` is built) and have `isAddressed` just `.some(re => re.test(t))`.

### run 2 — discord-assistant#5 — src/voice.js:579

**`src/voice.js:579`** — `log.debug(' voice: not addressed, no typing indicator')` omits the transcript that was rejected. Given the wake-phrase regex has real edge cases (see Should Fix items above and the anchor/boundary subtleties in the surrounding comments), including `{ transcript: e.transcript }` would make a missed-trigger report actually diagnosable instead of requiring log-level bumping and reproduction.

### run 2 — discord-assistant#5 — test/voice.test.js:211

**`test/voice.test.js:211-281`** — the new `flag()`/`isAddressed()` parsing tests live in `voice.test.js`, but `test/config.test.js` already exists and uses the identical `require.cache` reload pattern for config-level behaviour (`isAllowed`, `ALLOWED_USER_IDS`). Placing the boolean-spelling test and the `isAddressed` prefix-matching tests there instead would keep config-parsing coverage colocated with the module it tests, consistent with the file that already owns that convention.

### run 2 — discord-assistant#5 — src/config.js:175

**`src/config.js:175`** — the trailing `\b` after the interpolated phrase assumes the phrase's last character is a word character; all current phrases (`hey bot`, `hey bought`, `hey but`) satisfy this, but a future phrase ending in punctuation would silently fail to match. Not urgent given today's phrase list, but worth a comment noting the constraint.

### run 2 — discord-assistant#5 — src/config.js:16

**`src/config.js:16`** — `flag()` strips quotes with a regex anchored to the literal string ends, then trims. Whitespace outside the quotes leaves one quote behind and the value falls through to `fallback` silently, the exact failure the docstring says this function prevents. Verified: `INTERRUPT_RESPONSE='"1" '` and `' "1"'` both parse as `false`. Fix: trim, strip quotes, trim again. The shim comment at `shim/claude_openai_shim.py:104` now claims parity "on every spelling", but the shim treats any unknown token such as `maybe` as true while `flag()` returns the fallback. Only `SHIM_UNSAFE` and `SHIM_FRONT_HEURISTICS` go through the shim's bool path, so soften the comment or align the unknown-token rule.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:864

**`shim/claude_openai_shim.py:864`** — `strip_wake_phrase` strips `" ,.:;-—"` after the phrase but not `?` or `!`. "Hey bot?" reaches the model as the prompt `?`, and "Hey bot!" as `!`. The fallback to the original text only fires on an empty remainder. Add `?!` to the strip set, or fall back when the remainder contains no word character.

### run 2 — discord-assistant#5 — src/config.js:142

**`src/config.js:142`** — the default wake list ships `hey but`, a common English sentence opener. Verified on both sides: "Hey but that's not what I meant" is treated as addressed and the model receives "that's not what I meant". Speech-to-text routinely drops the comma that would protect it. Same default at `shim/claude_openai_shim.py:799`, `config.example.yaml:62` and `local.env.example:62`. Recommend dropping `hey but` from the shipped default and leaving it as a documented opt-in in the example files.

### run 2 — discord-assistant#5 — src/config.js:180

**`src/config.js:180`** — a `new RegExp` is built per phrase on every transcription event. Precompile once after `wakePhrases` is built.

### run 2 — discord-assistant#5 — src/config.js:143

**`src/config.js:143`** — the quote strip is whole-value only. Per-item quoting such as `"hey bot","hey but"` yields `hey bot"` and `"hey but`. Document the constraint or strip per item after the split.

### run 2 — discord-assistant#5 — src/voice.js:579

**`src/voice.js:579`** — the not-addressed debug line omits the transcript, so a missed trigger cannot be diagnosed from logs. Include it.

### run 2 — discord-assistant#5 — test/voice.test.js:211

**`test/voice.test.js:211`** — the `flag()` and `isAddressed` tests belong in `test/config.test.js`, which already uses the same require-cache reload pattern.

### run 2 — discord-assistant#5 — None

**`@commits`** — commit `d0b0695` "fix: no typing dots for an utterance that was not addressed" is a noun phrase where the other seven subjects use an imperative verb after the prefix. The rule text targets verb forms only, so this is a style nit. *(rule: `git-commit/imperative-mood`)*

### run 3 — tts-mcp#13 — src/server.py:353

**`src/server.py:353`** — `instruct` is enqueued without an engine-compatibility check, so a Voxtral server returns 202 and the request only fails later on the worker thread via `/status`. Mirror the existing `voice` check and raise 400 in `say()` when `instruct` is set and the engine is `voxtral`.

### run 3 — tts-mcp#13 — src/tts/engine.py:222

**`src/tts/engine.py:222`** — `Qwen3Engine.validate_model` only checks `hasattr(model, "generate_custom_voice")`, but every mlx-audio Qwen3-TTS variant (base, custom_voice, voice_design) exposes that method on one shared class. Check `model.config.tts_model_type == "custom_voice"` so a wrong variant fails at load, not on the first `/say`.

### run 3 — tts-mcp#13 — src/tts/engine.py:254

**`src/tts/engine.py:254`** — `language` is forwarded unvalidated; mlx-audio silently drops an unknown language (verified: `language_id = None` fallback in the installed package). Read `talker_config.codec_language_id` from `config.json` the same way `discover_voices` reads `spk_id` and reject unknown values at startup.

### run 3 — tts-mcp#13 — scripts/download-model.sh:114

**`scripts/download-model.sh:114`** — prints `default_voice: ryan` unconditionally for qwen3 with no check that the speaker exists in the downloaded model. Drop the line or read it from the model's `config.json`.

### run 3 — tts-mcp#13 — README.md:186

**`README.md:186`** — the `sample_rate` row still says "24000 for Voxtral" with no guidance for qwen3, though the same table now documents `engine` and `language`. State the expected value per engine.

### run 3 — tts-mcp#13 — tests/test_server.py

**`tests/test_server.py`** — no test covers `instruct` flowing from `POST /say` into the `WorkItem` and on to `generate_chunks`, nor that Voxtral plus `instruct` ends as an error status. Add both cases alongside the existing `TestServerAudioWorker` tests.

### run 3 — tts-mcp#13 — src/tts/engine.py:249

**`src/tts/engine.py:249`** — `cast(CustomVoiceModel, model)` lies about the accepted type; the cast is only safe because of `validate_model`. Consider widening the protocol's `model` parameter or asserting the type at the call site.

### run 3 — tts-mcp#13 — src/tts/engine.py:102

**`src/tts/engine.py:102`** — `VoxtralEngine.validate_model`'s `hasattr(model, "generate")` also passes for a Qwen3 model; only `discover_voices` gates today. Check `model.model_type == "voxtral_tts"` for symmetry with the qwen3 fix.

### run 3 — tts-mcp#13 — src/tts/generate.py:57

**`src/tts/generate.py:57`** — `streaming_interval` is threaded through the buffered path where the docstring admits it is unused. Split `generate`/`generate_stream` on the engine, or accept as documented.

### run 3 — tts-mcp#13 — src/server.py:142

**`src/server.py:142`** — 17 read-only properties wrap plain config scalars while `model`, `statuses`, `work_queue`, `ready_queue` stay public. Fine if intentional; otherwise scope the pattern to the injected `engine`.

### run 3 — tts-mcp#13 — src/server.py:287

**`src/server.py:287`** — `StatusResponse.audio_file`/`error` gained `= None` defaults, a drive-by schema change (required → optional in OpenAPI) not mentioned in the changelog. Idiomatic, but note it or split it out.

### run 3 — tts-mcp#13 — src/server.py:270

**`src/server.py:270`** — `instruct` has no max length before being fed to GPU inference. Add a Pydantic `max_length`.

### run 3 — tts-mcp#13 — src/main.py:299

**`src/main.py:299`** — engine/language validation duplicates `src/server.py:693` byte for byte. Extract a shared helper into `src/tts/config.py`.

### run 3 — tts-mcp#13 — mcp/tts-mcp.ts:225

**`mcp/tts-mcp.ts:225`** — the log line interpolates `instruct` unbounded while `text` is sliced to 80. Truncate the same way.

### run 3 — tts-mcp#13 — scripts/download-model.sh:101

**`scripts/download-model.sh:101`** — `speech_tokenizer/` files are a hardcoded second loop instead of `speech_tokenizer/<file>` entries in `QWEN3_FILES`, unlike the `voice_embedding/` pattern.

### run 3 — tts-mcp#13 — scripts/download-model.sh:66

**`scripts/download-model.sh:66`** — `read -rp` under `set -e` exits silently on non-TTY stdin (pre-existing, line touched).

### run 3 — tts-mcp#13 — README.md:162

**`README.md:162`** — the example config shows only `engine: voxtral`; add a `qwen3` block with `language:`.

### run 3 — tts-mcp#13 — README.md:16

**`README.md:16`** — "9 named speakers" is stated as fixed, but speakers come from the model's `config.json`. Point readers to `GET /voices`.

### run 3 — tts-mcp#13 — CHANGELOG.md:26

**`CHANGELOG.md:26`** — declares a breaking `src.tts` API change, but the prefix bump table has no major signal. Acceptable under 0.x SemVer; flag for manual bump review.

### run 3 — github-releaser-agent#8 — pkg/githubtags/tags.go:35

**`pkg/githubtags/tags.go:35`** — `LatestSemverTag` lost its interface-level doc in this diff. The paragraph covering the ErrNoTags contract and "Implementations MUST be safe for concurrent use" was deleted from the type comment and never moved onto the method, while the new sibling `CommitSHAForTag` got a full method comment. Restore a method comment on the interface. *(rule: `go-doc/exported-item-must-have-comment`)*

### run 3 — github-releaser-agent#8 — pkg/githubtags/tags.go:236

**`pkg/githubtags/tags.go:236`** — bare `return "", err` forwards the collectTags failure while the function's other two error branches add `commit sha for tag: %s` context. Wrap with `errors.Wrapf(ctx, err, "commit sha for tag: %s", tag)`. Low operational impact today because the only caller's warning log already names owner, repo, and tag. *(rule: `go-errors/no-bare-return-err`)*

### run 3 — github-releaser-agent#8 — pkg/plan_output.go:111

**`pkg/plan_output.go:111`** — `PlanOutcomeNothingToRelease` extends an untyped string outcome set to four values with no typed newtype or `Available…` collection. If addressed, convert the whole Outcome block in one pass, not this member alone. *(rule: `go-enum-type/typed-constants-with-collection`)*

### run 3 — github-releaser-agent#8 — pkg/steps_planning.go:39

**`pkg/steps_planning.go:39`** — `releaseTagVerdictEscalate` plus `PlanOutcomeNothingToRelease` form an ad-hoc two-value verdict token set. Private scope, low risk. *(rule: `go-enum-type/typed-constants-with-collection`)*

### run 3 — github-releaser-agent#8 — pkg/steps_planning.go:750

**`pkg/steps_planning.go:750`** — `handleValidationFailure` takes eight loose params. A small struct would tighten the signature. The extraction itself is sound new logic, not a line-count split.

### run 3 — github-releaser-agent#8 — pkg/githubtags/tags.go:178

**`pkg/githubtags/tags.go:178`** — `fetchPage` still declares named results `tags` and `nextURL` but the body returns the locals `tagEntries` and `next`. Drop the names or use them.

### run 3 — github-releaser-agent#8 — pkg/githubtags/tags_test.go:347

**`pkg/githubtags/tags_test.go:347`** — `data, _ := json.Marshal(items)` in the new `tagJSONWithCommits` helper (again at line 539) discards the error. Inputs are literals so risk is nil, but an `Expect(err).NotTo(HaveOccurred())` matches the file's style.

### run 3 — github-releaser-agent#8 — pkg/steps_planning_test.go:2255

**`pkg/steps_planning_test.go:2255-2820`** — twelve `It` blocks repeat the fakeFetcher, fakeRunner, tagsFetcher, and NewPlanningStep wiring verbatim. A fixture helper would cut about 40 lines. Mirrors the pre-existing file convention, so not a regression.

### run 3 — discord-assistant#1 — src/index.js:272

**`src/index.js:272-278`** — the swallow is process-global and keyed on generic errno text (`ECONNRESET`, `ETIMEDOUT`, `ENOTFOUND`, `socket hang up`), so any subsystem can trip it, yet the branch assumes gateway origin, sets `gatewayReady = false`, and only `shardReady`/`shardResume` ever restore it. If the fault was not the gateway, the process stays alive, passes liveness, and reports not-ready forever, which is the exact state the surrounding comment calls worse than exiting. Add a bounded fallback: after a survived fault, start a timer (e.g. 5 min) that checks `client.ws.status` and either restores readiness or exits 1 so the supervisor restarts. *(rule: `node/lifecycle/crash-on-unhandled-rejection`)*

### run 3 — discord-assistant#1 — src/index.js:275

**`src/index.js:275`** — the survived-fault log drops the stack. The stack is the only evidence that the error actually came from the gateway socket, which is the premise of swallowing it. Log `stack: err.stack` like the fatal path does.

### run 3 — discord-assistant#1 — scripts/supervise.sh:19

**`scripts/supervise.sh:19-23`** — `stop()` kills the child and exits immediately without `wait "$child"`. Before this PR `make run` returned only after node finished draining (600 ms to 10 s); now it returns instantly, and `dev.sh` cleanup starts killing shim/s2s while the bot is still leaving voice. Add `wait "$child" 2>/dev/null` after the kill. *(rule: `node/lifecycle/handles-sigterm`)*

### run 3 — discord-assistant#1 — scripts/supervise.sh:35

**`scripts/supervise.sh:35-44`** — exit codes 126/127 (not executable / not found) retry forever at max backoff, the "same complaint in a loop" the script says it avoids. Treat them like exit 2.

### run 3 — discord-assistant#1 — src/index.js:259

**`src/index.js:259-278`** — new fault classification has no test although the repo runs `node --test` (9 tests). Extract a pure `classifyUncaught(err, { connectedOnce })` into its own module and unit-test the three outcomes.

### run 3 — discord-assistant#1 — CHANGELOG.md:5

**`CHANGELOG.md:5`** — new bullet has no conventional prefix (`fix:`). Every bullet in this file lacks one, so this is repo-wide, but the rule is MUST and drives version-bump detection. *(rule: `changelog/conventional-prefix-required`)*

### run 3 — discord-assistant#1 — src/index.js:263

**`src/index.js:263`** — second `client.once('clientReady')` exists only to set `connectedOnce`; set it in the existing handler at line 75.

### run 3 — discord-assistant#1 — src/index.js:105

**`src/index.js:105-114`** — no `client.on('shardError')` / `client.on('error')` listener. It would not have caught this fault, but it logs gateway errors at the documented layer.

### run 3 — discord-assistant#1 — scripts/supervise.sh:12

**`scripts/supervise.sh:12-13`** — `SUPERVISE_DELAY_MIN/MAX` not validated as integers; garbage fails deep in the loop instead of at startup.

### run 3 — discord-assistant#1 — Makefile:33

**`Makefile:33`** — token is resolved once and reused across restarts; a revoked token loops at 60 s forever. Mapping discord.js `TokenInvalid` to exit 2 in `src/index.js` would let the supervisor give up.

### run 3 — discord-assistant#1 — CHANGELOG.md:5

**`CHANGELOG.md:5-22`** — 18-line incident narrative in one bullet; guide asks for brief entries. Matches the repo's existing style, so low priority.

### run 3 — discord-assistant#1 — CHANGELOG.md:3

**`CHANGELOG.md:3`** — file has no canonical preamble block; all 12 mechanical hits are pre-existing headers, not introduced here. *(rule: `changelog/preamble-frozen`)*

### run 3 — discord-assistant#1 — package.json

**`package.json`** — `UNLICENSED` with no LICENSE file. Consistent for a private repo; no action needed.

### run 3 — helm#3 — charts/github-pr-reviewer/values.yaml:56

**`charts/github-pr-reviewer/values.yaml:56-58`** — the rationale says trust "enables bot *review* only; merging still requires a human click until [[Auto-Merge Trust-Gated PRs]] ships". That is no longer true, and this line is the switch. Since github-pr-watcher v0.5.0 (2026-08-18; live on both clusters at v0.5.2) the watcher arms GitHub-native auto-merge for **trusted authors** whose PR carries `AUTO_MERGE_LABEL` (default `auto-merge`, not overridden by this chart), and the update-go agent applies that label at PR creation. Result on GitHub: go-updater PRs `github-update-go-agent#43/#58/#59` and `github-pr-watcher#24–#27` merged with the sole review `ben-s-pull-request-reviewer[bot]:APPROVED` and `mergedBy=app/ben-s-pull-request-reviewer`. No human. The claim was accurate when the PR was authored (2026-07-29) and went stale 3 weeks later. Rewrite the comment to state the real blast radius: trust ⇒ bot review + auto-merge arming ⇒ unattended merge on repos with `autoMerge.trivial: true` once CI and the bot review are green. The same stale claim is in the commit body. The "does not transfer" argument for other bots then reads correctly, because the stakes are higher than "review only".

### run 3 — helm#3 — charts/github-pr-reviewer/values.yaml:52

**`charts/github-pr-reviewer/values.yaml:52-53`** — "NOT the REST API's `app/<slug>` rendering" is backwards. The REST `user.login` *is* `<slug>[bot]` (exactly what go-github returns to the watcher); `app/<slug>` is the search-qualifier / `gh --author` form. Reword so the next editor isn't steered wrong.
