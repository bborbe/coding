0552d54e05ac7550264fced8ffd32ef2f43be62270385221bb841f57af955470

## Configuration
- model: sonnet
- effort: medium
- mode: short
- config_hash: 0552d54e05ac7550264fced8ffd32ef2f43be62270385221bb841f57af955470
- rules_commands_hash: f67813389ac823b2cd7901946045233f3d970a153bac2297ce9550594cac457d
- prs_version: dev-1
- coding version: 0.36.0
- golden baseline coding version: v0.35.6
- golden version: golden-dev-2
- runner_version: 1
- rows skipped: 0
- cost: not recorded — the ledger carries no cost field.

*golden-dev-2* carries 1 `rejected` entry, so precision is measurable: reporting one costs precision. With so few adjudicated, a single hit still moves the number a long way — read it as directional, not calibrated.

36 of the 45 signatures embed a line reference, so a re-report of the same issue at a different line does not match and is surfaced as a gap-triage candidate rather than a hit. On this golden set, `recall` measures whether a configuration cited the same line, not whether it found the issue.

## Runs
| run | span | PRs | complete | golden in scope | findings | hits | misses | matched rejected | gap candidates | recall | precision | wall time (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-08-08T20:48:40.503577+00:00 … 2026-08-08T20:54:51.239079+00:00 | 5 | complete | 45 | 7 | 2 | 41 | 0 | 5 | 0.047 | 1.000 | 430 |

## Per-PR
| run | pr_id | golden in scope | hits | misses | findings | gap candidates | duration (s) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | tts-mcp#20 | 1 | 0 | 1 | 0 | 0 | 25 |
| 1 | github-pr-review-agent#11 | 14 | 0 | 13 | 0 | 0 | 59 |
| 1 | quant#109 | 6 | 0 | 6 | 2 | 2 | 101 |
| 1 | node-skeleton#2 | 10 | 1 | 9 | 2 | 1 | 186 |
| 1 | python-skeleton#3 | 14 | 1 | 12 | 3 | 2 | 60 |

## Gap-triage candidates
These findings matched no golden entry. They are **not a precision failure** — the golden set is a bootstrap from one strong-model run, and a finding it does not describe is evidence the set is incomplete.

### run 1 — quant#109 — task/recurring-schedules/prod/plan-next-week.yaml:24

**`task/recurring-schedules/prod/plan-next-week.yaml:24-52`** — this file's diff mixes two unrelated concerns in one changeset: the mechanical `due_date` addition (line 67) and a substantive rewrite of Success Criteria/Tasks/Definition of Done (Must/Should/Could → priority groups + explicit deferrals). If the rest of this PR is a pure "add due_date" sweep, this file's content rewrite belongs in a separate, reviewable commit/PR.

### run 1 — quant#109 — opnsense-update.yaml:68

**Inconsistent `due_date` choice across Saturday-scheduled tasks** — same-weekday (`weekday: Saturday`) schedules split between `due_date: "{{current_date}}"` (`opnsense-update.yaml:68`, `run-update-all-saturday.yaml:64`, `shutdown-k3s.yaml:64`, `topic-backup-saturday.yaml:67`, `turn-on-hell.yaml:66`, `home-assistant-update-backup.yaml:68`) and `due_date: "{{next_sun_date}}"` (`check-ftmo-demo-accounts.yaml:65`, `lexoffice-invoices.yaml:65`, `moneymoney-review.yaml:65`, `plan-next-week.yaml:67`, `plan-weekend.yaml:65`, `renew-gmail-oauth-tokens.yaml:64`, `weekly-review.yaml:64`). This may be intentional (same-day-critical chores before the weekend reboot vs. slippable admin chores), but `home-assistant-update-backup.yaml` reads like the latter category and got `current_date` — worth a second look to confirm it's deliberate rather than a copy-paste slip.

### run 1 — node-skeleton#2 — src/config.ts:59

**`src/config.ts:59`** — `check()` validates `port` and `shutdownTimeoutMs` but not `logLevel`; an invalid `LOG_LEVEL` silently falls back to `info` in `log.ts:19` (`?? LEVELS.info`) rather than failing fast like the other two fields. Not a regression (original JS didn't validate it either), just an inconsistency now that `check()` exists as the place to do it.

### run 1 — python-skeleton#3 — .github/workflows/ci.yml:27

**`.github/workflows/ci.yml:27-34`** — Trivy is installed unpinned (`apt-get install -y trivy` pulls whatever's latest in the repo at run time), so the vulnerability gate's behavior can change and break unrelated PRs with no code change. Pin a version (e.g. via the official install script with `TRIVY_VERSION`, or `aquasecurity/setup-trivy` action).

### run 1 — python-skeleton#3 — Makefile.precommit:56

**`Makefile.precommit:56-67`** — `trivy` target has no version pin either, for the same reason as CI; consider documenting the expected trivy version or pinning in CI so local and CI results agree over time.
