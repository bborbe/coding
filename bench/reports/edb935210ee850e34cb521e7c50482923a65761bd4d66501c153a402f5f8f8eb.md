edb935210ee850e34cb521e7c50482923a65761bd4d66501c153a402f5f8f8eb

## Configuration
- model: deepseek-v4-flash-max[1m]
- effort: high
- mode: full
- config_hash: edb935210ee850e34cb521e7c50482923a65761bd4d66501c153a402f5f8f8eb
- rules_commands_hash: ecc803331f860b845a6a1b8a103e889bce02520e8cc04ea88102de74c8d5600d
- prs_version: curated-1
- coding version: 0.45.0
- golden baseline coding version: v0.35.6
- golden version: golden-curated-3
- runner_version: 1
- rows skipped: 0
- cost: not recorded — the ledger carries no cost field.

*golden-curated-3* carries 20 `rejected` entries, so precision is measurable: reporting one costs precision. With so few adjudicated, a single hit still moves the number a long way — read it as directional, not calibrated.

No signature embeds a line reference, so a re-report of the same issue at a different line still matches. `recall` measures issue detection.

## Runs
| run | span | PRs | complete | golden in scope | findings | hits | misses | matched rejected | gap candidates | recall | precision | wall time (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-08-17T23:44:38.123900+00:00 … 2026-08-18T01:19:43.650357+00:00 | 20/20 | complete | 158 | 104 | 15 | 123 | 0 | 90 | 0.109 | 1.000 | 5780 |

## Per-PR
| run | pr_id | golden in scope | hits | misses | findings | gap candidates | dropped items | missing sections | duration (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | backup#15 | 4 | 0 | 2 | 2 | 2 | 0 | — | 185 |
| 1 | bw#41 | 3 | 0 | 2 | 1 | 1 | 0 | — | 126 |
| 1 | dark-factory#71 | 2 | 0 | 2 | 3 | 3 | 0 | — | 177 |
| 1 | discord-assistant#1 | 9 | 1 | 8 | 12 | 11 | 1 | — | 451 |
| 1 | discord-assistant#5 | 13 | 2 | 11 | 12 | 10 | 0 | — | 425 |
| 1 | distill#6 | 10 | 1 | 6 | 7 | 6 | 0 | — | 353 |
| 1 | github-pr-review-agent#11 | 16 | 2 | 12 | 6 | 4 | 1 | — | 855 |
| 1 | github-releaser-agent#8 | 13 | 1 | 8 | 5 | 4 | 0 | — | 468 |
| 1 | helm#3 | 4 | 0 | 3 | 1 | 1 | 0 | — | 78 |
| 1 | node-skeleton#2 | 12 | 1 | 11 | 1 | 0 | 0 | — | 306 |
| 1 | python-skeleton#3 | 17 | 3 | 13 | 9 | 7 | 0 | — | 283 |
| 1 | quant#109 | 10 | 1 | 9 | 2 | 1 | 0 | — | 144 |
| 1 | recurring-task-creator#30 | 6 | 1 | 3 | 6 | 5 | 0 | — | 440 |
| 1 | tts-mcp#10 | 9 | 0 | 7 | 7 | 7 | 0 | — | 215 |
| 1 | tts-mcp#13 | 10 | 2 | 8 | 13 | 11 | 0 | — | 286 |
| 1 | tts-mcp#16 | 6 | 0 | 5 | 4 | 4 | 0 | — | 272 |
| 1 | tts-mcp#20 | 1 | 0 | 1 | 0 | 0 | 0 | — | 90 |
| 1 | tts-mcp#3 | 6 | 0 | 5 | 6 | 6 | 0 | — | 210 |
| 1 | vault-cli#68 | 1 | 0 | 1 | 0 | 0 | 0 | — | 134 |
| 1 | vault-ui#39 | 6 | 0 | 6 | 7 | 7 | 0 | — | 282 |

*Effective fixture: **20 of 20** PRs produced a scored row.* **2** findings carried no matching key and never reached scoring.

## Gap-triage candidates
These findings matched no golden entry. They are **not a precision failure** — the golden set is a bootstrap from one strong-model run, and a finding it does not describe is evidence the set is incomplete.

### run 1 — backup#15 — Makefile:127

**`Makefile:127`** — the `build` target still lacks the three canonical Go-service build args (`BUILD_GIT_VERSION`, `BUILD_GIT_COMMIT`, `BUILD_DATE`) that belong alongside the new `--build-arg NPM_REGISTRY`; the Go stage (`Dockerfile:4`) wires only `-ldflags "-s"` and `main.go` has no matching `-ldflags "-X"` var block. This is **pre-existing** (confirmed absent in `origin/bench-base-15`), not introduced by this PR — a reasonable follow-up rather than a blocker. *(rule: `go-build-args/three-args-required`)*

### run 1 — backup#15 — Dockerfile:13

**`Dockerfile:13`** — frontend deps install via `npm install` rather than `npm ci`, despite a committed `frontend/package-lock.json` (lockfileVersion 3), so the image build isn't guaranteed to match the reviewed dependency tree. Note: this line is **unchanged** by the PR, and the literal rule targets a Makefile `install` target that doesn't exist here (frontend is a Frontend-Variant app where the doc permits `npm install` in Makefile targets) — the image-build reproducibility argument is the only reason it's worth a look. *(rule: `node/make/install-from-lockfile`)*

### run 1 — bw#41 — groups/meta/k3s.py:7

**`groups/meta/k3s.py:7-10`** — the comment states `arp_tbl` is "a single GLOBAL kernel table counting entries across every network namespace." Kernel neighbour tables are actually instantiated **per network namespace** (shared across interfaces *within* one netns, not across netns). The failure signature and fix are still correct, but the wording is factually off in a load-bearing incident comment — worth tightening so the rationale stays accurate for future maintainers.

### run 1 — dark-factory#71 — CHANGELOG.md:15

**`CHANGELOG.md:15`** — "hands back exact find/replace pairs for each **Blocker**/Critical issue": the agent's severity vocabulary is `Critical Issues` / `Recommendations` (`agents/prompt-auditor.md:493,498`) — "Blocker" appears nowhere in the artifact the changelog describes. Change to "Critical issue" (or "Critical/Recommendation findings") to keep the entry accurate.

### run 1 — dark-factory#71 — agents/prompt-auditor.md:494

**`agents/prompt-auditor.md:494-496`** — the find/replace contract says "old text verbatim" but doesn't demand the find string be *unique* in the prompt or specify a pair format. The whole point is that the caller applies the pair "without re-deriving your reasoning"; a verbatim snippet that matches in several places re-derives exactly that. Extend one clause: "old text verbatim — include enough surrounding context that the find string is unique — and the new text to put in its place" (optionally `FIND:`/`REPLACE:` blocks). This also cross-links the existing `Anchoring` precedent at line 276 so the two instructions can't drift.

### run 1 — dark-factory#71 — agents/prompt-auditor.md:509

**`agents/prompt-auditor.md:509-518`** — the new `<final_step>` nearly verbatim duplicates the find/replace guidance already in `<output_format>` (lines 494-500) and drops the legitimate "Verify references" follow-up the old menu offered (a deep-dive the audit-only agent *can* perform — it has Bash). Trim the final_step to the audit-only refusal plus a re-audit offer, or have one copy reference the other.

### run 1 — discord-assistant#1 — CHANGELOG.md:5

**`CHANGELOG.md:5`** — the new `## Unreleased` bullet starts with `Survive…` instead of a conventional prefix, so dark-factory / `/coding:commit` cannot determine the version bump (release may patch-bump a feature or miss the entry entirely). Prepend `fix:` (crash-survival bug fix). *(rule: `changelog/conventional-prefix-required`)*

### run 1 — discord-assistant#1 — src/index.js:272

**`src/index.js:272-277`** — the `connectedOnce && RECOVERABLE_NET` swallow is unbounded: if the gateway never reconnects (revoked token, persistent partition), the process stays alive with `/healthz` green, readiness red forever, and **both** recovery paths bypassed — `supervise.sh` and the k8s liveness probe only restart on process exit. This is exactly the "alive, passing liveness, permanently disconnected — strictly worse than exiting" state the PR's own comment says it exists to avoid. Add a bounded recovery window (timer or `shardDisconnect`→`shardReady` cycles) and `process.exit(1)` if the gateway has not recovered, so the supervisor/k8s restart and make the failure visible.

### run 1 — discord-assistant#1 — src/index.js:259

**`src/index.js:259-260`** — `RECOVERABLE_NET` is not gateway-scoped: `ECONNRESET`, `ETIMEDOUT`, `socket hang up` can originate from the voice UDP path, the OpenAI endpoint, or REST command-registration — a misattributed fault would be swallowed, logged as a "gateway network fault", and drain traffic while masking a real failure elsewhere. Restrict to gateway-origin errors (e.g. the observed `Opening handshake has timed out` / `WebSocket was closed`), and match `err.code` as well as `err.message` (transport codes live on `code`, not `message`).

### run 1 — discord-assistant#1 — scripts/supervise.sh:47

**`scripts/supervise.sh:47`** — a trapped signal during the backoff `sleep` is deferred until `sleep` exits (up to `DELAY_MAX`=60s): Ctrl-C while backing off makes `make run` appear to hang. Make the sleep interruptible (`sleep "$delay" & wait $!`).

### run 1 — discord-assistant#1 — scripts/supervise.sh:19

**`scripts/supervise.sh:19-23`** — `stop()` kills the child and exits 0 immediately without waiting or reaping: the child's graceful drain runs orphaned, its exit code is never propagated, and a TERM-ignoring child survives while the supervisor reports a clean stop. `wait` the child (bounded) after the kill and exit `128+signum` so an orchestrator sees a non-zero "killed" status.

### run 1 — discord-assistant#1 — CHANGELOG.md:3

**`CHANGELOG.md:3`** — the canonical preamble ("All notable changes…", SemVer bullets) is entirely absent — the file goes `# Changelog` → `## Unreleased` — which breaks the same release tooling on the *next* release. *(rule: `changelog/preamble-frozen`)* **Pre-existing** (present in the base branch; the 12 mechanical flags all fire on unchanged lines) — do **not** block this PR; restore the frozen header block in a follow-up.

### run 1 — discord-assistant#1 — scripts/supervise.sh:29

**`scripts/supervise.sh:29-31`** — microsecond race where a signal landing between `wait` returning and `child=""` can `kill` a stale/recycled PID; clear the var inside the trap and guard with `kill -0`.

### run 1 — discord-assistant#1 — scripts/supervise.sh:50

**`scripts/supervise.sh:50-51`** — backoff never resets after a long healthy run: a crash after 8h uptime still waits up to 60s to restart. Consider resetting `delay` when the child has run past a healthy threshold.

### run 1 — discord-assistant#1 — scripts/supervise.sh:27

**`scripts/supervise.sh:27`** — no arg-count check; `supervise.sh` with no args quietly backgrounds an empty subshell and exits 0.

### run 1 — discord-assistant#1 — scripts/supervise.sh:41

**`scripts/supervise.sh:41`** — non-ASCII em-dash in a stderr message (garbles under legacy locales); the other message already uses ASCII.

### run 1 — discord-assistant#1 — package.json:18

**`LICENSE` / `README.md` / `package.json:18`** — no root LICENSE, no README license section, `"license": "UNLICENSED"`. **Pre-existing**, and `"private": true` + `UNLICENSED` suggest intentional non-distribution — only add a LICENSE if this is meant to be public (the Dockerfile label does point at `github.com/bborbe/discord-assistant`).

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:799

**`shim/claude_openai_shim.py:799`** + **`:826`** — per-item quoted values silently disable the whole gate. `SHIM_WAKE_PHRASES='"hey bot","hey bought"'` (a natural Makefile spelling) only loses its *outer* quotes via `.strip("\"'")`; each phrase then carries a literal `"` (`'hey bot"'`, `'"hey bought'` — verified), matches nothing, and every voice turn goes QUIET with no diagnostic. This is exactly the fail-quiet-without-a-trace failure mode the design says it wants to avoid. The JS side (`src/config.js:119-125`) has the identical hole. Fix: `p.strip().strip("\"'")` per phrase on both sides.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:799

**`shim/claude_openai_shim.py:799`** — a YAML-list `voice.wake_phrases: [hey bot, hey bought]` arrives through `setting()` as `str(list)` = `"['hey bot', 'hey bought']"`, so the split yields bracket/quoted fragments that match nothing → all voice turns QUIET (verified). The shipped `config.example.yaml` uses the working comma-string form, so this only bites a user who writes the natural YAML list. Fix: normalize list values in `setting()`/`WAKE_PHRASES`, or document loudly that the value must be a comma-joined string.

### run 1 — discord-assistant#5 — test/voice.test.js:227

**`test/voice.test.js:227`** — the boolean-spellings test evicts `require.cache[require.resolve('../src/config')]` and re-requires, manufacturing a fresh module object while the file's top-level `config` binding and `voice.js`'s captured reference still point at the old one — a shared-fixture ordering hazard that only works today because every reference was captured at load time. Fix: export `flag` from `config.js` and unit-test the parser directly (no env mutation, no cache eviction); the test already proves `flag()` is the unit under test.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:110

**`shim/claude_openai_shim.py:110`** — `setting()` strips whitespace before quotes, so `" 0 "` (Make-quoted with inner spaces) parses as TRUE. Swap to `.strip("\"'").strip()`.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:810

**`shim/claude_openai_shim.py:810-815`** — the `_WAKE_LEAD` comment lists "a 'so,' lead-in" among the covered real failures, but `so` is deliberately NOT in the skippable set (the JS test asserts `"So, hey bot, what's my task"` → false). The comment contradicts the changelog's stated design; correct it or add `so` deliberately.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:822

**`shim/claude_openai_shim.py:822-829`** — an all-empty-item config (`SHIM_WAKE_PHRASES=" , "`) bypasses the whole-string guard and compiles `_WAKE_RE` into `(?:)\b`, matching everything → gate silently always-on. Build the filtered phrase list once at module level and gate both `is_addressed`/`strip_wake_phrase` on it.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:826

**`shim/claude_openai_shim.py:826`** — the `\b` boundary doesn't protect apostrophes: `"hey bot's a good one"` matches the phrase. Decide whether a possessive/contraction should wake the bot; if not, use a negative lookahead.

### run 1 — discord-assistant#5 — test/voice.test.js:211

**`test/voice.test.js:211`** and **`:561`** — the `isAddressed` tests depend on the ambient default `config.wakePhrases`; a developer with `SHIM_WAKE_PHRASES` exported gets different results. Construct the matcher with an explicit phrase list.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:821

**`shim/claude_openai_shim.py:821`** + **`src/config.js:30`** — JS `\W` is ASCII-only while Python's is Unicode-aware, so a German filler `"Ähm hey bot, …"` raises the typing dots on the bot side while the endpoint stays quiet (JS treats `Ä` as non-word and consumes it, Python doesn't). This is within the documented "cosmetic drift" envelope, but for the German-speaking user this feature targets, consider adding `ähm` to both filler lists.

### run 1 — discord-assistant#5 — shim/claude_openai_shim.py:864

**`shim/claude_openai_shim.py:864`** — the lstrip set also strips a leading hyphen, so `"hey bot -list tasks"` becomes `"list tasks"`. Unlikely in speech; noted for completeness.

### run 1 — distill#6 — hooks/deny-generated-file-edits.sh:12

**`hooks/deny-generated-file-edits.sh:12`** — `set -e` combined with `tool=$(jq -r '.tool_name' <<<"$input")` fails closed on empty/malformed stdin: jq exits 2, `set -e` kills the script with exit 2, and per the Claude Code hook contract a non-0 exit with no JSON on stdout **blocks** the tool call — the opposite of the script's stated passthrough intent. Latent (Claude Code always pipes valid JSON to PreToolUse hooks in practice), but the cheap hardening is `jq -r '.tool_name' <<<"$input" 2>/dev/null || true`, which drops malformed input into the `*)` passthrough branch.

### run 1 — distill#6 — README.md:128

**`README.md:128`** and **`docs/adopting-distill.md:110`** — `cp hooks/deny-generated-file-edits.sh ~/.claude/hooks/` fails with `No such file or directory` if `~/.claude/hooks` doesn't exist yet. Prepend `mkdir -p ~/.claude/hooks &&` in both places.

### run 1 — distill#6 — hooks/deny-generated-file-edits.sh:36

**`hooks/deny-generated-file-edits.sh:36`** — `Source:` extraction uses `grep -oE 'Source:[[:space:]]*\S+'`, which truncates the value at the first space (a source dir like `../my rules` yields `../my` in the denial hint) and `\S` is a GNU-extension class. Portable fix: `source_dir=$(grep -m1 '^Source:' <<<"$header" | sed -E 's/^Source:[[:space:]]*//')`.

### run 1 — distill#6 — hooks/deny-generated-file-edits.sh:36

**`hooks/deny-generated-file-edits.sh:36-37`** — append `|| true` to the `grep | head | sed` pipelines. Today they're safe under `set -e`, but a future `set -o pipefail` would turn a missing `Source:`/`Regenerate:` line (grep exit 1) into a non-2 exit mid-deny — which per the hook contract silently converts a deny into a pass.

### run 1 — distill#6 — README.md:139

**`README.md:139`** — `MultiEdit` is not in the matcher (or the hook's `case`), so a generated file could still be rewritten via `MultiEdit`. Either extend the hook+matcher or scope the "protected, wherever it lives" claim.

### run 1 — distill#6 — README.md:166

**`README.md:166`** — the `adopting-distill.md` cross-reference sits inside `## Development` although it's usage/migration guidance; it would read more naturally under `## Usage` or its own heading.

### run 1 — github-pr-review-agent#11 — README.md:33

**`README.md:33`** — The `--skip-post` example (`go run ./cmd/run-task --task-file=/path/to/task.md --phase=execution --skip-post`) is not runnable as written: `cmd/run-task/main.go:125-131` requires GitHub App credentials (`APP_ID`, `INSTALLATION_ID`, `PEM_KEY_FILE`/`PEM_KEY`) and mints an IAT *before* `SkipPost` has any effect, so a reader following the example gets `pr-reviewer auth: GitHub App credentials not configured`. Auth stays required by design (cloning needs a token), but the docs never say so, and the CHANGELOG's "safe local review runs" wording implies otherwise. Fix: show the required env vars in the example, or add one line to the run-modes table noting credentials are still required for read ops.

### run 1 — github-pr-review-agent#11 — cmd/run-task/main.go:76

**`cmd/run-task/main.go:76` / `pkg/factory/runner.go:57-59`** — "Suppresses all GitHub *write* calls" under-describes the flag: it also suppresses the verifier's live GitHub API *read*. Suggest "suppresses all GitHub posting and verification calls". *(rule: `go-architecture/business-logic-not-in-main` adjudication; go-architecture + readme agents)*

### run 1 — github-pr-review-agent#11 — pkg/steps_checkout_execution.go:315

**`pkg/steps_checkout_execution.go:315-321`** — The nil-poster early return is silent. A `glog.V(2)` line here (matching the `cmd/run-task` startup log) would make "why wasn't this review posted?" self-evident from logs. Consider also a comment noting the funnel fail-closed gate is intentionally not applied in skip-post mode (nothing is posted, so there's nothing to gate — correct by design, but a future reader shouldn't "fix" it).

### run 1 — github-pr-review-agent#11 — pkg/skip_post_boundary_test.go

**`pkg/skip_post_boundary_test.go` (case 3)** — The synthetic combo (non-nil mock verifier + nil poster) sits outside the file's stated "nil-interface contract" premise; a one-line scope note would clarify why it exists (it's a positive control proving the fixture reaches the guard).

### run 1 — github-releaser-agent#8 — go.mod:98

**`go.mod:98`** — `make precommit` fails on the `vulncheck` step: indirect dep `golang.org/x/mod v0.37.0` carries **GO-2026-6179** and **GO-2026-6180** (sumdb/tlog tile verification bypass; unauthenticated-hash ignore in `Lookup`); fix is the upgrade `v0.37.0 → v0.40.0`. ⚠️ *Pre-existing — `go.mod` is untouched by this diff — but it hard-blocks the release gate this PR is heading toward, so it must be resolved in/alongside this work.* All other precommit stages passed (fmt, generate, tests OK, golangci-lint 0 issues, vet).

### run 1 — github-releaser-agent#8 — pkg/githubtags/tags.go:131

**`pkg/githubtags/tags.go:131`** — the `collectTags` pagination loop has no explicit `ctx.Done()` check at loop top. `ctx` is threaded into every request (`http.NewRequestWithContext`), so cancellation genuinely aborts in-flight fetches — this is belt-and-suspenders for rule-consistency: add `select { case <-ctx.Done(): return nil, ctx.Err(); default: }`. *(rule: `go-context/cancel-check-in-loop`)*

### run 1 — github-releaser-agent#8 — pkg/plan_output.go:111

**`pkg/plan_output.go:111`** — `PlanOutcomeNothingToRelease` joins a pre-existing block of untyped string outcome constants. The smell predates this diff (the new value merely follows the pattern), but the proper fix is a batch change: a typed `PlanOutcome` newtype + an `AvailablePlanOutcomes()` collection across the whole block, not per-constant. *(rule: `go-enum-type/typed-constants-with-collection`)*

### run 1 — github-releaser-agent#8 — pkg/steps_planning.go:750

**`pkg/steps_planning.go:750-773`** — `handleValidationFailure` threads 8 parameters (`ctx, md, owner, name, ref, effectiveVersion, reason, snapshotVersion`). Not a naive extraction (real decision branch, 450+ lines of tests), but the param list could group into a small struct. *(rule: `go-composition/no-same-package-private-helper-for-business-logic`, design note only)*

### run 1 — helm#3 — charts/github-pr-reviewer/Chart.yaml:9

**`charts/github-pr-reviewer/Chart.yaml:9`** — version bump magnitude: `0.1.0 → 0.2.0` for an additive config-only change. The repo's own precedent for a comparable additive change (`github-dark-factory` resource defaults) used a patch bump (`0.1.0 → 0.1.1`). A minor bump is defensible semver-wise (new trusted author = backward-compatible new behavior), so this is a consistency check, not a blocker — just confirm the minor bump is intentional.

### run 1 — python-skeleton#3 — CHANGELOG.md:18

**`CHANGELOG.md:18`** — bullet `- ci: install trivy in CI` uses the `ci:` prefix, which is **not** in the guide's recognised prefix set (`feat:`, `fix:`, `refactor:`, `test:`, `docs:`, `chore:`, `perf:`) that the release/bump tooling keys on. Use `chore:` (build/tooling) so the entry classifies correctly. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — python-skeleton#3 — Makefile.precommit:50

**`Makefile.precommit:50-62`** — `make precommit` now hard-fails with a bare `trivy: command not found` on any machine without trivy (only CI installs it). A `command -v trivy` guard with an install hint would help.

### run 1 — python-skeleton#3 — Makefile.precommit:54

**`Makefile.precommit:54`** — `--db-repository` pins a single DB source, dropping trivy's default mirror fallback; on ghcr rate-limit the gate fails. Consistent with go/node skeletons, so optional.

### run 1 — python-skeleton#3 — README.md:86

**`README.md:86`** — "to add a third advisory database over ground pip-audit and trivy already cover" — "over ground" is a malapropism; reword to "beyond the ground" / "on top of the ground".

### run 1 — python-skeleton#3 — README.md:69

**`README.md:69-73`** — the Development command list documents `make audit`/`make trivy` but omits `make check`, the central command of the new Security gates section.

### run 1 — python-skeleton#3 — README.md:76

**`README.md:76-94`** — the new section is five dense prose paragraphs, notably more verbose than the terse README; the decision-log rationale could be tightened to bullets (or moved to CLAUDE.md per readme-guide).

### run 1 — python-skeleton#3 — tests/test_factory.py:33

**`tests/test_factory.py:33`** — test name `test_create_app_includes_routers` still describes structure while the assertion is now behavioral (endpoints return 200); `test_create_app_serves_health_and_metrics` would match what it checks.

### run 1 — quant#109 — task/recurring-schedules/prod/check-ftmo-demo-accounts.yaml:65

**`task/recurring-schedules/prod/check-ftmo-demo-accounts.yaml:65`** — the `due_date` convention is applied inconsistently across **Saturday** schedules with no documented rationale. Seven Saturday tasks get `{{next_sun_date}}` (`check-ftmo-demo-accounts`, `lexoffice-invoices`, `moneymoney-review`, `plan-next-week`, `plan-weekend`, `renew-gmail-oauth-tokens`, `weekly-review`) while six ops Saturday tasks get `{{current_date}}` (`home-assistant-update-backup`, `opnsense-update`, `run-update-all-saturday`, `shutdown-k3s`, `topic-backup-saturday`, `turn-on-hell`). The bodies justify the split (ops tasks must complete before the Saturday cluster restart; planning/review/finance tasks have Sunday grace), so this is likely intentional — but the rule lives nowhere, so a future edit can't tell "deliberate" from "typo". Confirm the split and add a one-line comment (e.g. next to the first `{{next_sun_date}}` usage) stating the convention.

### run 1 — recurring-task-creator#30 — pkg/publisher/period_token.go:105

**`pkg/publisher/period_token.go:105,109,113`** — the `time.Date(base.Year(), base.Month(), 1, 0, 0, 0, 0, base.Location())` day-1 anchor is duplicated verbatim 3× across the Monthly/Quarterly/Yearly cases. The prompt explicitly permitted inline-per-case, so this is not a defect — but factoring a `firstOfMonth(t time.Time)` helper into `render.go` next to the existing `firstOfPreviousMonth` would be cleaner DRY. Must preserve `base.Location()` (unlike the UTC-hardcoding exemplar).

### run 1 — recurring-task-creator#30 — pkg/publisher/period_token_test.go:52

**`pkg/publisher/period_token_test.go:52-73`** — the `2026-08-31 → 2026-07` entry is grouped under the "Discriminating entries" comment but is actually a control/regression entry (July has 31 days, so old code also produced `2026-07`). The entry's own comment is self-aware; only the section grouping is slightly misleading.

### run 1 — recurring-task-creator#30 — pkg/publisher/period_token_test.go

**`pkg/publisher/period_token_test.go`** — no positive-offset discriminating case is covered (e.g. `2026-01-31, +1`: old code overflows to `2026-03` vs. new `2026-02`). A one-line `Entry` would close the `+1` direction.

### run 1 — recurring-task-creator#30 — CHANGELOG.md:13

**`CHANGELOG.md:13`** — the `fix:` bullet is a single 466-char line (by far the longest in the file). No line-length rule exists in the changelog guide and it's verbatim the prompt's required text, so this is style-only; a wrap would read better.

### run 1 — recurring-task-creator#30 — go.mod

**`go.mod`** — the `counterfeiter v6.12.2 // indirect` entry is stale (only used via `go run`, never imported); pre-existing, next `go mod tidy` cleans it.

### run 1 — tts-mcp#10 — CHANGELOG.md:16

**`CHANGELOG.md:16`** — new `## Unreleased` bullet under `### Fixed` starts with `Bumped…`, no conventional prefix. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — tts-mcp#10 — CHANGELOG.md:17

**`CHANGELOG.md:17`** — new bullet `The two remaining advisories are…` starts with `The`, no conventional prefix. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — tts-mcp#10 — CHANGELOG.md:21

**`CHANGELOG.md:21`** — new bullet under `### Changed` starts with a backtick (`` `make check` ``), no conventional prefix. *(rule: `changelog/conventional-prefix-required`)* All three are MUST-level per the coding plugin's changelog guide; as dependency/tooling entries they should carry `chore:` (e.g. `- chore: Bump the transitive dependency tree in mcp/ …`). **Nuance for the maintainer:** this repo's CHANGELOG uses Keep-a-Changelog style (category headings, unprefixed bullets) throughout — the already-released v0.1.0 section ships identically, so this is consistent with the repo's *established* convention, and `.maintainer.yaml` `release.autoRelease: true` means the prefix question is material only if the release bot parses prefixes. If the conventional-prefix convention is being adopted, the fix is trivial (prefix all three); otherwise this is a convention-drift call the team should make explicitly.

### run 1 — tts-mcp#10 — Makefile.precommit:30

**`Makefile.precommit:30`** — the `check` target now gates the TS relay (`mcp-typecheck`) but adds only typechecking: no dependency-vulnerability scan and no secret detection for the Node component the PR itself brings under the gate. *(rule: `node/make/security-gates-in-check`)*. **Caveat:** wiring `npm audit --audit-level=high` in blindly would fail CI immediately, because this PR *deliberately defers* a high-severity advisory (`@hono/node-server <2.0.5` path traversal, documented at `CHANGELOG.md:17` as Windows-specific and unreachable on this macOS-only, static-file-free relay). The right fix is a deliberate policy — e.g. `npm audit --audit-level=critical` in `check`, or an explicit documented exception — so the audit is enforced by the gate rather than by memory.

### run 1 — tts-mcp#10 — Makefile.precommit:26

**`Makefile.precommit:26`** — `npm ci` inside `mcp-typecheck` runs a full reinstall on every `make check`/`precommit`, including Python-only changes. Consider splitting install into a prerequisite and skipping it when `node_modules` is current.

### run 1 — tts-mcp#10 — .github/workflows/ci.yml:36

**`mcp/package.json` + `.github/workflows/ci.yml:36-39`** — CI pins the **Node 22 runtime** while `@types/node` is `^25.2.2` (this PR bumps the lockfile to 25.9.5 — the type surface is 3 majors ahead of the runtime). `tsc --noEmit` against Node 25 types cannot catch use of APIs that don't exist on Node 22. Consider pinning `@types/node` to `^22`.

### run 1 — tts-mcp#10 — Makefile.precommit

**`Makefile.precommit`** — the TS relay now has typecheck coverage but no lint or test target; it's the only Node gate. Coverage gap, not a rule violation.

### run 1 — tts-mcp#13 — src/server.py:353

**`src/server.py:353`** — A voxtral request with `instruct` is *accepted*: `POST /say` returns 202 and queues the item; `VoxtralEngine.generate` raises `ValueError` only later on the worker. The PR's CHANGELOG and README claim the voxtral engine "fails the request rather than silently ignoring it," but in **streaming mode** it can silently *succeed*: `streaming_chunk_iter(...)` raises on the first `next()`, `play_stream` pushes the `None` sentinel (`src/tts/player.py:248`), the player thread calls `on_complete` → `status="completed"` with no audio, racing `_fail_item`'s `"error"` — whichever lands last wins. A rejected request can report `completed` and the text is never spoken or saved. This violates the project's own no-silent-fallback rule. Fix: reject at the boundary — raise `HTTPException(400/422)` in `say()` when `state.engine.kind == VOXTRAL` and `body.instruct is not None` (mirrors the existing voice validation at `src/server.py:332`), and ensure the streaming error path can't be overwritten by the empty-stream `on_complete`.

### run 1 — tts-mcp#13 — README.md:133

**`README.md:133`** — "Download a Model" is now stale because of this PR: it still says "Presents **three Voxtral 4B variants**" and "update `config.yaml` with the **model path**," while `scripts/download-model.sh` now offers 5 models and prints `engine:`/`language:`/`default_voice:` lines. Also stale: `README.md:146` ("downloads a Voxtral model") and the architecture diagram at `README.md:57` (`src/tts.py`, which no longer exists — it's the `src/tts/` package).

### run 1 — tts-mcp#13 — src/tts/worker.py:227

**`src/tts/worker.py:227`** — `engine` is threaded as a runtime parameter through a 4-layer CLI chain (`audio_worker_from_model_id` → `audio_worker` → `_run_*_worker` → `_generate_worker_chunks`/`streaming_chunk_iter`), with the middle layers only forwarding it. The server path solves this properly via `ServerState.__init__` + `state.engine`; the codebase now carries two DI styles. Consolidate the CLI worker's stable collaborators (engine, settings, player) into a constructor-injected context/`AudioWorker` class.

### run 1 — tts-mcp#13 — CHANGELOG.md:12

**`CHANGELOG.md:12`** — None of the 9 `## Unreleased` bullets start with a conventional prefix. *(rule: `changelog/conventional-prefix-required`)* — flagged, but note the repo's established convention is free prose (0/48 prior bullets). The right fix is a repo-wide decision (adopt prefixes everywhere, or keep prose), not a one-off; forcing prefixes on this PR alone would make it inconsistent with every prior release.

### run 1 — tts-mcp#13 — src/tts/engine.py:47

**`src/tts/engine.py:47`** — `instruct` (qwen3-only) leaks into the shared `TTSEngine.generate` Protocol signature; voxtral fails at runtime instead of at build time. `language` was correctly constructor-injected — `instruct` could ride the engine too.

### run 1 — tts-mcp#13 — README.md:263

**`README.md:263`** — The `/say` example mixes engines: `casual_female` (a voxtral voice) with `instruct` (qwen3-only). Label it "qwen3 example" or use a qwen3 speaker (e.g. `ryan`).

### run 1 — tts-mcp#13 — README.md:161

**`README.md:161`** — The sole full config example never shows `language:`, the one new key a qwen3 user must get right.

### run 1 — tts-mcp#13 — src/main.py:299

**`src/main.py:299` / `src/server.py:693`** — Byte-identical `engine`/`language` presence+type validation duplicated in both config parsers; extract a shared helper.

### run 1 — tts-mcp#13 — src/tts/engine.py:190

**`src/tts/engine.py:190`** — `json.load(config.json)` in `Qwen3Engine.discover_voices` propagates a raw `JSONDecodeError` at startup; wrapping it to name `config_path` would match the module's own error style.

### run 1 — tts-mcp#13 — scripts/download-model.sh:83

**`scripts/download-model.sh:83`** — The `if [ "$ENGINE" = "voxtral" ] ... else` branch implicitly treats any non-voxtral engine as qwen3; an explicit `elif ... else exit 1` would be more robust.

### run 1 — tts-mcp#13 — scripts/download-model.sh:112

**`scripts/download-model.sh:112`** — `language: English` / `default_voice: ryan` hints are hardcoded rather than derived from the model.

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — the frontmatter `description` has grown to ~616 chars, mixing trigger phrases, the "sole authority" claim, the no-selftest caveat, and the full Args list, and it mixes unprefixed `/voice` with prefixed `/tts-mcp:voice-selfcheck`. Long, cluttered descriptions dilute the auto-activation discovery signal. Consider trimming to the WHEN/trigger sentence + `Args:` and using one command form. (Flagged independently by `skill-auditor` and `slash-command-auditor`.)

### run 1 — tts-mcp#16 — skills/voice/SKILL.md:8

**`skills/voice/SKILL.md:8,94`** — both rewritten lines reference the personal path `~/.claude/CLAUDE.md`, which the skill-writing guide's portability rule discourages. Functionally accurate here (this skill is user-specific by design), but worth knowing it's a deliberate exception. *(no rule id — guide checklist note, not a MUST violation)*

### run 1 — tts-mcp#16 — skills/voice/SKILL.md

**`skills/voice/SKILL.md`** (whole file) — no explicit Success Criteria section, per the skill-writing guide's checklist; verification is only implicit via `status` and the activation confirmation. *(no rule id)*

### run 1 — tts-mcp#16 — None

**`e75300e`** — commit subject is 63 chars, over the 50-char limit. *(rule: `git-commit/subject-under-50-chars`)*

### run 1 — tts-mcp#3 — commands/voice-test.md:1

**`commands/voice-test.md:1-3`** — frontmatter has only `description:`; the MUST rule requires `allowed-tools`, and omitting it leaves command permissions unbounded. Fix: add a restrictive tool list (e.g. `mcp__tts__say`, `mcp__tts__get_voices`, `mcp__tts__get_status`). *(rule: `agent-cmd/command-frontmatter`)*

### run 1 — tts-mcp#3 — CHANGELOG.md:12

**`CHANGELOG.md:12,16`** — the two new `## Unreleased` bullets don't start with a conventional prefix (`feat:` / `refactor:`), so the release bot can't machine-derive the bump. Caveat reported honestly: every prior section (v0.1.0 and older) uses this same prefix-less format and was auto-released fine, so this is a deliberate repo-wide convention — but `.maintainer.yaml` sets `autoRelease: true`, so adding prefixes is cheap insurance. *(rule: `changelog/conventional-prefix-required`)*

### run 1 — tts-mcp#3 — commands/voice-test.md:1

**`commands/voice-test.md:1-3`** — missing `argument-hint` (zero-arg command; `argument-hint: ""` satisfies the MUST mechanically). *(rule: `agent-cmd/command-frontmatter`)*

### run 1 — tts-mcp#3 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — the appended description clause ("Activation does not run a selftest…") is behavioral, not a discovery trigger; the behavior is already documented in the body. Consider trimming to keep the trigger signal strong. *(rule: `skill-writing/skill-md-frontmatter-required`)*

### run 1 — tts-mcp#3 — skills/voice/SKILL.md

**`skills/voice/SKILL.md`** — no explicit Success Criteria block, per the guide's template.

### run 1 — tts-mcp#3 — commands/voice-test.md:18

**`commands/voice-test.md:18`** — "per the playbook" references the skill's playbook without a link (minor traceability note).

### run 1 — vault-ui#39 — src/vault_ui/activity.py:61

**`src/vault_ui/activity.py:61`** — the `root.glob(f"*/{filename}")` fallback in `transcript_mtime` scans the **entire** `~/.claude/projects/*` tree per lookup, and `_task_to_response` / `_goal_to_response` call `compute_activity_date` per card inside the `list_tasks` / `list_goals` loops (`src/vault_ui/api/tasks.py:606`, `:705`, `:1794`) — including on cache hits, since the vault-mtime cache only skips the vault-cli subprocess, not response conversion. Every card whose session transcript isn't in the vault's own encoded-cwd directory (cloud/container sessions, or sessions started from a code repo) triggers an O(project-dirs) directory enumeration per render, so a large board with many such sessions is O(tasks × project-dirs) uncached filesystem scans per request. The direct-hit `stat()` covers the common local case, so this is a hot-path amplification, not a correctness bug. Fix: memoize — `transcript_mtime` is pure, so `functools.lru_cache` keyed on `(session_id, project_dir, projects_root)`, or batch-resolve session mtimes once per request per vault.

### run 1 — vault-ui#39 — src/vault_ui/vault_cli_client.py:316

**`src/vault_ui/vault_cli_client.py:316-319`** — the new `_parse_goal` `modified_date` block is a byte-for-byte copy of `_parse_task` (`:248-251`); a shared `_parse_datetime(value)` helper removes the duplication and gives `fromisoformat` + `suppress` behavior a single test surface.

### run 1 — vault-ui#39 — src/vault_ui/api/tasks.py:708

**`src/vault_ui/api/tasks.py:708`, `:1797`** — `derive_claude_project_dir(vault_config.vault_path, ...)` is recomputed for every task/goal although its inputs are constant per vault; derive it once per vault request and pass it in.

### run 1 — vault-ui#39 — src/vault_ui/static/app.js:1135

**`src/vault_ui/static/app.js:1135`** — `escapeHtml` doesn't escape double quotes, so the `title="Last activity: …"` attribute could break out if the value ever contained `"`. Safe today (server-generated ISO datetime) and matches the pre-existing in-attribute pattern, but a dedicated attribute encoder would harden it. *(defense-in-depth only)*

### run 1 — vault-ui#39 — src/vault_ui/static/app.js:1117

**`src/vault_ui/static/app.js:1117`** — a future-dated timestamp (server clock ahead of browser clock) yields negative seconds and silently renders `<1m`; `Math.max(0, seconds)` would make the intent explicit.

### run 1 — vault-ui#39 — src/vault_ui/activity.py:44

**`src/vault_ui/activity.py:44-50`** — `session_id` comes from frontmatter (user-controlled markdown) and is interpolated into a filesystem path (`project_dir / f"{session_id}.jsonl"`) and a glob pattern without sanitization. Only `stat().st_mtime` is read — no content, no writes — so impact is minimal in this single-user local tool, but treating frontmatter as a path component is worth an eye. *(defense-in-depth only)*

### run 1 — vault-ui#39 — src/vault_ui/api/tasks.py:705

**`src/vault_ui/api/tasks.py:705-709`, `:1794-1798`** — `compute_activity_date(...)` takes three positional args of three different types (`datetime`, `str | None`, `Path`); keyword args (`modified_date=`, `session_id=`, `project_dir=`) would self-document the calls. *(cosmetic)*
