a72dcc3d1049211e36e41341dc2a39600a6de812613eb4f99f56344964c6235b

## Configuration
- model: opus
- effort: xhigh
- mode: full
- config_hash: a72dcc3d1049211e36e41341dc2a39600a6de812613eb4f99f56344964c6235b
- rules_commands_hash: ecc803331f860b845a6a1b8a103e889bce02520e8cc04ea88102de74c8d5600d
- prs_version: curated-1
- coding version: 0.51.0
- golden baseline coding version: v0.35.6
- golden version: golden-curated-4
- runner_version: 1
- rows skipped: 0
- cost: not recorded — the ledger carries no cost field.

*golden-curated-4* carries 21 `rejected` entries, so precision is measurable: reporting one costs precision. With so few adjudicated, a single hit still moves the number a long way — read it as directional, not calibrated.

No signature embeds a line reference, so a re-report of the same issue at a different line still matches. `recall` measures issue detection.

## Runs
| run | span | PRs | complete | golden in scope | findings | hits | misses | matched rejected | gap candidates | recall | precision | wall time (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-08-19T16:03:13.912632+00:00 … 2026-08-19T16:27:04.701308+00:00 | 5/20 | partial | 56 | 33 | 8 | 45 | 0 | 25 | 0.151 | 1.000 | 1754 |
| 2 | 2026-08-19T18:52:42.101872+00:00 … 2026-08-19T20:20:13.027571+00:00 | 15/20 | partial | 102 | 102 | 12 | 72 | 0 | 90 | 0.143 | 1.000 | 5443 |

## Per-PR
| run | pr_id | golden in scope | hits | misses | findings | gap candidates | dropped items | missing sections | duration (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | tts-mcp#20 | 1 | 1 | 0 | 1 | 0 | 0 | — | 342 |
| 1 | github-pr-review-agent#11 | 16 | 0 | 14 | 7 | 7 | 0 | — | 326 |
| 1 | quant#109 | 10 | 0 | 10 | 4 | 4 | 0 | — | 304 |
| 1 | node-skeleton#2 | 12 | 2 | 10 | 9 | 7 | 0 | — | 458 |
| 1 | python-skeleton#3 | 17 | 5 | 11 | 12 | 7 | 0 | — | 324 |
| 2 | vault-cli#68 | 1 | 0 | 1 | 1 | 1 | 0 | — | 222 |
| 2 | backup#15 | 4 | 0 | 2 | 3 | 3 | 0 | — | 256 |
| 2 | bw#41 | 3 | 1 | 1 | 2 | 1 | 0 | — | 335 |
| 2 | dark-factory#71 | 2 | 0 | 2 | 4 | 4 | 0 | — | 391 |
| 2 | tts-mcp#16 | 6 | 0 | 5 | 6 | 6 | 0 | — | 287 |
| 2 | tts-mcp#3 | 6 | 0 | 5 | 5 | 5 | 0 | — | 304 |
| 2 | distill#6 | 10 | 1 | 6 | 11 | 10 | 0 | — | 485 |
| 2 | vault-ui#39 | 6 | 2 | 4 | 8 | 6 | 0 | — | 364 |
| 2 | recurring-task-creator#30 | 6 | 0 | 4 | 5 | 5 | 0 | — | 431 |
| 2 | tts-mcp#10 | 9 | 2 | 5 | 9 | 7 | 0 | — | 403 |
| 2 | discord-assistant#5 | 13 | 1 | 12 | 12 | 11 | 0 | — | 505 |
| 2 | tts-mcp#13 | 10 | 2 | 7 | 10 | 8 | 0 | — | 368 |
| 2 | github-releaser-agent#8 | 13 | 0 | 9 | 8 | 8 | 0 | — | 401 |
| 2 | discord-assistant#1 | 9 | 1 | 8 | 15 | 14 | 0 | — | 492 |
| 2 | helm#3 | 4 | 2 | 1 | 3 | 1 | 0 | — | 199 |

*Effective fixture: **20 of 40** PRs produced a scored row.* The 20 absent rows are not a zero-finding result — they were rejected before scoring, so they lower confidence without lowering recall.

## Gap-triage candidates
These findings matched no golden entry. They are **not a precision failure** — the golden set is a bootstrap from one strong-model run, and a finding it does not describe is evidence the set is incomplete.

### run 1 — github-pr-review-agent#11 — go.mod:99

**`go.mod:99`** — `make precommit` fails on this branch. `vulncheck` reports `GO-2026-6179` and `GO-2026-6180` against `golang.org/x/mod@v0.37.0` (fix: v0.40.0). The CHANGELOG states the `klauspost/compress` bump exists because "`make vulncheck` failed on master, which blocked the dark-factory preflight baseline" — but the gate is still red, so the stated blocker is not cleared. To be fair to the PR: `x/mod` is unchanged from the base and these two advisories are pre-existing, not a regression introduced here. The bump is a net improvement (3 advisories → 2). It just doesn't finish the job it claims. Either bump `x/mod` in the same PR or amend the CHANGELOG line so it doesn't assert a fix it didn't deliver.

### run 1 — github-pr-review-agent#11 — pkg/skip_post_boundary_test.go:224

**`pkg/skip_post_boundary_test.go:224`** — case 4's negative assertion cannot fail, and its comment states the opposite. `appendVerifyDiagnostic` runs only inside `if verifyResult != nil` (`pkg/steps_review.go:117`), and `callVerifier` returns `nil` when `result.Found` is true (`pkg/steps_review.go:243`) — so a verifier that *ran and succeeded* also appends nothing. I confirmed this empirically with a throwaway test: with a non-nil mock verifier returning `Found: true`, `VerifyReviewCallCount()==1` and `"ai_review verify:"` is still absent. The inline comment "if callVerifier were reached despite the suppressed verifier, it would have appended one" is therefore false. The test's real guard is the `result.Status == AgentStatusDone` assertion two lines up (a reached-but-network-backed verifier would return `Found: false` → `AgentStatusFailed`). Recommend dropping the diagnostics assertion and correcting the comment to name the Status assertion as the signal.

### run 1 — github-pr-review-agent#11 — pkg/factory/runner.go:57

**`pkg/factory/runner.go:57-60`** — `SkipPost` is silently inert on the production path, and the doc comment doesn't say so. `RunAgent` only calls `ResolvePosters` inside `if agent == nil` (`pkg/factory/runner.go:134`), and `main.go:187` always sets `Agent`. So setting `SkipPost` on the Kafka/pod path is a no-op that suppresses nothing. The neighbouring `Agent` field (line 50-53) documents exactly this interaction; `SkipPost`'s comment claims unqualified "suppresses all GitHub write calls". Add the caveat, or move the check somewhere both paths honour.

### run 1 — github-pr-review-agent#11 — README.md:33

**`README.md:33`** — the documented `--skip-post` example cannot run as written. `cmd/run-task` hard-errors at `cmd/run-task/main.go:129` ("GitHub App credentials not configured") before `RunAgent` is ever reached, unless `APP_ID` + `INSTALLATION_ID` + `PEM_KEY_FILE`/`PEM_KEY` are set. Since the flag is advertised for third-party PRs the operator does not own, the still-required App credentials are the surprising part and belong in the example or an adjacent note.

### run 1 — github-pr-review-agent#11 — pkg/factory/runner.go:66

**`pkg/factory/runner.go:66`** — `ResolvePosters` takes the whole `RunConfig` by value but reads only `SkipPost`, `GHToken`, and `CurrentDateTime`. A narrower signature would decouple the helper from the config struct and let the test drop the `RunConfig{...}` fixture.

### run 1 — github-pr-review-agent#11 — pkg/factory/runner_test.go:42

**`pkg/factory/runner_test.go:42`** — `libtime.NewCurrentDateTime()` is the real clock. Harmless here (pure wiring, no time is read), and `factory_test.go:80` already does the same, but `libtime.CurrentDateTimeGetterFunc` (as at `factory_test.go:128`) is the repo's injected-time convention.

### run 1 — github-pr-review-agent#11 — pkg/skip_post_boundary_test.go:104

**`pkg/skip_post_boundary_test.go:104-166`** — case 3 largely duplicates the existing "verification runs and succeeds" case in `steps_review_test.go`, as its own comment acknowledges; the only delta is sourcing the poster from `ResolvePosters`. Could fold into case 4.

### run 1 — quant#109 — task/recurring-schedules/prod/plan-weekend.yaml:65

**`task/recurring-schedules/prod/plan-weekend.yaml:65`** — `due_date: "{{next_sun_date}}"` makes a weekend *plan* due at the end of the weekend it plans. The body says "as the first thing Saturday morning" (line 18) and `priority: 1` (line 63), yet the task is not overdue until Sunday — by which point the Sat+Sun sequencing it produces has no remaining value. `{{current_date}}` is the consistent choice here, unlike the other six `next_sun_date` files (reviews/admin) where a weekend-long window is genuinely correct.

### run 1 — quant#109 — task/recurring-schedules/prod/feed-worms.yaml

**`task/recurring-schedules/prod/feed-worms.yaml`** — fires on `weekdays: [Sunday, Wednesday]` and got no `due_date`, so a Sunday-firing schedule is missed by a change titled "add due_date to weekend recurring schedules". It uses the plural `weekdays` form, which is likely why the sweep skipped it. `due_date: "{{current_date}}"` fits both firing days.

### run 1 — quant#109 — task/recurring-schedules/prod/README.md

**`task/recurring-schedules/prod/README.md`** — the Saturday schedules now split between `{{current_date}}` (infra/automation: opnsense-update, shutdown-k3s, run-update-all-saturday, topic-backup-saturday, turn-on-hell, home-assistant-update-backup) and `{{next_sun_date}}` (review/admin). The split is sensible but undocumented; one line in this 6-line README would keep the next schedule author consistent.

### run 1 — quant#109 — task/recurring-schedules/prod/plan-next-week.yaml:40

**`task/recurring-schedules/prod/plan-next-week.yaml:40`** — the new pre-check bullet asserts `/plan-week` "is gated on both" `reviewed: true` and an empty decision list. `vault-cli decision list --vault personal` is a valid command (verified), but the `/plan-week` command file is not present locally, so the gating claim itself is **unverified**. If the gate is advisory rather than enforced, "is gated on both" will mislead at execution time.

### run 1 — node-skeleton#2 — src/config.ts:59

**`src/config.ts:59`** — `module.exports = config` combined with `export interface` makes the module's runtime value invisible to TypeScript. TS resolves `./config` to its *types only*: a probe importing `cfg.host` fails with `TS2339: Property 'host' does not exist on type 'typeof import(".../config")'`. Consequently every consumer's `const x: T = require('./x.ts')` is an unchecked `any`→`T` assignment, not a verified import. Same shape at `src/log.ts:49`, `src/server.ts:82`, `src/handlers/health.ts:47`. *(rule: `node/make/typecheck-for-typescript`)*

### run 1 — node-skeleton#2 — src/handlers/health.ts:12

**`src/handlers/health.ts:12`** — same defect: `HealthRouter` is never bound to `healthRouter` at `src/handlers/health.ts:27`. `src/config.ts:34` and `src/log.ts:42` *do* bind correctly (`const config: Config`, `const log: Logger`) — these two files are the outliers, so the fix is a consistency change, not a new pattern. *(rule: `node/make/typecheck-for-typescript`)*

### run 1 — node-skeleton#2 — package.json:8

**`package.json:8`** — `"node": ">=22"` is now false. Type stripping was only unflagged in **Node 22.18.0** ([release notes](https://nodejs.org/en/blog/release/v22.18.0), PR #56350); on 22.0–22.17 `node src/index.ts` cannot start at all. `Dockerfile` (`node:22-slim`) and `.github/workflows/ci.yml` (`node-version: '22'`) both float to latest 22.x so they pass today, which is exactly why the wrong floor goes unnoticed. Set `">=22.18"`.

### run 1 — node-skeleton#2 — src/config.ts:47

**`src/config.ts:47`** — `check()` validates `port` and `shutdownTimeoutMs` but not `logLevel`, despite its own docstring ("Collect every problem"). Verified: `LOG_LEVEL=warning` (a plausible typo for `warn`) returns `check() == []` and silently logs at info level — an operator reducing log volume gets the opposite, with no signal. *(rule: `node/config/validate-before-serving`)*

### run 1 — node-skeleton#2 — test/health.test.ts:22

**`test/health.test.ts:22`** — `? address.port : 0` falls back to port `0`, producing `http://127.0.0.1:0` and a confusing connection error rather than a clear failure. Throw instead. (The `listen(0, …)` ephemeral port itself is correct — *(rule: `node/test/ephemeral-port`)* satisfied.)

### run 1 — node-skeleton#2 — eslint.config.js:44

**`eslint.config.js:44-46`** — the comment explains the `erasableSyntaxOnly` gate (enum, namespace, parameter properties) but sits directly above `no-explicit-any` / `no-non-null-assertion`, which have nothing to do with erasability. The rule it describes is in `tsconfig.json`, not here.

### run 1 — node-skeleton#2 — src/index.ts:14

**`src/index.ts:14`** — validation moved from import-time to an explicit entrypoint call. Better error reporting, but a second entrypoint (worker, CLI) must now remember to call `config.check()`; previously requiring config validated automatically. Worth a line in CLAUDE.md since services inherit this.

### run 1 — python-skeleton#3 — .github/workflows/ci.yml:35

**`.github/workflows/ci.yml:35`** — `apt-get install -y trivy` pulls whatever version the repo currently serves, while `Makefile.precommit:31` pins `PIP_AUDIT_VERSION ?= 2.9.0`. One gate is reproducible and the other is not; a new Trivy release with added checks can turn a green branch red with no commit. Pin the trivy version the same way.

### run 1 — python-skeleton#3 — README.md:76

**`README.md:76-94`** — the `## Security gates` section carries tool-selection rationale rather than usage: why pip-audit has no severity threshold, why `S104`/`S101` are suppressed, why `osv-scanner` was rejected, and a three-skeleton comparison table (line 90-94). That is maintainer/agent context. Keep the two-line "what runs and how to trigger it" in README; move the rationale and the cross-skeleton table to `CLAUDE.md` or `docs/security.md`. *(rule: `readme/user-facing-not-agent-context`)*

### run 1 — python-skeleton#3 — pyproject.toml:64

**`pyproject.toml:64-68`** — `S104` is ignored globally, so a genuinely wrong `0.0.0.0` bind anywhere in `src/` is also unflagged. The justification given (containerised service) applies only to the config default, so a `per-file-ignores` entry for that one module would keep the rule live everywhere else.

### run 1 — python-skeleton#3 — Makefile.precommit:47

**`Makefile.precommit:47`** — `uv export` runs without `--frozen`/`--locked`, and `audit: sync` calls plain `uv sync`, so the audit can silently re-resolve and rewrite `uv.lock` rather than failing on drift. Adding `--frozen` would make the gate assert the lock instead of following it.

### run 1 — python-skeleton#3 — .github/workflows/ci.yml:12

**`.github/workflows/ci.yml:12`** — `timeout-minutes: 10` now has to cover two apt passes, the Trivy DB pull from ghcr.io (anonymous, rate-limited), and `uvx pip-audit` fetching from PyPI, on top of the existing suite. Worth raising, or caching the Trivy DB.

### run 1 — python-skeleton#3 — Makefile.precommit:24

**`Makefile.precommit:24`** — `check` invokes `trivy` with no preflight; a contributor without the binary gets a bare `command not found` from make rather than an install hint.

### run 1 — python-skeleton#3 — README.md:92

**`README.md:92`** — the go-skeleton and node-skeleton gate lists are cross-repo claims that cannot be verified from this repo and will drift as those skeletons change.

### run 2 — vault-cli#68 — CHANGELOG.md:13

**`CHANGELOG.md:13`** — the `fix:` bullet is a ~130-word unbroken run packing four separate things: the Go error string, the root-cause history ("injected fleet-wide by an old version of the `updater` tool"), a verification note ("`go mod tidy` after removal is zero-churn"), and a self-referential compliance claim ("no longer violates its own `docs/dod.md`"). Every other entry in the file — including the three `v0.104.0` bullets directly below it — is a single 15–30 word clause. `changelog-guide.md` has no hard length RULE, so this is a convention deviation rather than a violation, but the audit-trail detail belongs in the commit body, not the user-facing release note. Suggested trim: `- fix: Remove the stale `exclude cloud.google.com/go v0.26.0` directive from go.mod — it broke `go install github.com/bborbe/vault-cli@latest``

### run 2 — backup#15 — go.mod:74

**`go.mod:74`** — `make precommit` fails at the `vulncheck` target: `golang.org/x/mod@v0.37.0` carries two vulnerabilities not in the allowlist (`GO-2026-6179`, `GO-2026-6180`, both fixed in `v0.40.0`). **Pre-existing and unrelated to this diff**, but `.github/workflows/ci.yml` gates on `make precommit`, so this PR's CI will be red for a reason the PR did not cause. Everything else in precommit passed (format, goimports, golines, generate, tests).

### run 2 — backup#15 — Dockerfile:8

**`Dockerfile:8`** / **`Makefile:6`** — the default registry URL is hardcoded in two independent places. `make build` always passes `--build-arg`, so the Dockerfile default only applies to a bare `docker build .`; if the host moves again and only one is updated, Makefile-driven and direct builds silently diverge. Either drop the `ARG` default or document the Makefile as the single source.

### run 2 — backup#15 — Dockerfile:11

**`Dockerfile:11`** — `make build NPM_REGISTRY=` passes `--build-arg NPM_REGISTRY=`, which overrides the `ARG` default with an empty string, leaving `npm set registry` with no value. It fails loudly rather than silently, so this is low-impact, but a `${NPM_REGISTRY:-https://registry.npmjs.org}` fallback would make the override total-function.

### run 2 — bw#41 — groups/meta/k3s.py:2

**`groups/meta/k3s.py:2-4`** — the fix is scoped by node *name* (`r'.*k3s.*'`) but k3s membership in this repo is actually driven by the `k3s/enabled` metadata key, and the two sets do not match. `hm.hell` has `k3s.enabled: true` (server mode, the standalone K3s cluster) yet its name does not contain `k3s`, so it is not a member of this group. Verified: `bw metadata hm.hell -k sysctl` returns only `net.ipv4.ip_forward` and `vm.swappiness` — none of the `gc_thresh` values. That node runs the same one-veth-per-pod workload the comment describes and stays on the 128/512/1024 defaults, so it remains exposed to exactly the failure this PR fixes. (The pattern does not over-match — all 13 name-matching nodes genuinely run k3s.) Idiomatic fix, already the dominant convention in this repo: move the `sysctl` block into `bundles/k3s/metadata.py` as a `@metadata_reactor.provides('sysctl/options')` gated on `metadata.get('k3s', {}).get('enabled', False)`. That file already contains an `iptables` reactor with precisely this gate, and `bundles/netplan`, `bundles/kvm-guest`, `bundles/kvm-host`, and `bundles/intellij` all supply `sysctl/options` the same way. It would cover `hm.hell` and any future k3s node regardless of hostname.

### run 2 — dark-factory#71 — CHANGELOG.md:15

**`CHANGELOG.md:15`** — the bullet promises find/replace pairs "for each **Blocker**/Critical issue", but `prompt-auditor` has no Blocker tier. Its severity vocabulary is exactly two words, `Critical` and `Recommendation` (sections `## Critical Issues` at `agents/prompt-auditor.md:493`, `## Recommendations` at `:498`); the only "blocker" string in the file is unrelated prose at `:349` about an execution-report field. A reader looking for a Blocker category in the report won't find one. Replace with "for each Critical Issue".

### run 2 — dark-factory#71 — agents/scenario-auditor.md:173

**`agents/scenario-auditor.md:173`**, **`agents/spec-auditor.md:601`** *(outside the diff — scope remark)* — both carry the identical `NEVER modify files - audit only` constraint at line 17 and both still end with `1. **Implement fixes** - Apply critical issues and top recommendations`. The bug being fixed here exists verbatim in two of the three auditors; fixing one leaves the same broken offer live for spec and scenario audits.

### run 2 — dark-factory#71 — agents/prompt-auditor.md:509

**`agents/prompt-auditor.md:509-519`** — the new `<final_step>` restates what the template at `:493-500` already says (find/replace pairs for Critical, when-obvious for Recommendations). Two copies of one instruction drift apart on the next edit. A one-line `<final_step>` pointing at the template ("the report is the deliverable — do not offer to apply fixes") would carry the same weight.

### run 2 — dark-factory#71 — agents/prompt-auditor.md:509

**`agents/prompt-auditor.md:509-519`** — the rewrite dropped all three old options, including `2. **Verify references** - Deep-dive into code reference accuracy`. That one was read-only and never conflicted with the audit-only constraint; only option 1 and 3 did. Worth keeping as an offered follow-up.

### run 2 — tts-mcp#16 — CHANGELOG.md:16

**`CHANGELOG.md:16`** — bullet under `### Changed` is prefixed `feat:` but describes a docs/policy consolidation (moving rationale out of `~/.claude/CLAUDE.md` into the skill), not a new capability. The sibling bullet at `:17` describing the same off-by-default shift correctly uses `docs:`, and v0.4.0 set the precedent of `docs:` for playbook-language changes. Re-prefix to `docs:`, or merge into `:17`. Impact is prefix accuracy, not a wrong release — the `### Added` narrate bullet at `:12` already forces the minor bump either way. *(rule: `changelog/conventional-prefix-required`)*

### run 2 — tts-mcp#16 — skills/voice/SKILL.md:87

**`skills/voice/SKILL.md:87`** — "Anything at all when the user is clearly sitting there watching it scroll by" sits under the hard heading **"Never speak, in any mode:"** (`:81`), which contradicts `narrate`'s contract at `:15` and `:72` ("a spoken gist of **every** substantive answer"). Before this diff the bullet lived under a softer "Do **not** speak:" list with no answer-speaking mode to collide with; the diff both absolutised the prohibition and added the mode it excludes. Claude in `narrate` mode now has two opposed instructions and no way to observe whether the user is at the screen. Scope the bullet per mode, e.g. "in `on` / `interview`, skip signals the user is watching happen; `narrate` speaks the gist regardless — the user chose it deliberately."

### run 2 — tts-mcp#16 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** and **`skills/voice/SKILL.md:8`** — assert as fact that "no always-on rule competes with it" / "there is no competing always-on rule in `~/.claude/CLAUDE.md`". That file is user-owned and not shipped by this repo; any existing install still carrying the old "Voice Attention Only … `ryan`" rule keeps exactly the two-sources-of-truth problem this PR removes, while the skill states otherwise. Phrase as an instruction rather than a claim: "this skill owns spoken-output volume — if `~/.claude/CLAUDE.md` still carries an always-on voice rule, remove it."

### run 2 — tts-mcp#16 — skills/voice/SKILL.md:14

**`skills/voice/SKILL.md:14`** — `on` (default) reads ambiguously against `:8` ("voice is off until someone invokes `/voice`"). Disambiguate to "default when `/voice` is invoked with no arg".

### run 2 — tts-mcp#16 — skills/voice/SKILL.md:71

**`skills/voice/SKILL.md:71-73`** — the "Every question" column mixes a boolean ❌ with the qualifier "decisions only", which skims as though `on` answers some questions. Either drop the qualifier and let `:76-79` carry it, or write `❌ (decision points only)`.

### run 2 — tts-mcp#16 — skills/voice/SKILL.md:94

**`skills/voice/SKILL.md:94`** — the escape hatch for reinstating a persistent default recreates the precise conflict the rest of the diff eliminates. It self-caveats, but add "not recommended" so a future editor doesn't casually re-add the rule.

### run 2 — tts-mcp#3 — skills/voice/SKILL.md:32

**`skills/voice/SKILL.md:32-33`** — The refactor makes `restart` and `/tts-mcp:voice-test` mutually recursive with no explicit terminator. Restart step 3 calls `/tts-mcp:voice-test`; step 4 says "follow the troubleshooting steps in `/tts-mcp:voice-test`" — and that command's troubleshooting list ends by calling `/tts-mcp:voice restart` (`commands/voice-test.md:22`), which re-enters step 3. Each lap costs a real ~15–20s `launchctl kickstart` + model reload. The pre-change version was acyclic: the old restart step 4 fell through to the inline handshake, whose last step was *"report the specific failure and fall back to on-screen only"* — it never called restart. The restart-from-troubleshooting bullet is new in this PR, so this is a regression the refactor introduced, not a pre-existing loop. `commands/voice-test.md:23` (step 5) is the only guard and it's prose-only, requiring the model to remember it already restarted across a cross-file hop. Fix: make step 5 an explicit one-shot guard ("restart at most once per test run; if this test was invoked *by* `/tts-mcp:voice restart`, skip step 4's restart bullet and go straight to step 5"), and change `SKILL.md:33` to point at voice-test **step 5** rather than the whole troubleshooting list.

### run 2 — tts-mcp#3 — commands/voice-test.md:1

**`commands/voice-test.md:1-3`** — Frontmatter has only `description`; `allowed-tools` is absent. The guide's required-frontmatter template and its DoD checklist both list it, and the rationale is explicit: *"missing `allowed-tools` = unbounded permissions."* The command's tool surface is small and known — `mcp__tts__say`, `mcp__tts__get_voices`, `mcp__tts__get_status`. `argument-hint` is correctly omitted (no `$ARGUMENTS`/`$1` in the body). Fix: add `allowed-tools: [mcp__tts__say, mcp__tts__get_voices, mcp__tts__get_status]`. *(rule: `agent-cmd/command-frontmatter`)*

### run 2 — tts-mcp#3 — commands/voice-test.md:22

**`commands/voice-test.md:22`** — "run `/tts-mcp:voice restart` …, then re-run this test" double-runs the test: `skills/voice/SKILL.md:32` already makes voice-test restart's own step 3. Post-restart the user hears two test utterances. Drop "then re-run this test" — restart verifies itself. (Same wiring as the Important finding above; fixing that one likely resolves this.)

### run 2 — tts-mcp#3 — commands/voice-test.md:5

**`commands/voice-test.md:5,10`** — Unprefixed `/voice on` and `/voice restart`, while line 22 uses the fully-qualified `/tts-mcp:voice restart`. Inconsistent inside one file; the unprefixed form isn't guaranteed to resolve for a user who has another `voice` skill installed. Use `/tts-mcp:voice …` throughout.

### run 2 — tts-mcp#3 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — The appended clause *"Activation does not run a selftest — use /tts-mcp:voice-test to verify the audio path"* is a behavioral disclaimer, not a WHEN-to-activate trigger; the `description` field is Claude's discovery signal. It doesn't harm discovery (the real triggers still lead the sentence) and the line is ~454 chars, well under the limit — but line 22 already states this in more detail, so the frontmatter copy is redundant. Consider dropping it and keeping the description trigger-only. *(rule: `skill-writing/skill-md-frontmatter-required`, guidance tier)*

### run 2 — distill#6 — hooks/deny-generated-file-edits.sh:12

**`hooks/deny-generated-file-edits.sh:12`** — the two unguarded `jq` calls combined with `set -e` make the hook **fail open on its own failure**. Verified: missing `jq` exits 127, malformed stdin JSON exits 5, and neither emits stdout. Claude Code's hook contract treats exit 0 as allow and exit 2 as block; *any other* non-zero shows stderr to the user and **continues with the tool call**. So a machine without `jq` — the one dependency `README.md:149` names — silently loses all protection while appearing installed. Guard both calls and `deny` on failure: `tool=$(jq -r '.tool_name' <<<"$input" 2>/dev/null) || deny "hook: could not parse tool input"`.

### run 2 — distill#6 — hooks/deny-generated-file-edits.sh:34

**`hooks/deny-generated-file-edits.sh:34`** — the banner check is a bare case-insensitive substring grep over the first 2 KB, so the script's own header comment (lines 5–7) trips it and the hook **denies edits to itself**, with a garbled reason harvested from that prose (`source_dir` becomes a lone backtick, `regen` becomes `` ` lines, so the denial message can quote``). Verified directly. This is a self-lockout: once installed, the two bugs above can no longer be fixed via `Edit`/`Write`. Anchor detection on a marker line (e.g. `^[[:space:]]*AUTO-GENERATED by distill`) instead of free text. The other changed files are unaffected today only because the phrase falls outside their first 2 KB.

### run 2 — distill#6 — docs/adopting-distill.md:95

**`docs/adopting-distill.md:95`** — the runbook recommends `--title`, but `pkg/distill/driver.go:315` writes a `Regenerate:` line containing only `--source` and `--output`. An agent following the hook's denial message (which quotes that line verbatim) regenerates without `--title`, silently dropping the H1. Either include `--title` in the emitted banner or warn here that the banner command is incomplete when `--title` is used.

### run 2 — distill#6 — docs/adopting-distill.md:115

**`docs/adopting-distill.md:115`** — "the hook is what makes that mistake impossible rather than merely documented" overstates it. The matcher covers only `Edit`/`Write`/`NotebookEdit`; a `Bash` `sed -i` edit passes through untouched (verified), as do the two gaps above. Soften to "blocks the common path".

### run 2 — distill#6 — hooks/deny-generated-file-edits.sh

**`hooks/deny-generated-file-edits.sh`** — new executable behavior ships with zero tests, and `Makefile:22` (`precommit: ensure format generate test check addlicense`) runs no `shellcheck`, so nothing in CI would have caught any of the three Critical findings. The repo's own `docs/dod.md:20` requires ≥80% coverage for new code. Add a shell test harness feeding the script sample events, plus `shellcheck` to `precommit`.

### run 2 — distill#6 — README.md:134

**`README.md:134-147`** — the `settings.json` snippet is a complete top-level object with no merge guidance; a reader pasting it verbatim clobbers other top-level keys or an existing `hooks.PreToolUse` array. Add a one-line "append to your existing `PreToolUse` array" caveat.

### run 2 — distill#6 — docs/adopting-distill.md:32

**`docs/adopting-distill.md:32`** — `git rev-parse --short HEAD` is offered as the recovery point, but it recovers nothing if the existing `CLAUDE.md` is untracked or has uncommitted edits — precisely the accretion case the doc targets. Recommend `cp CLAUDE.md CLAUDE.md.bak` or an explicit commit instead.

### run 2 — distill#6 — README.md:128

**`README.md:128`** — `cp hooks/deny-generated-file-edits.sh ~/.claude/hooks/` fails on a fresh setup where `~/.claude/hooks/` does not exist. Prepend `mkdir -p ~/.claude/hooks`.

### run 2 — distill#6 — CHANGELOG.md:7

**`CHANGELOG.md:7`** — `feat(hooks):` carries a scope, which the rule's exact-prefix regex `^- ([a-z]+:)` does not match. *(rule: `changelog/conventional-prefix-required`)* Low severity in practice: the bump detector is judgment-based ("starts with `feat:` **or otherwise describes a new additive capability**"), and this repo already ships scoped prefixes in released sections (`CHANGELOG.md:13`, `CHANGELOG.md:21`). Worth resolving as a convention question rather than treating the entry as broken.

### run 2 — distill#6 — README.md:119

**`README.md:119-152`** — at ~34 lines this is the second-largest README section, for an optional companion feature. The repo already uses a one-line pointer for `docs/adopting-distill.md` (`README.md:166`); the same shape would fit here.

### run 2 — vault-ui#39 — src/vault_ui/static/style.css:947

**`src/vault_ui/static/style.css:947`** — `color: #64748b` on the card background `#3a3a3a` (`.task-card`, which `.goal-card` inherits) is a **2.39:1** contrast ratio — below WCAG AA's 4.5:1, and below even the 3:1 large-text floor, at `0.7rem` (~11px). The comment labels it `slate-500`, a Tailwind token from a *light*-theme scale, dropped into a dark UI. Every neighbour in the same footer is more readable: `.assignee-badge` `#96999e` = 3.98:1, `.priority-chip` `#cbd5e1` on `#334155` ≫ 4.5:1. The stylesheet already has a dominant muted grey — `#9ca3af`, used 11 times — which measures **4.48:1** here. Switch to `#9ca3af` and update the assertion in `tests/test_activity.py:168` that currently pins `#64748b`. Note the file comment says the badge "should lose against the badges beside it", but `#64748b` is *darker* than `#9ca3af`, so the chosen value works against the stated intent as well as against legibility.

### run 2 — vault-ui#39 — src/vault_ui/activity.py:59

**`src/vault_ui/activity.py:59-61`** — the glob fallback scans the entire `~/.claude/projects/*/` tree, and it runs once per card inside `async def _process_vault` / `_process_goal_vault` (`src/vault_ui/api/tasks.py:606`, `:793`), which are `asyncio.gather`-ed per vault. It is synchronous filesystem I/O on the event loop, unmemoized, repeated on every 60s poll (`app.js:20`) and every WebSocket-triggered reload, for every connected browser. Cost scales as O(cards × project-dirs) and the miss path — cloud/container sessions, or any transcript that has been archived — is the *permanent* path for those cards, never a one-off. Measured on this machine (50 project dirs, 927 transcripts): **0.166 ms per miss, ≈6.6 ms of blocking loop time for a 40-card board**. Small today, but it grows with both factors and there is no cache. Build the `filename → mtime` index once per request (or memoize with a short TTL) instead of re-scanning per card; `derive_claude_project_dir(...)` is also recomputed per card when it is constant per vault.

### run 2 — vault-ui#39 — src/vault_ui/static/app.js:1135

**`src/vault_ui/static/app.js:1135`** — the hover title interpolates the raw wire value, so it renders as `Last activity: 2026-08-09T08:58:32.417382+00:00` — microseconds and a UTC offset, not the viewer's local time. The CHANGELOG promises "the full timestamp on hover"; `new Date(iso).toLocaleString()` delivers that.

### run 2 — vault-ui#39 — src/vault_ui/activity.py:53

**`src/vault_ui/activity.py:53-61`** — `session_id` is interpolated straight into a path (`project_dir / filename`) and a glob pattern (`root.glob(f"*/{filename}")`) with no shape validation. A frontmatter value like `../../../etc/hosts` walks out of the projects tree, and one containing `*` or `?` turns the fallback into a wildcard match. Impact is limited (it only ever leaks an mtime, and the value is written by the vault owner), but the repo already has an `is_uuid()` helper used at `src/vault_ui/api/tasks.py:1728` — gating on it here would cost one line.

### run 2 — vault-ui#39 — src/vault_ui/static/app.js:1129

**`src/vault_ui/static/app.js:1129`** — weeks is the terminal unit, so a year-old task renders `52w` and a two-year-old one `104w`. The CHANGELOG's examples stop at `3w`. Capping at a months/years unit would keep the badge glanceable.

### run 2 — vault-ui#39 — src/vault_ui/static/app.js:1135

**`src/vault_ui/static/app.js:1135`** — `escapeHtml` (line 1484) uses `textContent`/`innerHTML`, which escapes `&`, `<`, `>` but **not** `"`, and the value is interpolated into a double-quoted `title="…"` attribute. Not exploitable for this field — `activity_date` is a Pydantic `datetime` and cannot contain a quote — and it matches the pre-existing pattern used throughout the file. Flagged only because the same helper does guard genuinely user-controlled values elsewhere (e.g. `goal.assignee` at line 1261).

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token_test.go

**`pkg/publisher/period_token_test.go`** — no test entry uses a positive `PeriodOffset`, but positive offsets were broken by the identical bug and are valid config. Verified: `2026-01-31` with `offset: +1` produced `2026-02-31` → normalized to `2026-03-03` → token `"2026-03"` instead of `"2026-02"`. The CEL guard at `pkg/k8s_connector_schema.go:80` constrains `periodOffset` by recurrence kind only, not by sign. `publisher_test.go` does have `+1` entries, but only on the 1st/15th — none discriminate old vs. fixed. Add e.g. `Entry(..., schedule.NewDate(2026, time.January, 31), 1, "2026-02")`. *(rule: `tdd/failing-test-before-implementation`)*

### run 2 — recurring-task-creator#30 — CHANGELOG.md:14

**`CHANGELOG.md:14`** — `fix(deps):` is not in the allowed prefix set (`feat:`/`fix:`/`refactor:`/`test:`/`docs:`/`chore:`/`perf:`); the enforcement regex `^- ([a-z]+:)` rejects the inserted scope. Practical impact is low — it still resolves to a patch bump, and `chore(deps):` already has precedent at `CHANGELOG.md:61` — but commit `3f42ce6` existed specifically to fix this bullet's prefix and landed on the non-canonical variant. Drop the scope. *(rule: `changelog/conventional-prefix-required`)*

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token_test.go:68

**`pkg/publisher/period_token_test.go:68-73`** — the `2026-08-31` entry sits under the `// Discriminating entries — old code produces wrong token on these` comment at line 52, but it is a control entry: old code computed Aug 31 − 1mo = Jul 31, a valid date with no overflow, yielding the correct `2026-07`. Its own label ("July has 31 days but August target does too") is also muddled — July is the target, August is the fire month. Move it up to the control block at line 36. The categorization is the only thing telling a future reader which entries actually guard the regression.

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token.go:105

**`pkg/publisher/period_token.go:105,109,113`** — the same clamp is written out three times. `github.com/bborbe/time` v1.27.6 (the pinned version) ships `BeginningOfMonth(t)` implemented byte-for-byte identically; `libtime` is already imported in `pkg/tick`, `pkg/factory`, `pkg/handler`, and is not on `pkg/publisher`'s forbidden-import list. `libtime.BeginningOfMonth(base)` collapses all three.

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token.go:104

**`pkg/publisher/period_token.go:104-115`** — no comment records *why* the day-1 anchor exists. Without the `AddDate` normalization footgun spelled out, the clamp reads as arbitrary and is a plausible "simplification" target in a future refactor.

### run 2 — tts-mcp#10 — CHANGELOG.md:16

**`CHANGELOG.md:16`** — The counts do not reconcile. "clearing 6 of 8 npm-audit findings" is followed by a breakdown enumerating 2 high + 4 moderate + 2 low = **8** cleared, and line 17 then adds 2 remaining, totalling 10 against a stated base of 8. The "2 high" bucket also names a single package. The same numbers are duplicated verbatim in the commit body, so a CHANGELOG-only fix leaves the commit message wrong.

### run 2 — tts-mcp#10 — Makefile.precommit:30

**`Makefile.precommit:30`** — Changing the contract of `check` left the docs that enumerate it stale: `README.md:306` and `CLAUDE.md:46` both still say `make check` runs "lint + typecheck", `README.md:312`/`CLAUDE.md:51` describe CI as Python-only, and `README.md:80` lists "**Node.js 18+** — For the MCP server (optional)" when Node is now mandatory for `make check`/`make precommit` and CI pins 22.

### run 2 — tts-mcp#10 — Makefile.precommit:26

**`Makefile.precommit:26`** — `npx tsc --noEmit` resolves to `node_modules/.bin/tsc` today (verified), but `npx` silently downloads an arbitrary TypeScript from the registry if the local binary is ever absent (`NODE_ENV=production`, `npm ci --omit=dev`). That is the silent-fallback pattern `CLAUDE.md` forbids; `npm exec --no-install -- tsc --noEmit` or `./node_modules/.bin/tsc --noEmit` fails fast instead.

### run 2 — tts-mcp#10 — .github/workflows/ci.yml:36

**`.github/workflows/ci.yml:36-39`** — No npm caching. `cache: npm` + `cache-dependency-path: mcp/package-lock.json` is one line. Low value in practice — `npm ci` measured 2s locally.

### run 2 — tts-mcp#10 — .github/workflows/ci.yml:33

**`.github/workflows/ci.yml:33-35`** — The comment's causal claim is inverted: the `test` check was blind to `mcp/` because `mcp-typecheck` did not exist, not because a Node step was missing. Pinning Node is still worth doing; the stated reason just isn't the one.

### run 2 — tts-mcp#10 — Makefile.precommit:30

**`Makefile.precommit:30`** — Type-checking is the only `mcp/` gate; there is no lint or test coverage for `tts-mcp.ts`. Adding ESLint to a single 250-line relay is arguably scope creep for this PR — but if that's deliberate, saying so in the Makefile comment would stop it reading as an oversight. *(rule: `node/make/required-targets`)*

### run 2 — tts-mcp#10 — CHANGELOG.md:14

**`CHANGELOG.md:14`** — `### Fixed` is inserted above `### Changed`; Keep a Changelog orders Added → Changed → Deprecated → Removed → Fixed → Security.

### run 2 — discord-assistant#5 — config.example.yaml:65

**`config.example.yaml:65`** — `voice.wake_phrases` is read **only by the Python shim**; the Node bot has no YAML reader at all (no `js-yaml`, `readFileSync`, or config-file path anywhere in `src/` or `package.json` — it reads `SHIM_WAKE_PHRASES` from the environment and nothing else). Every other key in this file is shim-only, so this is the first setting that both processes need, and the file's own header promises "environment > this file > default". Set it here without also exporting `SHIM_WAKE_PHRASES` and the two gates desync. In the direction that matters — bot says addressed, shim says not — the shim returns empty content, nothing arrives to clear `answering`, and `speak()` refuses typed turns as `busy` until `TYPING_MAX_MS` (`src/voice.js:56`, 5 min). That is precisely the bug commit `d0b0695` exists to fix, reintroduced through the config surface this PR adds. Either drop `wake_phrases` from this file, or state in-line that it must *also* be exported to the bot's environment.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:110

**`shim/claude_openai_shim.py:110`** — `setting()`'s boolean branch treats **any unrecognized value as `True`**, so a typo silently enables a flag. Verified: `SHIM_UNSAFE=treu`, `=maybe`, `=2` all return `True`, which drops `ALLOWED_TOOLS` entirely (`shim/claude_openai_shim.py:1161,1571`) for a bot the repo's own `CLAUDE.md` describes as reaching "a session with vault and repository access". The JS counterpart this line's new comment claims parity with (`src/config.js:15`) does it correctly — explicit true-list, explicit false-list, otherwise fall back. The permissive default predates this PR, but the line is rewritten here and the parity claim is new, and `CLAUDE.md`'s "fails closed … never default it to permissive" principle applies to the tool allowlist as much as to `ALLOWED_USER_IDS`.

### run 2 — discord-assistant#5 — config.example.yaml:65

**`config.example.yaml:65`** / **`local.env.example:62`** / **`src/config.js:142`** / **`shim/claude_openai_shim.py:799`** — the default phrase list ships `hey but`, which is an ordinary English sentence opener, not a wake word. Verified identical on both sides: `"hey but what about the deploy"`, `"hey but i thought we agreed"`, `"hey but did bob reply"` all return **addressed**. The comma'd form `"Hey, but …"` correctly does not match — but STT output is frequently unpunctuated, which is exactly the case where it collapses to the matching form. This contradicts the PR's own stated asymmetry ("a missed trigger costs one repeat while a false one interrupts a room"): `hey bought` is a plausible mishearing, `hey but` is a phrase people say to each other.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:110

**`shim/claude_openai_shim.py:110`** — `setting()` and `flag()` strip whitespace and quotes in **opposite order** (Python: whitespace → quotes; JS: quotes → whitespace), so they still disagree wherever whitespace surrounds the quotes, despite the comment's "agrees … on every spelling". Verified with fallback `false`: `' "1" '` → Python `True`, JS `false`; likewise `'"true"\n'`. A trailing newline on a quoted value is a normal shape for k8s env-from-file and here-doc-sourced vars. No boolean is currently read by both processes, which is the only reason this isn't biting today — normalize both to strip-to-fixed-point.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py

**`shim/claude_openai_shim.py`** (whole file) — the entire Python side has **zero automated coverage**. `make test` is `node --test` (`Makefile.precommit:28`) and no Python test file exists in the repo. This PR adds ~135 lines of Python — `is_addressed`, `strip_wake_phrase`, `_WAKE_RE`, the `setting()` boolean branch, the `_PANEL` anchor-line addition, and `strip_panels` on the bridge path — none of it exercised by CI. The JS mirror *is* tested, and the two are required to agree; a parity test over a shared case table is exactly what would have caught the strip-order and `hey but` issues above.

### run 2 — discord-assistant#5 — src/config.js:30

**`src/config.js:30`** — `WAKE_LEAD_WORDS` is a hand-maintained copy, but the shim derives its list programmatically (`sorted(_FILLER_WORDS | {"hey"})`, `shim/claude_openai_shim.py:821`). So editing `_FILLER_WORDS` updates Python automatically and drifts JS silently, with nothing asserting the two match. The comment's claim that drift "is cosmetic … never a wrong answer" holds in only one direction: if JS accepts a lead word the shim doesn't, the bot arms `answering` for a turn the shim answers with silence — the 5-minute typed-path wedge again, not a cosmetic mismatch.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:810

**`shim/claude_openai_shim.py:810-820`** — the comment lists `a "so," lead-in` among the three live failures motivating the skip-set, then eight lines later states only noise may precede the phrase and never a real word. The behavior is correct and deliberate — `"So, hey bot, what's my task"` → `false`, asserted at `test/voice.test.js:223` and `:572` — but the comment reads as if that case was fixed. Drop it from the failure list.

### run 2 — discord-assistant#5 — src/config.js:30

**`src/config.js:30`** — describing the skippable set as noise-only undersells it: `right`, `alright`, `okay`, `yeah`, `oh` are discourse markers, not disfluencies, so `"right, hey bot"` and `"alright hey bot go"` both count as addressed (verified). Fine as a design choice, worth naming accurately.

### run 2 — discord-assistant#5 — test/voice.test.js:227

**`test/voice.test.js:227-250`** — busting `require.cache` for `../src/config` per iteration leaves the registry pointing at a fresh instance while the file's top-level binding and `src/voice.js` both still hold the original. It works today only because nothing else re-requires config; any later test that does will silently read a different object than `Session` mutates.

### run 2 — discord-assistant#5 — src/voice.js:319

**`src/voice.js:319-341`** — `log.info('voice: interrupt-on-speech', …)` fires unconditionally right after `ws.send`, before any server acknowledgement, so a rejected `session.update` still logs as if applied. Mild, since `case 'error'` at `src/voice.js:632` would surface the rejection separately — but that's the same optimistic-logging shape that let commit `c93b7c5`'s invalid payload go unnoticed "on every connect".

### run 2 — discord-assistant#5 — src/config.js:170

**`src/config.js:170`** — `config.isAddressed` puts a second, larger domain predicate on the config object, compounding the `isAllowed` deviation `CLAUDE.md` already flags, and rebuilds a `RegExp` per phrase per call. A small `src/wake-phrase.js` taking `wakePhrases` as an argument would also make it testable without cache-busting.

### run 2 — tts-mcp#13 — src/server.py:353

**`src/server.py:353`** — `instruct` is accepted into the work queue without ever being validated against the engine, so `POST /say {"instruct": …}` against a `voxtral` server returns `202 Accepted`. `voice` is validated synchronously eight lines up (`src/server.py:333` → 400); `instruct` is not. The MCP `say` tool relays that 202 straight back, so a calling agent is told "queued" for a request the engine will refuse. This contradicts the README (`the voxtral engine fails the request rather than ignoring it`) and the repo's fail-fast rule in `CLAUDE.md`. Fix: reject at the boundary, mirroring the `voice` check — the engine is already reachable as `state.engine`, so a `supports_instruct` property (or `validate_request(voice, instruct)`) on the `TTSEngine` protocol closes it cleanly.

### run 2 — tts-mcp#13 — tests/test_engine.py

**`tests/test_engine.py`** — the engine seam is well covered (construction, config validation, speaker discovery, per-family dispatch, streaming wiring, cross-family rejection), but the two defects above sit in the gaps: no test drives `POST /say` with `instruct` against a `VoxtralEngine` state, and no test asserts the terminal status after a streaming generation failure. A test asserting `status == "error"` after a failed streaming item would have caught the masking.

### run 2 — tts-mcp#13 — tests/test_main.py:25

**`tests/test_main.py:25`** — `engine` was added to the base CLI config fixture, but there is no negative test for the new required key on either entry path: missing `engine`, non-string `engine`, unknown engine value, or `language` supplied alongside `voxtral` through `_parse_server_config` (`src/server.py:693`). `build_engine` itself is tested for all four; the config-parsing layer that feeds it is not.

### run 2 — tts-mcp#13 — mcp/tts-mcp.ts:224

**`mcp/tts-mcp.ts:224`** — `console.error` for request logging. *(rule: `node/logging/structured-not-console`)* Consistent with the two adjacent pre-existing calls at lines 236 and 250, so this is convention-following, not new drift — flagging only because this line is in the diff. Worth resolving repo-wide or suppressing the rule for this relay deliberately, not piecemeal.

### run 2 — tts-mcp#13 — src/tts/generate.py:36

**`src/tts/generate.py:36-90`** — `generate_chunks` takes `streaming_interval` and forwards it into a `stream=False` engine call where it is unused. The docstring says so explicitly, which is honest, but it puts a dead parameter in the public signature. Splitting `TTSEngine.generate` into `generate` / `generate_stream` would drop both the unused argument and the `stream: bool` branch inside each engine.

### run 2 — tts-mcp#13 — src/server.py:118

**`src/server.py:118-133`** — the `ServerState` private-fields refactor (commit `59f9fe8`) is ~90 lines of read-only property boilerplate unrelated to pluggable engines, landing in the same PR. It also lands half-applied: `status_lock` became `_status_lock` + property, while the `statuses` dict it guards, plus `work_queue`, `ready_queue`, and `model`, stay public and mutable. The mutable ones are load-bearing (the worker assigns `state.model`), so the split is defensible — but the encapsulation now reads as inconsistent rather than intentional.

### run 2 — tts-mcp#13 — CHANGELOG.md:8

**`CHANGELOG.md:8-27`** — bullets under `## Unreleased` use Keep a Changelog prose under `Added`/`Changed`/`Removed` rather than conventional prefixes. *(rule: `changelog/conventional-prefix-required`)* Reporting for traceability only — the file declares Keep a Changelog in its own header and every released section follows that form, so the repo convention should win. Consider suppressing this rule for the repo so it stops surfacing.

### run 2 — tts-mcp#13 — src/tts/engine.py:263

**`src/tts/engine.py:263`** — `cast(CustomVoiceModel, model)` in `Qwen3Engine.generate` is unchecked. Both callers run `validate_model` first, so it is safe today; a `getattr` guard at the cast site would make the invariant local rather than dependent on call order.

### run 2 — github-releaser-agent#8 — pkg/githubtags/tags.go:33

**`pkg/githubtags/tags.go:33-48`** — the `TagsFetcher` interface doc lost two contracts that have no replacement anywhere: `Implementations MUST be safe for concurrent use.` was deleted outright, and `LatestSemverTag`'s `ErrNoTags` contract moved to the `httpTagsFetcher` method. Since `httpTagsFetcher` is unexported, its method docs never reach public godoc — so callers programming against the interface can no longer see either the concurrency requirement or that `ErrNoTags` is the snapshot-fallback signal. Restore both as a method doc on `LatestSemverTag` inside the interface, mirroring the new `CommitSHAForTag` block. *(rule: `go-doc/exported-item-documented`)*

### run 2 — github-releaser-agent#8 — pkg/githubtags/tags.go:33

**`pkg/githubtags/tags.go:33`** — summary line still reads "resolves a remote GitHub repo's highest-semver tag"; the interface now also resolves tag → commit SHA. *(rule: `go-doc/exported-item-documented`)*

### run 2 — github-releaser-agent#8 — pkg/githubtags/tags.go:175

**`pkg/githubtags/tags.go:175-178`** — `fetchPage`'s named returns `tags`/`nextURL` are now dead: after the refactor every exit is an explicit `return`, nothing assigns to them. Drop the names or keep only `err`.

### run 2 — github-releaser-agent#8 — pkg/githubtags/tags.go:132

**`pkg/githubtags/tags.go:132`** — the `collectTags` pagination loop (up to 100 iterations of network I/O) has no `ctx.Done()` check. Low impact in practice: `fetchPage` builds each request with `http.NewRequestWithContext`, so a cancelled ctx fails the next call immediately rather than doing wasted work. *(rule: `go-functional-composition/list-checks-ctx-done`)*

### run 2 — github-releaser-agent#8 — pkg/steps_planning.go:750

**`pkg/steps_planning.go:750`** — `handleValidationFailure` takes six adjacent `string` params (`owner, name, ref, effectiveVersion, reason, snapshotVersion`). The call site at `pkg/steps_planning.go:193` is correct, but a transposition of `effectiveVersion`/`reason` would compile and silently flip the tag consulted. A small struct (as already done for `escalation`) removes the class.

### run 2 — github-releaser-agent#8 — pkg/steps_planning_test.go:2616

**`pkg/steps_planning_test.go:2616`** — the `"6 chars below bound"` entry uses `"765fc0"`, which is not a prefix of `7657fc0af1…` either, so it passes for the wrong reason and does not isolate `minRefMatchLen`. `"7657fc"` (a genuine 6-char prefix) would actually pin the bound. The end-to-end bound *is* covered by the `ref: "a"` planning test, so this is precision, not a coverage hole.

### run 2 — github-releaser-agent#8 — pkg/steps_planning_test.go:2612

**`pkg/steps_planning_test.go:2612`** — entry labelled `"case-insensitive lowercase"` passes an uppercase ref (`"7657FC0"`); the label reads backwards.

### run 2 — github-releaser-agent#8 — pkg/steps_planning.go:38

**`pkg/steps_planning.go:38`** — `releaseTagVerdictEscalate` and `PlanOutcomeNothingToRelease` (`pkg/plan_output.go:110`) are untyped string constants used as an enum. Consistent with the pre-existing `PlanOutcome*` block, so fixing it means touching the whole set, not this PR alone. *(rule: `go-enum-type/typed-constants-with-collection`)*

### run 2 — discord-assistant#1 — src/index.js:276

**`src/index.js:276`** — `gatewayReady = false` has no path back to `true` unless a *real* shard event fires. `gatewayReady` is only re-set by `shardResume`/`shardReady`/`clientReady` (lines 109–114, 75). If the swallowed exception did not actually come from the gateway, none of those ever fire: the process sits alive, `/healthz` 200, `/readiness` 503, forever. `k8s/discord-assistant-deploy.yaml:42-46` points liveness at `/healthz` with no dependencies (deliberately), so k8s never restarts it — this is precisely the "alive, passing liveness, and permanently disconnected" state the design comment at 254–258 calls *worse than exiting*. The `connectedOnce` gate closes that hole pre-login and the swallow reopens it post-login. *(rule: `node/lifecycle/crash-on-unhandled-rejection`)*

### run 2 — discord-assistant#1 — src/index.js:260

**`src/index.js:260`** — three of the six patterns can *only* match non-gateway errors. `@discordjs/ws` treats `ECONNRESET`/`ECONNREFUSED`/`ETIMEDOUT`/`EAI_AGAIN` as `KnownNetworkErrorCodes` and swallows them internally during the handshake (`node_modules/@discordjs/ws/dist/index.js:570,1066-1072`) — they set `failedToConnectDueToNetworkError` and return without re-emitting, so they never reach `uncaughtException` via the gateway. Meanwhile this process has three other network clients that *do* produce them: `src/llm.js:19` (`fetch` to `config.baseUrl`), `src/voice.js:248` (a second `ws` socket to speech-to-speech), `src/status.js:26` (`net.Socket`). The arms that can fire are exactly the ones that must not flip `gatewayReady`. `src/voice.js:242` makes this reachable: `removeAllListeners()` strips the old socket's `error` handler while its handshake may still be pending. *(rule: `node/lifecycle/crash-on-unhandled-rejection`)*

### run 2 — discord-assistant#1 — src/index.js:277

**`src/index.js:277`** — the survived path returns without exiting, so `scripts/supervise.sh` cannot help: the supervisor only acts on process *exit*. If discord.js does not in fact reconnect (the throw escaped its state machine), the PR has converted "crash + nothing restarts it" into "hang + nothing restarts it" — same silent outage, minus the crash that made it visible. Needs a watchdog: if `gatewayReady` is still false N seconds after a survived fault, `process.exit(1)` and let the supervisor/k8s do their job. *(rule: `node/lifecycle/crash-on-unhandled-rejection`)*

### run 2 — discord-assistant#1 — src/index.js:272

**`src/index.js:272`** — discord.js v14.27 has a supported hook for this exact fault class: `client.on('shardError', (error, shardId) => …)` (`WebSocketManager.js:297-304`), fed by `WebSocketShard.onError()`. It is scoped to the gateway, carries `shardId`, and does not break when upstream error text changes. A `process.on('uncaughtException')` regex on `err.message` is both fragile and unscoped. Note `client.on('error')` is *not* the right event — `Events.Error` only fires from the `ShardingManager` IPC layer, never in a single-process bot.

### run 2 — discord-assistant#1 — src/index.js:234

**`src/index.js:234`** — `unhandledRejection` still exits 1 unconditionally. For the gateway this is correct and intentional (`client.login()` at 283 has no `.catch()`, so a pre-connect handshake failure exits by design). But a post-login rejection from the LLM or s2s path still kills a bot that now tolerates the synchronous equivalent — the tolerance is inconsistent across the two paths.

### run 2 — discord-assistant#1 — src/index.js:259

**`src/index.js:259-277`** — new classification logic is inline against module-level mutable bindings, not exported, not tested. `test/` covers `config`/`health`/`llm`; nothing asserts "given `connectedOnce=true` and message X, survive and flip readiness". Extract to a pure `classify(connectedOnce, message) → {survive}` and unit-test it under `node --test`. *(rule: `node/architecture/inject-dependencies`)*

### run 2 — discord-assistant#1 — scripts/supervise.sh:21

**`scripts/supervise.sh:21`** — `stop()` sends SIGTERM and `exit 0`s without `wait "$child"`. `src/index.js:210-226` deliberately leaves voice channels and waits ~600ms before destroying the client, precisely to avoid leaving a ghost participant that a later process must evict. The supervisor returns to `make`/`dev.sh` before that completes, undermining it.

### run 2 — discord-assistant#1 — scripts/supervise.sh:50

**`scripts/supervise.sh:50-51`** — `delay` only grows; nothing resets it to `DELAY_MIN` after a healthy run. A bot that runs clean for days then hits one transient crash resumes backoff wherever the last streak left it (up to 60s). Reset on a start-timestamp check before `wait`.

### run 2 — discord-assistant#1 — Makefile:33

**`Makefile:33`** — `DISCORD_TOKEN` is resolved from TeamVault once, before the supervisor starts. A revoked/rotated token makes node exit 1 on every restart with the same stale value, forever, capped at 60s — the supervisor's own comment names "token revoked" as a case backoff handles, but backoff only throttles it, never gives up. `exit 2` covers only `config.check()` failures.

### run 2 — discord-assistant#1 — src/index.js:263

**`src/index.js:263-265`** — second `client.once('clientReady')` registration is safe (`triggerClientReady()` is guarded one-shot by `Status.Ready`), but `connectedOnce = true` folds cleanly into the existing handler at line 75.

### run 2 — discord-assistant#1 — scripts/supervise.sh:51

**`scripts/supervise.sh:51`** — `[ … ] && delay=…` as the loop's last statement works only because `-e` is absent; `if/then/fi` is `-e`-safe.

### run 2 — discord-assistant#1 — scripts/supervise.sh:27

**`scripts/supervise.sh:27-28`** — narrow race: a signal between `"$@" &` and `child=$!` orphans the just-spawned child.

### run 2 — discord-assistant#1 — scripts/supervise.sh:10

**`scripts/supervise.sh:10`** — `pipefail` is inert; there is no pipeline in the script.

### run 2 — discord-assistant#1 — CHANGELOG.md:5

**`CHANGELOG.md:5`** — no conventional prefix (`fix:`/`feat:`). Consistent with every other entry in the file, so house style rather than regression, but dark-factory bump detection reads these. *(rule: `changelog/conventional-prefix-required`)*

### run 2 — helm#3 — charts/github-pr-reviewer/Chart.yaml:9

**`charts/github-pr-reviewer/Chart.yaml:9`** — `0.1.0 → 0.2.0` (MINOR) diverges from this repo's only precedent for the same shape of change: cd6e677 bumped `github-dark-factory` `0.1.0 → 0.1.1` (PATCH) for a values-defaults-only edit. Not wrong per semver — a changed default is arguably a feature — but it makes this the sole chart outside the `0.1.x` band. Either is fine; pick one and be consistent.
