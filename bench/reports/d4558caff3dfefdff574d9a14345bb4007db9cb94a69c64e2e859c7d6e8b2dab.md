d4558caff3dfefdff574d9a14345bb4007db9cb94a69c64e2e859c7d6e8b2dab

## Configuration
- model: opus
- effort: xhigh
- mode: full
- config_hash: d4558caff3dfefdff574d9a14345bb4007db9cb94a69c64e2e859c7d6e8b2dab
- rules_commands_hash: ecc803331f860b845a6a1b8a103e889bce02520e8cc04ea88102de74c8d5600d
- prs_version: curated-1
- coding version: 0.45.0
- golden baseline coding version: v0.35.6
- golden version: golden-curated-3
- runner_version: 1
- rows skipped: 0
- cost: not recorded — the ledger carries no cost field.

*golden-curated-3* carries 20 `rejected` entries, so precision is measurable: reporting one costs precision. With so few adjudicated, a single hit still moves the number a long way — read it as directional, not calibrated.

No signature embeds a line reference, so a re-report of the same issue at a different line still matches. `recall` measures issue detection.

## Runs
| run | span | PRs | complete | golden in scope | findings | hits | misses | matched rejected | gap candidates | recall | precision | wall time (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-08-09T20:54:31.750374+00:00 … 2026-08-09T23:20:57.512212+00:00 | 20/20 | complete | 158 | 133 | 114 | 24 | 19 | 0 | 0.826 | 0.857 | 8967 |
| 2 | 2026-08-10T14:29:34.932356+00:00 … 2026-08-12T11:12:44.846706+00:00 | 20/20 | complete | 158 | 148 | 24 | 114 | 0 | 124 | 0.174 | 1.000 | 8675 |
| 3 | 2026-08-12T10:26:55.009865+00:00 … 2026-08-12T12:27:46.663498+00:00 | 19/20 | partial | 156 | 143 | 22 | 114 | 0 | 121 | 0.162 | 1.000 | 7148 |

## Per-PR
| run | pr_id | golden in scope | hits | misses | findings | gap candidates | dropped items | missing sections | duration (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | tts-mcp#20 | 1 | 1 | 0 | 1 | 0 | 0 | — | 204 |
| 1 | github-pr-review-agent#11 | 16 | 8 | 6 | 9 | 0 | 0 | — | 1005 |
| 1 | quant#109 | 10 | 6 | 4 | 6 | 0 | 0 | — | 249 |
| 1 | node-skeleton#2 | 12 | 6 | 6 | 6 | 0 | 0 | — | 640 |
| 1 | python-skeleton#3 | 17 | 11 | 5 | 12 | 0 | 0 | — | 546 |
| 1 | vault-cli#68 | 1 | 1 | 0 | 1 | 0 | 0 | — | 367 |
| 1 | backup#15 | 4 | 2 | 0 | 4 | 0 | 0 | — | 270 |
| 1 | bw#41 | 3 | 2 | 0 | 3 | 0 | 0 | — | 309 |
| 1 | dark-factory#71 | 2 | 2 | 0 | 2 | 0 | 0 | — | 381 |
| 1 | tts-mcp#16 | 6 | 5 | 0 | 6 | 0 | 0 | — | 385 |
| 1 | tts-mcp#3 | 6 | 5 | 0 | 6 | 0 | 0 | — | 374 |
| 1 | distill#6 | 10 | 7 | 0 | 10 | 0 | 0 | — | 450 |
| 1 | vault-ui#39 | 6 | 6 | 0 | 6 | 0 | 0 | — | 342 |
| 1 | recurring-task-creator#30 | 6 | 3 | 1 | 5 | 0 | 0 | — | 400 |
| 1 | tts-mcp#10 | 9 | 7 | 0 | 9 | 0 | 0 | — | 392 |
| 1 | discord-assistant#5 | 13 | 12 | 1 | 12 | 0 | 0 | — | 440 |
| 1 | tts-mcp#13 | 10 | 9 | 1 | 9 | 0 | 0 | — | 299 |
| 1 | github-releaser-agent#8 | 13 | 9 | 0 | 13 | 0 | 0 | — | 1175 |
| 1 | discord-assistant#1 | 9 | 9 | 0 | 9 | 0 | 0 | — | 556 |
| 1 | helm#3 | 4 | 3 | 0 | 4 | 0 | 0 | — | 184 |
| 2 | tts-mcp#20 | 1 | 1 | 0 | 2 | 1 | 0 | — | 217 |
| 2 | github-pr-review-agent#11 | 16 | 4 | 10 | 12 | 8 | 0 | — | 641 |
| 2 | quant#109 | 10 | 2 | 8 | 5 | 3 | 0 | — | 232 |
| 2 | node-skeleton#2 | 12 | 2 | 10 | 12 | 10 | 0 | — | 544 |
| 2 | python-skeleton#3 | 17 | 4 | 12 | 8 | 4 | 0 | — | 325 |
| 2 | vault-cli#68 | 1 | 0 | 1 | 0 | 0 | 0 | — | 285 |
| 2 | backup#15 | 4 | 0 | 2 | 3 | 3 | 0 | — | 278 |
| 2 | bw#41 | 3 | 1 | 1 | 3 | 2 | 0 | — | 361 |
| 2 | tts-mcp#16 | 6 | 0 | 5 | 3 | 3 | 0 | — | 545 |
| 2 | tts-mcp#3 | 6 | 0 | 5 | 6 | 6 | 0 | — | 372 |
| 2 | distill#6 | 10 | 3 | 4 | 11 | 8 | 0 | — | 732 |
| 2 | vault-ui#39 | 6 | 0 | 6 | 9 | 9 | 1 | — | 444 |
| 2 | recurring-task-creator#30 | 6 | 1 | 3 | 8 | 7 | 0 | — | 473 |
| 2 | tts-mcp#10 | 9 | 1 | 6 | 11 | 10 | 0 | — | 552 |
| 2 | discord-assistant#5 | 13 | 1 | 12 | 13 | 12 | 0 | — | 620 |
| 2 | tts-mcp#13 | 10 | 2 | 8 | 16 | 14 | 0 | — | 455 |
| 2 | github-releaser-agent#8 | 13 | 0 | 9 | 8 | 8 | 0 | — | 534 |
| 2 | discord-assistant#1 | 9 | 0 | 9 | 9 | 9 | 0 | — | 575 |
| 2 | helm#3 | 4 | 2 | 1 | 5 | 3 | 0 | — | 205 |
| 2 | dark-factory#71 | 2 | 0 | 2 | 4 | 4 | 0 | — | 283 |
| 3 | tts-mcp#20 | 1 | 0 | 1 | 1 | 1 | 0 | — | 221 |
| 3 | github-pr-review-agent#11 | 16 | 2 | 12 | 10 | 8 | 0 | — | 317 |
| 3 | quant#109 | 10 | 2 | 8 | 8 | 6 | 0 | — | 254 |
| 3 | node-skeleton#2 | 12 | 1 | 11 | 11 | 10 | 0 | — | 591 |
| 3 | python-skeleton#3 | 17 | 3 | 13 | 8 | 5 | 0 | — | 401 |
| 3 | vault-cli#68 | 1 | 0 | 1 | 1 | 1 | 0 | — | 324 |
| 3 | backup#15 | 4 | 0 | 2 | 4 | 4 | 0 | — | 345 |
| 3 | bw#41 | 3 | 0 | 2 | 4 | 4 | 4 | — | 296 |
| 3 | tts-mcp#16 | 6 | 0 | 5 | 7 | 7 | 0 | — | 393 |
| 3 | tts-mcp#3 | 6 | 1 | 4 | 7 | 6 | 0 | — | 302 |
| 3 | distill#6 | 10 | 2 | 5 | 7 | 5 | 0 | — | 315 |
| 3 | vault-ui#39 | 6 | 0 | 6 | 8 | 8 | 0 | — | 533 |
| 3 | recurring-task-creator#30 | 6 | 0 | 4 | 6 | 6 | 0 | — | 425 |
| 3 | tts-mcp#10 | 9 | 3 | 4 | 7 | 4 | 0 | — | 338 |
| 3 | discord-assistant#5 | 13 | 1 | 12 | 10 | 9 | 0 | — | 435 |
| 3 | tts-mcp#13 | 10 | 3 | 7 | 17 | 14 | 0 | — | 422 |
| 3 | github-releaser-agent#8 | 13 | 1 | 8 | 11 | 10 | 0 | — | 661 |
| 3 | discord-assistant#1 | 9 | 1 | 8 | 11 | 10 | 0 | — | 393 |
| 3 | helm#3 | 4 | 2 | 1 | 5 | 3 | 0 | — | 182 |

*Effective fixture: **59 of 60** PRs produced a scored row.* The 1 absent row is not a zero-finding result — they were rejected before scoring, so they lower confidence without lowering recall. **5** findings carried no matching key and never reached scoring.

## Gap-triage candidates
These findings matched no golden entry. They are **not a precision failure** — the golden set is a bootstrap from one strong-model run, and a finding it does not describe is evidence the set is incomplete.

### run 2 — tts-mcp#20 — mcp/package-lock.json

**`mcp/package-lock.json`** — commit message `Bump fast-uri from 3.1.4 to 3.1.5 in /mcp` is the Dependabot default and omits the security rationale (GHSA-7p8r-x3mc-p8w7). The repo's prior dep-bump commit documented advisory IDs, verification method, and what was deliberately left unfixed.

### run 2 — github-pr-review-agent#11 — pkg/factory/runner.go:66

**`pkg/factory/runner.go:66-73`** — `ResolvePosters` places a boot-time conditional inside `pkg/factory`, contradicting this repo's own stated invariant ("Factory functions are pure composition — no conditionals, no I/O") and the package doc at `pkg/factory/factory.go:5-9` ("zero business logic"). The branch is wiring *policy*, not composition. Fix: delete the helper and inline the branch in `RunAgent` (an orchestrator, where conditionals are already idiomatic — see lines 105-131), keeping `Create*` functions unconditional. *(rule: `go-factory/no-conditional-in-body`, `go-factory/no-impl-in-factory-pkg`)*

### run 2 — github-pr-review-agent#11 — pkg/factory/runner.go:57

**`pkg/factory/runner.go:57-60`** — the `SkipPost` doc says "suppresses all GitHub write calls" unconditionally, but the effect only materialises when `RunConfig.Agent == nil` (the call site at line 133-136 is inside `if agent == nil`). Production `main.go` sets `Agent` via `CreateAgentProvider`, so `SkipPost: true` there is silently inert — a fail-open trap for any future caller. Fix: scope the comment to "only honoured when `Agent` is nil; `CreateAgentProvider`-built agents ignore it," or thread the flag through `CreateAgentProvider` too.

### run 2 — github-pr-review-agent#11 — pkg/skip_post_boundary_test.go:224

**`pkg/skip_post_boundary_test.go:224`** — case 4's only load-bearing assertion is a non-discriminator. `appendVerifyDiagnostic` (`pkg/steps_review.go:119-126`) runs *only* when `callVerifier` returns non-nil, i.e. only on verification **failure** — a verifier that ran and *succeeded* also leaves `## Diagnostics` free of `"ai_review verify:"`. The comment at lines 220-221 ("if callVerifier were reached despite the suppressed verifier, it would have appended one") is therefore false, and the test would pass with the guard deleted. Fix: assert on a real mock verifier's `VerifyReviewCallCount() == 0`, which does distinguish the two states. *(rule: `test-pyramid/push-down-when-unsure`)*

### run 2 — github-pr-review-agent#11 — pkg/skip_post_boundary_test.go:102

**`pkg/skip_post_boundary_test.go:102-167`** — case 3 does not exercise the contract its comment claims. It discards the `ResolvePosters` verifier (`poster, _ :=` at line 124) and substitutes `&mocks.ReviewVerifier{}`; and on the `pass`-verdict path `tryDismissHallucinated` is never called, so the nil poster is never touched either. What remains duplicates the pre-existing "verification runs and succeeds" case at `pkg/steps_review_test.go:298-321` — same fixture shape, same `VerifyResult`, same two assertions. Fix: delete, or rewrite to pass the verifier from `ResolvePosters`. *(rule: `test-pyramid/push-down-when-unsure`)*

### run 2 — github-pr-review-agent#11 — pkg/factory/runner_test.go:37

**`pkg/factory/runner_test.go:37-45`** — the `SkipPost: false` case passes `CurrentDateTime: libtime.NewCurrentDateTime()`, but the real `cmd/run-task` wiring never sets that field (verified: `CurrentDateTime` appears in prod `main.go:188` and nowhere in `cmd/run-task/main.go`). The fixture is more populated than production, so it cannot surface the nil-getter path it nominally covers.

### run 2 — github-pr-review-agent#11 — cmd/run-task/main.go:76

**`cmd/run-task/main.go:76`** — `default:"false"` restates the bool zero value and is the only `default:` tag in the struct; the other 20+ fields omit it.

### run 2 — github-pr-review-agent#11 — CHANGELOG.md:10

**`CHANGELOG.md:10`** — "which blocked the dark-factory preflight baseline" is internal tooling detail in a user-facing Keep-a-Changelog file; the GO-2026-5841 half is correctly specific.

### run 2 — github-pr-review-agent#11 — prompts/cancelled/001-run-task-skip-post-flag.md

**`prompts/cancelled/001-run-task-skip-post-flag.md`** — number `001` is reused by `prompts/completed/001-run-task-skip-post-flag.md`; the two differ only in frontmatter status. Ambiguous for any tooling that keys on the number.

### run 2 — quant#109 — task/recurring-schedules/prod/weekly-review.yaml:64

**`task/recurring-schedules/prod/weekly-review.yaml:64`** — same Sunday due date as `plan-next-week.yaml:67`, but this PR newly makes the two ordered: `plan-next-week.yaml:40` adds a pre-check requiring the previous week's note to have `reviewed: true`, which `/complete-week` produces — and `/complete-week` is a step inside `weekly-review` (line 39). Prerequisite and dependent now share one deadline, so the ordering the new pre-check documents isn't encoded anywhere. Consider `{{current_date}}` for `weekly-review` so it lands before `plan-next-week`.

### run 2 — quant#109 — task/recurring-schedules/prod/plan-next-week.yaml:29

**`task/recurring-schedules/prod/plan-next-week.yaml:29`** — "Month link present (target may not exist yet if the monthly plan is deferred)" is a success criterion that can pass with a dangling link. Reads as intentional (relaxing the old "Linked to current month's plan"), but a checkbox that permits a broken wikilink will not catch the case where the month link is simply wrong rather than deferred.

### run 2 — quant#109 — task/recurring-schedules/prod/plan-next-week.yaml:40

**`task/recurring-schedules/prod/plan-next-week.yaml:40`** — this pre-check restates a gate the diff says `/plan-week` already enforces ("`/plan-week` is gated on both"). Useful as a heads-up, but it will drift silently if the command's gating changes.

### run 2 — node-skeleton#2 — src/server.ts:31

**`src/server.ts:31`** — `CreateApp` is declared but never applied to the implementation at `src/server.ts:40`, so `tsc` never checks that the published type matches the function. Verified: making `options` required while leaving the alias optional gives **`tsc --noEmit` exit 0** and a runtime `Cannot destructure property 'isReady' of 'undefined'`. Fix verified to close it: `export type CreateApp = typeof createApp;` (re-running the same drift then fails typecheck with 4 errors, and is clean without drift).

### run 2 — node-skeleton#2 — src/handlers/health.ts:12

**`src/handlers/health.ts:12`** — same pattern: `HealthRouter` is never applied to `healthRouter` at `src/handlers/health.ts:27`. Same fix.

### run 2 — node-skeleton#2 — src/index.ts:8

**`src/index.ts:8-10`** — the `const x: T = require('./y.ts')` idiom used across `src/log.ts:5`, `src/server.ts:6-9`, `src/handlers/health.ts:5-6`, `test/health.test.ts:5-7` is an *unchecked* assertion, not a verified type — `require()` erases to `any`. Verified: rebinding `const log: Logger = require('./config.ts')` typechecks green and would crash on first `log.error`. `Config` and `Logger` are safe only because their producers (`src/config.ts:34`, `src/log.ts:42`) annotate the literal; the consumer side is unverified everywhere. Worth an explicit comment in the template, since this is the one place the conversion's safety claim doesn't hold.

### run 2 — node-skeleton#2 — src/log.ts:24

**`src/log.ts:24-25`** — `LEVELS_BY_NAME[config.logLevel] ?? LEVELS.info` reads through the prototype chain. Verified with `LOG_LEVEL=constructor`: `threshold` becomes a *function*, `??` doesn't fall back, and `LEVELS['debug'] > threshold` is `false` — **every level emits, including debug, in production**. The added comment at `src/log.ts:21-23` claims `noUncheckedIndexedAccess` resolves this; it doesn't, for inherited keys. `k8s/skeleton-deploy.yaml:33` pins `info`, so this needs an operator typo to trigger. Fix: validate `logLevel` against the four levels in `config.check()`, or use `Object.hasOwn`.

### run 2 — node-skeleton#2 — README.md:20

**`README.md:20`** — Quick start never states the minimum Node version for the sources to run, nor that there is no build step / no `dist/`. Both are new user-facing facts created by this PR; `CLAUDE.md:11` documents them for agents only. Fix: one Requirements line under Quick start.

### run 2 — node-skeleton#2 — eslint.config.js:44

**`eslint.config.js:44-46`** — the comment explaining the `erasableSyntaxOnly` pairing sits directly above `no-explicit-any` and `no-non-null-assertion`, which have nothing to do with erasable syntax. Move it above the `no-require-imports` block or the tsconfig reference.

### run 2 — node-skeleton#2 — test/health.test.ts:22

**`test/health.test.ts:22`** — falling back to port `0` when `server.address()` isn't an `AddressInfo` turns a listen failure into a confusing connection error. Throw instead.

### run 2 — node-skeleton#2 — src/config.ts:52

**`src/config.ts:52`** — `check()` accepts `SHUTDOWN_TIMEOUT_MS=0`, which forces immediate exit and defeats the drain path; it also doesn't enforce the documented `< terminationGracePeriodSeconds` invariant.

### run 2 — node-skeleton#2 — CHANGELOG.md:13

**`CHANGELOG.md:13`** — "startup-validated config (`config.check()`) instead of a type assertion" mischaracterizes the old code, which threw at module load (`src/config.js`, deleted) rather than asserting. Also, the new README License section isn't covered by any bullet.

### run 2 — node-skeleton#2 — README.md:42

**`README.md:42`** — the `make typecheck` row's "the only step that validates types" rationale duplicates the `CLAUDE.md:11` invariant; sibling rows stay descriptive.

### run 2 — python-skeleton#3 — .github/workflows/ci.yml:33

**`.github/workflows/ci.yml:33`** — `$(lsb_release -sc)` resolves to `noble` on `ubuntu-latest`, but the Aqua deb repo consolidated every release under a single `generic` suite. `deb …/trivy-repo/deb noble main` 404s on `InRelease`, so `apt-get update` errors and `apt-get install -y trivy` then fails — the job dies before `make precommit`. Upstream also moved the host to `get.trivy.dev`. Fix: `deb [signed-by=/usr/share/keyrings/trivy.gpg] https://get.trivy.dev/deb generic main`, or drop the hand-rolled block for `aquasecurity/setup-trivy@v0.2.x`. This never surfaced because `on:` restricts CI to `master`/`main` (`ci.yml:4-7`), so no run on this branch exercised the new step.

### run 2 — python-skeleton#3 — pyproject.toml:64

**`pyproject.toml:64-68`** — `S104` is suppressed globally, yet only two call sites need it (`src/skeleton/server.py:9`, `src/skeleton/__main__.py:33`). The file already demonstrates the narrower mechanism two lines below for `S101`; scoping S104 via `per-file-ignores` keeps the check live for code added later. The stated reason ("Overridable via the HOST env var") is accurate — verified at `__main__.py:33` and `example.env:2`.

### run 2 — python-skeleton#3 — .github/workflows/ci.yml:33

**`.github/workflows/ci.yml:33`** — `tee -a` appends to `trivy.list`; plain `tee` is idempotent if the step is ever re-run in a reused workspace.

### run 2 — python-skeleton#3 — README.md:82

**`README.md:82`** — "a third advisory database over ground pip-audit and trivy already cover" reads as a typo; likely "over ground that pip-audit and trivy already cover".

### run 2 — backup#15 — Dockerfile:8

**`Dockerfile:8`** — the default registry host is duplicated with `Makefile:6`. The exact failure this PR fixes (host decommissioned → every image build 404s) now has two places that must be updated in lockstep, and the Dockerfile's copy is the one nobody exercises since `make build` always passes `--build-arg`. It also leaves a bare `docker build .` failing off the nuke network, which contradicts the PR's own stated goal ("target … public npm without editing the Dockerfile"). Fix: `ARG NPM_REGISTRY=https://registry.npmjs.org` and let `Makefile:6` remain the single source for the verdaccio default.

### run 2 — backup#15 — Dockerfile:13

**`Dockerfile:13`** — `npm install --verbose --include=optional` now runs against a caller-overridable registry. The line itself is unchanged, but the risk is introduced by this diff: `npm install` rewrites `frontend/package-lock.json` on mismatch instead of failing, so a build with a different `NPM_REGISTRY` can silently resolve versions the lockfile does not pin. Fix: `npm ci --include=optional` so a registry/lockfile mismatch fails loudly.

### run 2 — backup#15 — Makefile:127

**`Makefile:127`** — the `docker build` line carries no `BUILD_GIT_VERSION` / `BUILD_GIT_COMMIT` / `BUILD_DATE` build args, and the Go stage has no matching `-ldflags "-X"` wiring. Pre-existing, not introduced here; noted because this diff is what touched the line. *(rule: `go-build-args/three-args-required`)*

### run 2 — bw#41 — groups/meta/k3s.py:2

**`groups/meta/k3s.py:2-4`** — Coverage gap: membership is keyed on the node *name* (`r'.*k3s.*'`), not on `k3s.enabled`. `hm.hell` has `'k3s': {'enabled': True}` (`nodes/hm.hell.py:66`) but its name doesn't match the pattern, so it does not get the fix. Verified empirically: ``` bw metadata hm.hell -k sysctl → {net.ipv4.ip_forward, vm.swappiness} # no gc_thresh bw metadata hm.nuke-k3s-prod-0 -k sysctl → …gc_thresh1/2/3 present # ok ``` hell runs pods, so it has the same one-veth-per-pod pressure that overflowed the global `arp_tbl` on `nuke-k3s-prod-0` — it is silently left on the 128/512/1024 defaults. Every other sysctl provider in this repo gates on the bundle's own flag (`netplan`, `kvm-guest`, `kvm-host`, `intellij` all use `metadata.get('<x>', {}).get('enabled', False)`). Moving this block into `bundles/k3s/metadata.py` as a `@metadata_reactor.provides('sysctl/options')` gated on `k3s.enabled` covers hell automatically and matches the established idiom.

### run 2 — bw#41 — bundles/intellij/metadata.py:28

**`bundles/intellij/metadata.py:28-29`** — Unrelated whitespace-only change in an incident-fix PR. It is its own commit (`de4f030 fmt`), so the cost is low, but it widens the diff of a change that will likely be cherry-picked or referenced later.

### run 2 — tts-mcp#16 — skills/voice/SKILL.md:8

**`skills/voice/SKILL.md:8`** — states as fact "there is no competing always-on rule in `~/.claude/CLAUDE.md`". That file lives outside this repo and differs on every install, so the skill cannot know this. For a user who *does* have such a rule, the skill asserts a falsehood — and Claude, reading the skill, would trust the assertion over the actual memory file. That is the same silent-outranking bug this PR sets out to kill, pointed the other way. Line 94 already has the correct framing ("If you want a persistent default back … then this file stops being the single source of truth"); line 8 should match it. Fix: claim ownership, not absence — e.g. *"This skill owns spoken-output volume. If a rule in `~/.claude/CLAUDE.md` also sets it, that rule wins and this file is no longer the single source of truth."* The frontmatter clause at **`skills/voice/SKILL.md:3`** ("Sole authority on spoken-output volume — no always-on rule competes with it") needs the same softening. *(rule: `agent-cmd/single-source-of-truth`)*

### run 2 — tts-mcp#16 — CHANGELOG.md:16

**`CHANGELOG.md:16`** — `feat:` prefix is semantically off. The bullet describes consolidating existing rules into one file plus fixing a precedence conflict; that reads as `fix:` or `refactor:`, not a new capability (the actual new capability is the `narrate` bullet at line 12). Net impact is nil here — line 12's genuine `feat:` already forces the minor bump — and `v0.4.0:23` sets precedent for `feat:` under `### Changed`, so this is a consistency nit rather than a bump hazard. *(rule: `changelog/conventional-prefix-required`)*

### run 2 — tts-mcp#16 — skills/voice/SKILL.md:69

**`skills/voice/SKILL.md:69-74`** — `narrate` and `interview` are mutually exclusive by construction (each is defined as "`on` plus X"), but they address the same user: away from the screen. Someone driving by voice who wants both spoken answers *and* every question read aloud has no way to ask for it. Worth either documenting the exclusivity deliberately or allowing `/voice narrate interview`.

### run 2 — tts-mcp#3 — commands/voice-test.md:22

**`commands/voice-test.md:22`** — introduces a circular procedure with `skills/voice/SKILL.md:32-33`: restart step 3 says "Verify: run `/tts-mcp:voice-test`", and voice-test's last troubleshooting bullet says "Still silent → run `/tts-mcp:voice restart` … then re-run this test". SKILL.md:33 makes it explicit — the *already-restarted-and-still-broken* branch routes into a procedure whose terminal step is another restart. There's no attempt counter, and per SKILL.md:35 each restart drops in-flight messages for **every** Claude session sharing the server. The pre-PR text terminated cleanly (old handshake troubleshooting ended at "report the failure, fall back to on-screen only" — it never restarted). Fix: make voice-test's restart bullet conditional ("only if you have not already restarted this session; otherwise go to step 5"), or have SKILL.md:33 point at voice-test steps 4a–4c only.

### run 2 — tts-mcp#3 — commands/voice-test.md:1

**`commands/voice-test.md:1-3`** — frontmatter declares only `description`; `allowed-tools` is missing, leaving the command's tool permissions unbounded. The tool surface is small and known: `mcp__tts__say`, `mcp__tts__get_voices`, `mcp__tts__get_status`. `argument-hint` is correctly omitted (no arguments). *(rule: `agent-cmd/command-frontmatter`)*

### run 2 — tts-mcp#3 — skills/voice/SKILL.md:20

**`skills/voice/SKILL.md:20-24`** — the PR's own mitigation isn't wired into the path that needs it. `interview` is defined at SKILL.md:13 as the mode "used when the user is away from the keyboard", and `commands/voice-test.md:11` names "the user is about to walk away and rely on voice alerts" as a reason to run the test — yet activation now just flips the mode and nothing prompts the user toward `/tts-mcp:voice-test`. The one-line confirmation at SKILL.md:18 doesn't mention it either. Dropping the gate for `on` is defensible; dropping it for `interview` reinstates precisely the "walks away trusting voice, never gets alerted" failure the deleted section called the worst case. Fix: have `interview` either run the test or append a suggestion to its confirmation line.

### run 2 — tts-mcp#3 — CHANGELOG.md:12

**`CHANGELOG.md:12`** — `## Unreleased` bullets carry no conventional prefix (`feat:` / `docs:` / …). *(rule: `changelog/conventional-prefix-required`)* Low priority and arguably correct as-is: the file's preamble declares Keep a Changelog, and the released `## v0.1.0` section uses the same `### Added` / `### Changed` + prose-bullet shape. `.maintainer.yaml` has `autoRelease: true`, but per CHANGELOG:29 the releaser cuts from the `## Unreleased` block rather than parsing prefixes. Changing this PR alone would make the file internally inconsistent — migrate wholesale in a follow-up or leave it.

### run 2 — tts-mcp#3 — commands/voice-test.md:5

**`commands/voice-test.md:5`** — inconsistent command naming within one file: `/voice on` here and `/voice restart` at line 10, versus the prefixed `/tts-mcp:voice restart` at line 22.

### run 2 — tts-mcp#3 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — `description` is now 453 chars; the appended sentence is a caveat about what activation *doesn't* do, not an activation trigger, so it adds match-surface without adding discovery value. Body sections 20–24 already say it.

### run 2 — distill#6 — hooks/deny-generated-file-edits.sh:6

**`hooks/deny-generated-file-edits.sh:6`** — The hook denies edits to *itself*. Its own header comment contains the literal string `"AUTO-GENERATED by distill"` at byte 255, well inside the 2048-byte window that line 34 greps, so `grep -qi 'AUTO-GENERATED by distill'` matches. Verified empirically: feeding `{"tool_name":"Edit","tool_input":{"file_path":".../hooks/deny-generated-file-edits.sh"}}` into the script returns `permissionDecision: "deny"`. Once installed per `README.md:128`, no agent can edit the hook — including to fix this — and the denial reason falsely asserts the script "is AUTO-GENERATED by distill". Same trap applies to the installed copy at `~/.claude/hooks/`. Fix: anchor detection structurally instead of on a bare substring — require line 1 to be `<!--` and match the full banner line `AUTO-GENERATED by distill — do not edit by hand.` within the leading comment block (neither line 6 nor line 39 of the hook contains `do not edit by hand`, so that alone breaks the self-match).

### run 2 — distill#6 — hooks/deny-generated-file-edits.sh

**`hooks/deny-generated-file-edits.sh`** — No automated coverage for a newly shipped executable artifact. `make precommit` (`Makefile:33`) runs Go tooling only — no `shellcheck`, and `scenarios/` contains only `001-compile-anti-injection.md`. Both functional bugs above (self-lock, `NotebookEdit` no-op) are one-fixture tests away from being caught. Run `shellcheck` manually today: **clean, zero findings**.

### run 2 — distill#6 — README.md:123

**`README.md:123`** — The 2 KB detection window leaves a thin margin against self-locking distill's own docs. Measured first-match byte offsets: `README.md` 2486 (only ~440 bytes below the cap), `docs/adopting-distill.md` 3662, `docs/spec.md` 4164. Adding a paragraph above `README.md:68` would make distill's own README un-editable by any agent. Same root cause as the Must Fix.

### run 2 — distill#6 — hooks/deny-generated-file-edits.sh:37

**`hooks/deny-generated-file-edits.sh:37`** — `regen` is captured as `.+` from the target file and interpolated verbatim into `permissionDecisionReason` at line 46, i.e. attacker-influenced text lands in agent-visible instruction context. Low reachability (needs a hostile file already in the repo), but `README.md:149` claims the hook is "safe to enable globally" — either sanitize the capture or scope the claim.

### run 2 — distill#6 — README.md:127

**`README.md:127-130`** — Install snippet omits `mkdir -p ~/.claude/hooks`; on a fresh Claude Code install `cp … ~/.claude/hooks/` fails. `docs/adopting-distill.md:110` also drops the `chmod +x` that README includes (harmless — `cp` preserves the committed `0755` mode — but the two snippets differ for no reason).

### run 2 — distill#6 — docs/adopting-distill.md:5

**`docs/adopting-distill.md:5,97,107`** — Three cross-references name a README section (`§ Source rule format`, `§ Make integration`, `§ Edit protection hook`) but link to bare `../README.md` with no anchor, landing the reader at the top of a 170-line file. Add `#source-rule-format`, `#make-integration`, `#edit-protection-hook`.

### run 2 — distill#6 — CHANGELOG.md:7

**`CHANGELOG.md:7`** — `feat(hooks):` uses a scoped Conventional-Commits prefix, which `docs/changelog-guide.md`'s bare-prefix regex (`feat:`/`fix:`/`docs:`/…) does not recognize, risking a patch bump where a minor was intended. Weak finding: this repo has released precedent for scoped prefixes (`chore(dev):` at lines 13 and 21, shipped in v0.4.1/v0.4.2), so it is more likely an accepted local convention than a defect — maintainer's call.

### run 2 — distill#6 — README.md:166

**`README.md:166`** — A user-facing migration runbook is linked under `## Development` next to contributor-facing `docs/spec.md` / `docs/dod.md`. Follows the pre-existing pattern, so not a regression.

### run 2 — vault-ui#39 — src/vault_ui/activity.py:46

**`src/vault_ui/activity.py:46`** — `claude_session_id` is interpolated straight into a filename (`f"{session_id}.jsonl"`) and then into `project_dir / filename` and `root.glob(f"*/{filename}")` with no validation. Every other consumer of this field in the repo guards it first: `src/vault_ui/cleanup.py:56` rejects ids containing `/` or `\`, and `cleanup.py:59`, `factory.py:92`, `factory.py:140`, `api/tasks.py:1728` all gate on `is_uuid()`. This has two concrete consequences:

### run 2 — vault-ui#39 — factory.py:92

**Correctness — the feature silently no-ops for display-name sessions.** Non-UUID `claude_session_id` values are a first-class concept here: `session_resolver.resolve_session_id()` exists specifically to map a display name (its own docstring example: `"trading-alerts"`) to the real UUID, and `factory.py:92-94` / `cleanup.py:157-158` / `tasks.py:1728-1731` all perform that resolution before touching disk. `activity.py` skips it, so a card with `claude_session_id: trading-alerts` looks for `trading-alerts.jsonl`, which never exists (transcripts are UUID-named), scans the entire projects root, and returns `None`. The card then falls back to file mtime — i.e. it shows "4h" while the agent is actively mid-turn, which is exactly the false-stale case the CHANGELOG says this feature was built to eliminate.

### run 2 — vault-ui#39 — src/vault_ui/activity.py:56

**`src/vault_ui/activity.py:56`** — the `root.glob(f"*/{filename}")` fallback is an unbounded, blocking directory walk executed **once per card**, synchronously, inside an `async def` handler. `src/vault_ui/api/tasks.py:606` is `[_task_to_response(task, vault_config) for task in tasks]` under `async def list_tasks`, and `tasks.py:792-800` is the equivalent per-goal comprehension. `~/.claude/projects` accrues one directory per distinct cwd Claude Code has ever run in and is never pruned. Every card that misses the direct lookup — which, per the finding above, is *every* display-name session, plus every session started from another cwd — pays a full scan, and the miss path is the most expensive one. 50 cards against 300 project dirs is ~15k blocking syscalls per poll, on the event loop, blocking all other in-flight requests. Fix: list `projects_root` once per request into a `session_id → mtime` dict, or offload via `asyncio.to_thread`.

### run 2 — vault-ui#39 — tests/test_activity.py

**`tests/test_activity.py`** — no test asserts `activity_date` actually reaches an API response. `grep activity_date tests/` outside this file returns nothing, so the wiring added at `src/vault_ui/api/tasks.py:705-709` and `tasks.py:1794-1798` is uncovered; `compute_activity_date` is tested only in isolation. A wiring regression (wrong argument order — the signature is `(modified_date, session_id, project_dir)`, both positional and both nullable) would pass the whole suite. Add a `/api/tasks` and `/api/goals` case asserting the field is populated.

### run 2 — vault-ui#39 — src/vault_ui/static/app.js:1105

**`src/vault_ui/static/app.js:1105-1108`** — the two new functions were inserted between `cardShellHtml`'s doc comment ("Shared card body: menu button + title block + footer skeleton…") and `cardShellHtml` itself at line 1137. The comment now reads as documentation for `formatActivityAge`. Move the new pair above the comment block.

### run 2 — vault-ui#39 — src/vault_ui/static/app.js:1135

**`src/vault_ui/static/app.js:1135`** — `title="Last activity: ${escapeHtml(activityDate)}"` renders the raw serialized value (`2026-08-09T08:58:32.123456+00:00`). The CHANGELOG promises "the full timestamp on hover"; a UTC ISO string with microseconds is not what a user reads as a timestamp. `new Date(iso).toLocaleString()` gives local time.

### run 2 — vault-ui#39 — src/vault_ui/activity.py:21

**`src/vault_ui/activity.py:21-22`** — `_claude_projects_root()` hardcodes `Path.home() / ".claude" / "projects"`, a second independent resolver of the path `cleanup.py:31` already derives. Neither call site passes the `projects_root` override, so the seam exists but is unused in production. `VaultConfig.session_project_dir` is the established precedent for making this configurable.

### run 2 — vault-ui#39 — tests/test_activity.py:160

**`tests/test_activity.py:160-169`** — `test_activity_age_styled_small_and_grey` asserts the literal strings `font-size: 0.7rem` and `#64748b`. Restyling the badge breaks a test that is not about behaviour. Consistent with `test_card_render_unify.py` / `test_responsive_nav.py` house style, so noted only.

### run 2 — vault-ui#39 — src/vault_ui/vault_cli_client.py:318

**`src/vault_ui/vault_cli_client.py:318-319`** — `with suppress(ValueError, TypeError)` discards a malformed `modified_date` with no log line, making it indistinguishable from an absent field — and this is the fallback signal the whole feature rests on. Mirrors the pre-existing `_parse_task` at line 250, so fix both or neither.

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token_test.go:52

**`pkg/publisher/period_token_test.go:52-73`** — the `// Discriminating entries — old code produces wrong token on these` group comment covers the `2026-08-31 → 2026-07` entry at line 68, but old code produces `2026-07` there too (Aug 31 −1mo = Jul 31, no overflow); it is a control, not a discriminating case, and the prompt file at `prompts/completed/033-fix-period-token-month-end-offset.md:141` itself classifies it as control. A reader trusting the comment believes the fix is proven for a case it does not exercise. *(rule: `tdd/failing-test-before-implementation`)*

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token_test.go:24

**`pkg/publisher/period_token_test.go:24-84`** — positive `PeriodOffset` is completely untested, yet it is a documented, reachable, and behavior-changing input: `k8s/apis/task.benjamin-borbe.de/v1/types.go:76` states "positive values name a future period," and `pkg/k8s_connector_schema.go:182` declares `periodOffset` as a bare `integer` with no `Minimum`/`Maximum` (only the Monthly/Quarterly/Yearly CEL gate). Old vs. new diverge on 5 of 7 day-31 months at `offset=+1` (e.g. `2026-01-31` → old `2026-03`, new `2026-02`; `2026-10-31` → old `2026-12`, new `2026-11`). Add at least one `offset=+1` day-31 entry. *(rule: `test-pyramid/push-down-when-unsure`)*

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token_test.go:24

**`pkg/publisher/period_token_test.go:24-84`** — no *discriminating* day-29 or day-30 entry exists, though the CHANGELOG and the prompt summary both claim "a day-29/30/31 fire date" is affected. The only day-30 entry (line 42, `2026-07-30`) is a control. The real day-29/30 cases are `2026-03-29` and `2026-03-30` at `offset=-1` (old `2026-03`, new `2026-02`). As it stands the tests only substantiate the day-31 half of the changelog claim. *(rule: `test-pyramid/push-down-when-unsure`)*

### run 2 — recurring-task-creator#30 — prompts/completed/033-fix-period-token-month-end-offset.md

**`prompts/completed/033-fix-period-token-month-end-offset.md`** — commit `23d403d` carries the subject `033-fix-period-token-month-end-offset`, i.e. the prompt filename slug rather than an imperative description of the change. *(rule: `git-commit/imperative-mood`)*

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token.go:105

**`pkg/publisher/period_token.go:105,109,113`** — the identical anchor expression `time.Date(base.Year(), base.Month(), 1, 0, 0, 0, 0, base.Location())` is written three times. `render.go:53` already hosts exactly this kind of leaf helper (`firstOfPreviousMonth`), and `placeholders.go:134` already uses the same day-1 idiom — a shared `firstOfMonth(base)` would put all period arithmetic in one place. The prompt explicitly permitted either shape, so this is preference, not a defect.

### run 2 — recurring-task-creator#30 — pkg/publisher/period_token.go:104

**`pkg/publisher/period_token.go:104-115`** — the anchoring is non-obvious arithmetic with no comment explaining *why* (Go's `AddDate` rolls a nonexistent "June 31" into July). The sibling `RecurrenceOnDate` case at line 116 does carry an explanatory comment. Without one, a future "simplification" back to `base.AddDate(...)` reintroduces the bug silently — the mislabeled entry above means the test suite would still pass for the Aug-31 case.

### run 2 — recurring-task-creator#30 — go.mod:95

**`go.mod:95`** — `go mod tidy -diff` wants to drop `github.com/maxbrunsfeld/counterfeiter/v6 v6.12.2 // indirect` (not in the `tool (…)` block at line 5). This is **pre-existing** — byte-identical on `origin/bench-base-30` — so not introduced here, but a `fix(deps)` commit already touching `go.mod` is the natural moment to clear it. *(rule: `go-mod-dependency-fix/tidy-after-get-update`)*

### run 2 — tts-mcp#10 — mcp/package-lock.json

**`mcp/package-lock.json`** — The lockfile has gone stale in the 16 days since it was authored (commit 2026-07-25, today 2026-08-10) and still carries **4 advisories, one high**. Measured with the same DB at the same moment: base = 8 (3 high, 3 moderate, 2 low), this branch = 4 (1 high `fast-uri` 3.0.0–3.1.4, 3 moderate `hono` ≤4.12.33 / `@hono/node-server` / `@modelcontextprotocol/sdk`). Re-running the PR's own approach today reaches **zero**: `npm update --package-lock-only` resolves `fast-uri` 3.1.5, `hono` 4.13.1, `@hono/node-server` 2.1.0, SDK 1.30.0 — all inside the existing `^1.26.0` range, no `package.json` change, no downgrade. I verified `npm ci && npx tsc --noEmit` still exits 0 against that tree. Refresh the lockfile before merge.

### run 2 — tts-mcp#10 — CHANGELOG.md:17

**`CHANGELOG.md:17`** — "The two remaining advisories are `@hono/node-server <2.0.5` … Left unfixed deliberately" is wrong as of today: four remain, and the high-severity `fast-uri` host-confusion issue is a separate chain (via `ajv`) that the stated "Windows-specific `serve-static`, macOS-only relay, serves no static files" rationale does not cover. Because `.maintainer.yaml` sets `autoRelease: true`, this text auto-publishes as release notes asserting a security posture the artifact doesn't have. The deferral reasoning is also now obsolete — SDK 1.30.0 (published 2026-07-27, two days after this commit) widened its constraint to `^1.19.9 || ^2.0.5`, so the fix no longer requires the breaking downgrade to 1.24.3 the entry describes.

### run 2 — tts-mcp#10 — CHANGELOG.md:16

**`CHANGELOG.md:16`** — Internally inconsistent: the sentence claims "clearing 6 of 8 npm-audit findings" but the list after the colon enumerates 8 (2 high + 4 moderate + 2 low). Reads like the full base-audit breakdown mislabeled as the cleared subset. Measured reality for this diff is 5 packages cleared (`body-parser`, `esbuild`, `express-rate-limit`, `ip-address`, `qs`).

### run 2 — tts-mcp#10 — Makefile.precommit:26

**`Makefile.precommit:26`** — The new gate typechecks but adds no audit check, so nothing detects advisory regression. *(rule: `node/make/security-gates-in-check`)* This is precisely the drift that produced the stale state above: the PR fixes the finding once, and CI cannot tell when it comes back. Since `npm ci` already runs here, `npm audit --omit=dev --audit-level=high` is zero extra install cost. Consistent with the repo's lean-toolchain stance in `AGENTS.md` — no new scanner dependency.

### run 2 — tts-mcp#10 — Makefile.precommit:26

**`Makefile.precommit:26`** — `npm ci` unconditionally wipes and re-fetches `mcp/node_modules` on every `make check` / `make precommit`, so the documented local gate now requires network access on each run. Guard it behind a lockfile-change sentinel, or at minimum use `npm ci --prefer-offline --no-audit`.

### run 2 — tts-mcp#10 — Makefile.precommit:26

**`Makefile.precommit:26`** — `npx tsc` silently falls back to fetching an unpinned compiler if `typescript` ever leaves `devDependencies`. Not a live bug (it's pinned at `^5.9.3`), but `npx --no-install tsc --noEmit` fails loudly instead.

### run 2 — tts-mcp#10 — CHANGELOG.md:14

**`CHANGELOG.md:14`** — `### Fixed` placed above `### Changed`; Keep a Changelog order (which line 5 commits to) is Added, Changed, Deprecated, Removed, Fixed, Security.

### run 2 — tts-mcp#10 — CHANGELOG.md:16

**`CHANGELOG.md:16`** — A lockfile-only dependency bump fits `### Security` (or `chore:`) better than `### Fixed`.

### run 2 — tts-mcp#10 — CHANGELOG.md:16

**`CHANGELOG.md:16-17`** — 60–90 word entries carrying rationale, verification notes, and deferral justification largely duplicate the commit body; the guide prescribes one terse line per change.

### run 2 — tts-mcp#10 — CHANGELOG.md:16

**`CHANGELOG.md:16,17,21`** — No conventional prefix. *(rule: `changelog/conventional-prefix-required`)* Flagged low because the entire file, including released v0.1.0, follows the same prefix-free style — this PR is consistent with repo convention rather than introducing drift.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:110

**`shim/claude_openai_shim.py:110`** — the boolean branch of `setting()` fails **open**: `not in ("0","false","no","off","")` means *any* unrecognised string resolves to `True`. This diff edited this exact line and its new comment claims it now "agrees with the bot's `flag()` (src/config.js) on every spelling" — it does not. `flag()` correctly falls back to the caller's default on unrecognised input; `setting()` returns `True` regardless of the declared default. Verified by running both implementations side by side: | value | shim `setting()` | bot `flag()` | |---|---|---| | `disabled` / `none` / `flase` / `n` / `2` | **True** | `false` | | `" 0 "` / `" off "` (quoted + padded) | **True** | `false` | Failure scenario: `UNSAFE = setting("SHIM_UNSAFE", "unsafe", False)` (`:164`) gates the tool allowlist at `:1161` and `:1571`. `SHIM_UNSAFE=disabled` — or any typo, or `unsafe: disabled` in the YAML documented at `config.example.yaml:35` — silently yields `UNRESTRICTED` tools on a bot the repo's own `CLAUDE.md` says "can reach a session with vault and repository access." Fail-open on the one flag named `UNSAFE`. Fix: mirror `flag()` — explicit true-list, explicit false-list, `return default` otherwise (and re-`.strip()` after `.strip("\"'")`).

### run 2 — discord-assistant#5 — src/config.js:142

**`src/config.js:142`** — the bot's `wakePhrases` reads `process.env.SHIM_WAKE_PHRASES` only, while the shim's `WAKE_PHRASES` (`shim/claude_openai_shim.py:799`) resolves env **or** `voice.wake_phrases` — a YAML key this PR newly documents at `config.example.yaml:65`. I confirmed the bot reads no YAML anywhere. Configure wake phrases via YAML and the two sides run different lists. The comment at `src/config.js:139-140` says drift costs only "dots without an answer… never a wrong answer," but "dots without an answer" *is* the wedge: with the bot more permissive (e.g. YAML sets `jarvis`, bot keeps the default `hey bot`), a "hey bot …" utterance sets `answering = true` at `src/voice.js:576` while the endpoint answers with silence, so nothing clears it and `speak()` returns `busy` (`src/voice.js:407`) for the full `TYPING_MAX_MS` — 5 minutes. That is exactly the bug this PR's second commit fixes, reachable through the config surface the same PR added. Either have the bot read the YAML, or drop `voice.wake_phrases` from the example config.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:799

**`shim/claude_openai_shim.py:799`** — `hey but` ships as a **default** wake phrase and false-triggers on ordinary spoken English. Differential-tested both implementations over 20 utterances; they agree on all 20, including `"hey but wait, that's not what I meant"` → **addressed** and `"hey bought me lunch"` → **addressed**. "Hey but…" is a common sentence-initial interjection, so every deployment that hasn't overridden the default inherits the precise failure the feature exists to prevent — answering a sentence aimed at someone else. The CHANGELOG accepts variants as "a false-trigger risk accepted on purpose," which is defensible for an operator who adds one from their own transcripts, but not for a shipped default.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py

**`shim/claude_openai_shim.py`** — ~135 new lines (bool parser, `is_addressed`, `strip_wake_phrase`, `_PANEL` anchor lines, the wake gate at `:1830`, the bridge strip at `:2016`) ship with **zero** test coverage. Confirmed the repo has no `pytest.ini`, no `pyproject.toml`, no `test_*.py` — `test/` is Node-only. Every defect above is exactly what a small `setting()`/`is_addressed` table test would catch mechanically.

### run 2 — discord-assistant#5 — src/voice.js:331

**`src/voice.js:331-338`** — the new `session.update` payload has no test asserting its shape, though the CHANGELOG records that the wrong shape "silently did nothing on every connect" and reads as an unsupported-event error rather than a validation failure. A single assertion on the serialised JSON (both `type` discriminators present, `interrupt_response` tracking `config.interruptResponse`) pins the one thing that already regressed once.

### run 2 — discord-assistant#5 — None

**`LICENSE`** — no LICENSE file in the repo root. *(rule: `go-licensing/license-file-required`)* Pre-existing, not introduced here.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:2016

**`shim/claude_openai_shim.py:2016`** — the "should we post?" decision (`why`, computed at `:2003-2007`) runs against the raw `answer`, but the posted content is `strip_panels(answer)`. An answer judged postable *because* of a URL that sits inside a `⏰ Next:` line loses that line to the stripper, so `post_chat_message("")` fires. The bot rejects it (`src/health.js:72` returns 400), so nothing empty reaches Discord — but the shim then logs `chat bridge: post failed (HTTP Error 400)` for what is really correct behaviour. Compute `why` on the stripped text, or skip the POST when stripping empties it.

### run 2 — discord-assistant#5 — shim/claude_openai_shim.py:822

**`shim/claude_openai_shim.py:822-830`** — a punctuation-only `SHIM_WAKE_PHRASES` (`","`) is truthy after `.strip()`, so `is_addressed`'s empty-list guard is skipped and the regex compiles with an empty alternation that matches at span `(0,0)` on every input. Verified: gate silently turns fully **open**. The JS side reaches the same open state via `phrases: []`, so the two agree — but "answer everything" is the direction the module's own comment says must never be the fallback. Log a warning when the phrase list filters to zero.

### run 2 — discord-assistant#5 — src/config.js:181

**`src/config.js:181`** — `isAddressed` compiles a fresh `RegExp` per phrase per call, inside `.some()`, on every transcription event. Precompile at module load as the shim does.

### run 2 — discord-assistant#5 — src/voice.js:331

**`src/voice.js:331`** — the `ws.send` in the `open` handler is unguarded; a synchronous throw there is not caught by the `ws.on('error')` handler registered below it.

### run 2 — discord-assistant#5 — src/config.js:30

**`src/config.js:30`** — `WAKE_LEAD_WORDS` is hand-synced with the shim's `_FILLER_WORDS | {"hey"}`. I diffed both: **identical, 20 members, no drift today.** Noted only because nothing enforces it.

### run 2 — discord-assistant#5 — CHANGELOG.md:1

**`CHANGELOG.md:1`** — no canonical SemVer preamble between `# Changelog` and the first `##` section. *(rule: `changelog/preamble-frozen`)* Confirmed present in `origin/bench-base-5`, so pre-existing; the mechanical funnel reported it 18 times (once per section header) — it is one file-level gap, not 18 findings.

### run 2 — tts-mcp#13 — src/server.py:353

**`src/server.py:353`** — `body.instruct` is queued without checking `state.engine` supports it, so `POST /say` returns `202 Accepted` for a request that can never succeed on a voxtral server; the `ValueError` at `src/tts/engine.py:132` only surfaces asynchronously via polling. The handler already does exactly this kind of synchronous validation for `voice` five lines earlier — mirror it: reject `instruct` with `HTTPException(400)` when `state.engine.kind != QWEN3`. This is also the cleanest fix for the Critical above.

### run 2 — tts-mcp#13 — README.md:266

**`README.md:266-270`** — The documented `POST /say` example pairs `"voice": "casual_female"` (a Voxtral voice) with `"instruct"` (qwen3-only), so the example as printed is rejected by *both* engines — voxtral rejects `instruct`, qwen3 has no `casual_female` speaker. Use a qwen3 speaker (`ryan`) in the `instruct` example, or show two separate examples.

### run 2 — tts-mcp#13 — README.md:133

**`README.md:133-140`** — "Presents three Voxtral 4B variants to choose from" plus a 3-row table is made stale by this PR's own `scripts/download-model.sh`, which now offers 5 choices across two engines. `README.md:146` ("downloads a Voxtral model") has the same problem. This is the only documented route to a Qwen3 model, and it doesn't mention Qwen3.

### run 2 — tts-mcp#13 — src/tts/worker.py:55

**`src/tts/worker.py:55,116,146,187,230`, `src/server.py:76,470,528,577`** — `model` is typed `TTSModel` throughout the orchestration layer, but `TTSModel` declares `.generate()`, which a Qwen3 CustomVoice model does not have. The annotation is false for one of the two supported families and only typechecks because `Qwen3Engine.generate` discards it via an unchecked `cast(CustomVoiceModel, model)` (`src/tts/engine.py:249`). The real runtime guard is `validate_model`. Type it as an opaque loaded-model marker outside the engine, where the true shape is known.

### run 2 — tts-mcp#13 — tests/test_engine.py

**`tests/test_engine.py`** — Test coverage stops at the engine boundary. There is no test that `POST /say` carries `instruct` through to the engine, none that a voxtral server rejects an `instruct` request (which is why the Critical above ships green), and none for the new `engine`/`language` parsing in `_parse_server_config` or `load_cli_config`.

### run 2 — tts-mcp#13 — src/server.py:115

**`src/server.py:115-232`** — 15 of the 17 new read-only properties wrap immutable config scalars (`sample_rate`, `target_lufs`, `meter`, …), not injected collaborators; ~100 lines of boilerplate buying no encapsulation the existing frozen `_ServerConfig` dataclass wouldn't give for free. Keeping `engine` private is well-motivated; the rest would be better as a single held config object. (`model`/`statuses`/`work_queue`/`ready_queue` staying public is correct — they genuinely need post-construction mutation.)

### run 2 — tts-mcp#13 — src/tts/engine.py:47

**`src/tts/engine.py:47-57`** — `instruct` sits in the shared `TTSEngine.generate` signature although only one family accepts it, forcing `VoxtralEngine` to defend against it at every call. Validating once at the request boundary would let the Protocol stay honest.

### run 2 — tts-mcp#13 — src/tts/generate.py:57

**`src/tts/generate.py:57-87`** — `streaming_interval` is threaded through the buffered path where both engines provably ignore it; every buffered call site now passes a meaningless value.

### run 2 — tts-mcp#13 — src/main.py:299

**`src/main.py:299-307` / `src/server.py:693-701`** — Byte-identical `engine`/`language` validation blocks. Extends a pre-existing duplication rather than introducing one, but a shared helper would remove both.

### run 2 — tts-mcp#13 — src/server.py:287

**`src/server.py:287-288`** — `StatusResponse.audio_file`/`error` widened from required to `= None`, though the sole construction site always passes both explicitly. Unused defaults in a client-visible schema sit awkwardly against "never assume any default values."

### run 2 — tts-mcp#13 — README.md:88

**`README.md:88-100`** — `src/tts/` tree lists every module except `__init__.py`, which this PR changed substantively (the package's public API).

### run 2 — tts-mcp#13 — README.md:186

**`README.md:186`** — `sample_rate` documented only as "24000 for Voxtral"; needs engine-neutral wording now. (Qwen3's rate isn't stated anywhere in the repo — don't invent one.)

### run 2 — tts-mcp#13 — README.md:180

**`README.md:180-198`** — Neither the `engine` nor `default_voice` row warns that Voxtral voice names and qwen3 speaker names are disjoint, so flipping `engine:` alone fails at startup.

### run 2 — tts-mcp#13 — tests/test_engine.py:78

**`tests/test_engine.py:78-96`** — qwen3 dispatch tests use bare `MagicMock()`, which accepts any method name and any kwargs; they'd pass unchanged if `generate_custom_voice` were misspelled or a kwarg renamed. `create_autospec` against the real class would guard against library drift. (`TestValidateModel` already uses `spec=` correctly.)

### run 2 — github-releaser-agent#8 — pkg/steps_planning.go:758

**`pkg/steps_planning.go:758`** — the `nothing_to_release` completion gate keys off `precondition == PreconditionP1UnreleasedNotFirst`, but that token covers **two distinct changelog shapes**, and only one of them means "nothing to release". `changelog.ValidateUnreleased` (`pkg/changelog/changelog.go:61` and `:69`) emits the identical `"is not the first ## section"` reason both when no `## Unreleased` heading exists at all (the healthy post-release case this feature targets) *and* when a `## Unreleased` section exists **with real bullets** but sits below a version heading. `classifyValidationFailure` (`pkg/steps_planning.go:898`) collapses both into `P1_unreleased_not_first`. So a repo that keeps `## Unreleased` at the bottom of its CHANGELOG, re-checked at the commit its current tag points at, gets `status: completed` / `phase: done` with genuinely unreleased bullets silently discarded — no escalation, no operator signal. `changelogBytes` is already in hand at the call site, so the guard is free and deterministic: require `len(changelog.ExtractUnreleasedBullets(changelogBytes)) == 0` before calling `publishNothingToRelease`. (I confirmed `ExtractUnreleasedBullets` at `pkg/changelog/changelog.go:176` locates the first non-version `##` heading regardless of position, so it *does* return those bullets.) This also makes the `docs/dod.md:26` and CHANGELOG claims accurate — both currently assert every non-matching case "escalates byte-identically to before", which holds for the SHA comparison but not for the precondition classification feeding it.

### run 2 — github-releaser-agent#8 — pkg/githubtags/tags.go:33

**`pkg/githubtags/tags.go:33-35`** — doc regression: the `TagsFetcher` interface comment lost `"Implementations MUST be safe for concurrent use."` with no replacement anywhere in the file, and `LatestSemverTag` now carries **no doc on the interface at all** while its new sibling `CommitSHAForTag` got a full contract block. The `LatestSemverTag`/`ErrNoTags` contract survives only on the concrete impl at `:154`, which `go doc` on the interface will not surface. Restore the concurrency sentence at type level and add a short method doc mirroring `CommitSHAForTag`'s style. *(rule: `go-doc/exported-item-must-have-comment`)*

### run 2 — github-releaser-agent#8 — pkg/steps_planning.go:718

**`pkg/steps_planning.go:718-736`** — `releaseTagCommit` collapses `ErrTagNotFound` and genuine transport/decode failures into the same `""` return, logs the latter only via `glog.Warningf`, and returns no error. The escalation that follows carries the original changelog-validation `reason` verbatim, so an operator reading a `P1_unreleased_not_first` escalation has no signal that a GitHub outage — not a real non-match — produced it. This diverges from the file's own convention for exactly this failure class: both `resolveCurrentVersion` (`:353`) and `resolveMaintainerConfig` (`:308`) thread a `fetchWarning` onto `PlanOutput.ConfigFetchWarning`. The fail-open *safety* property is sound and correctly implemented; the observability half is missing. Thread a warning out and merge it into the escalation via the existing `joinWarnings` shape.

### run 2 — github-releaser-agent#8 — pkg/steps_planning_test.go:2603

**`pkg/steps_planning_test.go:2603-2626`** — the `sameCommit` swap branch at `pkg/steps_planning.go:75` (`len(a) > len(b)`) is never executed. Every table entry and every `step.Run` scenario has `len(ref) ≤ len(tagCommit)` — `ref` is the 40-char `incidentSHA` or a short/empty/non-hex value, and `CommitSHAForTagReturns` always supplies a full 40-char SHA or empty. Production risk is bounded (GitHub always returns a 40-char commit SHA), but this is unexercised defensive logic in the predicate that decides whether a release auto-completes without human review. One entry closes it: `Entry("swapped: full ref vs short tag commit", incidentSHA, "7657fc0", true)`.

### run 2 — github-releaser-agent#8 — pkg/githubtags/tags.go:132

**`pkg/githubtags/tags.go:132`** — the `collectTags` pagination loop is the one new loop doing I/O per iteration (up to 100 `fetchPage` round-trips) with no `ctx.Done()` check at the top. `fetchPage` uses `http.NewRequestWithContext` at `:179`, so cancellation is detected one HTTP-layer error later rather than ignored — hence Important, not Critical. Add a non-blocking `select` at loop top so give-up is deterministic rather than incidental. *(rule: `go-context/cancel-check-in-loop`)*

### run 2 — github-releaser-agent#8 — pkg/plan_output.go:110

**`pkg/plan_output.go:110`** and **`pkg/steps_planning.go:38`** — `PlanOutcomeNothingToRelease` and `releaseTagVerdictEscalate` are bare untyped string constants with no paired newtype + `Available*` collection. Every sibling in those const blocks (`PlanOutcomeReady`, `PlanOutcomeNeedsInput`, `PlanOutcomeFailed`, all four `Precondition*`) is already the same shape, so this PR extends an existing pattern rather than introducing one. Worth a package-wide typed-enum pass as its own change, not tacked on here. *(rule: `go-enum-type/typed-constants-with-collection`)*

### run 2 — github-releaser-agent#8 — pkg/githubtags/tags.go:231

**`pkg/githubtags/tags.go:231`** — `CommitSHAForTag` validates only `tag == ""` itself; empty `owner`/`repo` surface from `collectTags` with a `"list tags: ..."` prefix instead of `"commit sha for tag: ..."`. Cosmetic message-prefix inconsistency; both errors remain accurate and traceable.

### run 2 — github-releaser-agent#8 — pkg/githubtags/tags.go:226

**`pkg/githubtags/tags.go:226`** — `// CommitSHAForTag implements TagsFetcher.` is thinner than `LatestSemverTag`'s impl comment at `:148`. Defensible now that the interface carries the full contract, but it creates asymmetry between the two methods on the same struct.

### run 2 — discord-assistant#1 — src/index.js:272

**`src/index.js:272-278`** — `RECOVERABLE_NET` is not scoped to the gateway, but the branch unconditionally clears `gatewayReady` — and nothing can set it back. `gatewayReady = true` happens only at `src/index.js:76`, `110`, `113` (`clientReady` / `shardResume` / `shardReady`). A voice-side fault matches the same regex: the voice connection (`src/voice.js:438`) attaches only a `stateChange` listener and the audio player (`src/voice.js:99`) only `idle` — neither has an `'error'` listener, so their `ECONNRESET` / `ETIMEDOUT` / `WebSocket was closed` errors reach `uncaughtException`. The gateway never dropped, so no shard event ever fires again, and readiness is pinned at 503 permanently. Liveness ignores the gateway by design (`src/health.js:30-31`, `k8s/discord-assistant-deploy.yaml` livenessProbe → `/healthz`), replicas is 1, and `supervise.sh` only reacts to process *exit* — so nothing restarts it. Before this PR that fault exited 1 and got restarted. Gate the branch on the error actually originating from the gateway, or restore readiness from the same signal that cleared it.

### run 2 — discord-assistant#1 — src/index.js:272

**`src/index.js:272-278`** — no bounded fallback, so the state the comment at `src/index.js:254-258` calls "strictly worse than exiting" is now reachable *after* login. The comment's own justification is that discord.js reconnects past login — but if that reconnect does not recover (and lines 248-252 note this error class escapes `ws` with no listener, i.e. outside discord.js's handling), the process stays alive, liveness-green, and permanently disconnected, with no crash to make it visible. That is the exact 2026-08-05 symptom ("`/join` doing nothing"), now silent forever instead of silent until the next crash. Add a watchdog: if `gatewayReady` is still false N minutes after the swallow fires, `process.exit(1)` and let `supervise.sh` / k8s do their job.

### run 2 — discord-assistant#1 — scripts/supervise.sh:19

**`scripts/supervise.sh:19-24`** — `stop()` sends SIGTERM and `exit 0`s immediately with no `wait "$child"`, so the supervisor reports success while node is still draining; its graceful shutdown (`src/index.js:199-227` — leave voice, evict ghosts, `client.destroy()`, `health.close()`) needs ~600ms+. Verified empirically: the supervisor exits 0 while the child is still running 3s later. Masked under interactive Ctrl-C (shared process group delivers SIGINT to node too), not masked for `kill <supervise-pid>` — which is the unattended case the file's header describes. Fix: `kill "$child" 2>/dev/null; wait "$child" 2>/dev/null` before `exit 0`.

### run 2 — discord-assistant#1 — scripts/supervise.sh:46

**`scripts/supervise.sh:46-51`** — `delay` only ever grows; nothing resets it to `DELAY_MIN` after a healthy run. One unrelated crash weeks into a clean run still waits the capped 60s instead of 2s. Reset when the child stayed up longer than some threshold.

### run 2 — discord-assistant#1 — scripts/supervise.sh:35

**`scripts/supervise.sh:35-44`** — exit 127 (command not found) and 126 (not executable) fall through to the generic restart branch and loop forever printing the same unfixable error. Same "restarting cannot fix this" class the `2)` branch exists for; short-circuit them too.

### run 2 — discord-assistant#1 — src/index.js:262

**`src/index.js:262-281`** — no test covers the new behaviour, though `test/` already has `config.test.js`, `health.test.js`, `llm.test.js` as precedent. The `connectedOnce` gate is the whole design of this change and is untested in both directions (pre-login must exit, post-login must survive); `scripts/supervise.sh`'s exit-code policy (0 → stop, 2 → give up, else restart) is likewise untestable-by-inspection only.

### run 2 — discord-assistant#1 — src/index.js:263

**`src/index.js:263-265`** — a second `client.once('clientReady')` listener 190 lines away from the first (`src/index.js:75`), setting a flag that duplicates what line 76 already records. Folding `connectedOnce = true` into the existing handler keeps gateway-lifecycle state in one place, next to the grouped `shardDisconnect`/`shardResume`/`shardReady` handlers.

### run 2 — discord-assistant#1 — scripts/supervise.sh:24

**`scripts/supervise.sh:24`** — `trap stop INT TERM` where the sibling `scripts/dev.sh:33` uses `trap cleanup EXIT INT TERM`. Any other exit path leaves the child orphaned.

### run 2 — discord-assistant#1 — CHANGELOG.md:5

**`CHANGELOG.md:5`** — no conventional prefix *(rule: `changelog/conventional-prefix-required`)*. Every existing bullet in the file is prose without a prefix, so the new entry matches the repo's own uniform convention; this is a repo-wide decision, not a fix for this PR.

### run 2 — helm#3 — charts/github-pr-reviewer/values.yaml:61

**`charts/github-pr-reviewer/values.yaml:61`** — This is the **first comma-delimited env value anywhere in the monorepo** (grep across all `charts/*/values.yaml` returns only this line); the previous value `bborbe` was a single token, so the watcher's list-splitting path has never been exercised by this chart. `github-pr-watcher` lives in another repo, so I can't verify it here. If it doesn't split on `,`, the whole string becomes one login and **`bborbe`'s existing trust regresses silently** — no error, just PRs quietly dropping to `human_review`. Confirm the delimiter against the watcher source before deploying.

### run 2 — helm#3 — charts/github-pr-reviewer/values.yaml:52

**`charts/github-pr-reviewer/values.yaml:52-53`** — The claim that the watcher sees `<slug>[bot]` and matches case-sensitively is an unverified assertion about another repo's runtime. A mismatch (watcher lowercases, strips the suffix, or uses the REST `app/<slug>` form) fails **silently** — the entry never matches and nothing logs. Verify against the watcher's matching code, not the App's GitHub UI rendering.

### run 2 — helm#3 — charts/github-pr-reviewer/Chart.yaml:9

**`charts/github-pr-reviewer/Chart.yaml:9`** — `annotations:` already carries `artifacthub.io/prerequisites`; a behavior-changing minor bump is a natural fit for an `artifacthub.io/changes` entry so the trust widening is visible in release metadata rather than only in git history.

### run 2 — dark-factory#71 — agents/prompt-auditor.md:509

**`agents/prompt-auditor.md:509-518`** — the fix is applied to 1 of 3 identical instances. `agents/spec-auditor.md:600-604` and `agents/scenario-auditor.md:172-176` carry the same `<final_step>` → `1. **Implement fixes** - Apply critical issues and top recommendations`, under the same line-17 constraint `NEVER modify files - audit only, report findings`, with the same `Read, Bash, Glob` toolset (no `Edit`/`Write`). Same bug, same wording, same position — both still end every audit proposing an action they must refuse. The prompt-auditor's own checklist has a "Sibling entry points covered" criterion (line 468) that this change fails against itself. *(rule: `agent-cmd/no-user-prompts`)*

### run 2 — dark-factory#71 — CHANGELOG.md:15

**`CHANGELOG.md:15`** — entry claims pairs are produced "for each Blocker/Critical issue", but there is no `Blocker` severity. The report template defines only `## Critical Issues` and `## Recommendations`; grepping `agents/`, `commands/`, `docs/` for "blocker" returns only unrelated container-failure prose (`agents/prompt-auditor.md:349`) and `spec-auditor.md` using it as a synonym, never as a tier. The sentence also omits that Recommendations get pairs "when the wording is obvious". Suggested replacement for the last sentence: `It now hands back exact find/replace pairs for each Critical Issue — and for Recommendations where the wording is obvious — so the caller can apply them directly.`

### run 2 — dark-factory#71 — agents/prompt-auditor.md:494

**`agents/prompt-auditor.md:494-496`** — Critical Issues demands `an exact find/replace pair: the old text verbatim` for *each* entry, with no escape hatch, but two common Critical classes have no old text to match. Missing required sections: lines 37-44 mandate six XML sections (`<summary>`…`<verification>`), so the fix is an insertion, not a replacement. Wrong location: line 56 (`New prompts MUST be in prompts/ inbox`) makes the fix a file move. In both cases the agent is instructed to emit something that cannot exist. Recommendations received the fallback (`otherwise describe the change in one line`) at line 500; Critical Issues did not. Give it the same fallback, or add an insertion form (anchor line + text to insert after it).

### run 2 — dark-factory#71 — agents/prompt-auditor.md:514

**`agents/prompt-auditor.md:514-518`** — the second paragraph of `<final_step>` restates the find/replace requirement already specified in `<output_format>` at lines 494-500, creating two places to keep in sync; the template is the enforcing copy, so this prose can go stale silently. The first paragraph (lines 510-512) carries the genuinely new instruction — `a mid-task instruction from the caller does not lift that` — and is worth keeping. Collapse the rest to a pointer, e.g. `Hand the work back via the report's Critical Issues / Recommendations sections, which already specify the find/replace format.`

### run 3 — tts-mcp#20 — CHANGELOG.md

**`CHANGELOG.md`** — no entry for this security fix, and no `## Unreleased` block exists. `.maintainer.yaml` sets `release.autoRelease: true`, and the releaser cuts from `## Unreleased` — so as-is this High-CVE fix ships only whenever the next feature release happens to land. The repo has direct precedent: commit `c6f136a` documented an equivalent mcp/ lockfile-only security bump under `### Fixed`, naming `fast-uri` explicitly. Dependabot won't add this on its own.

### run 3 — github-pr-review-agent#11 — pkg/factory/runner.go:57

**`pkg/factory/runner.go:57-60`** — `SkipPost` is documented as "suppresses all GitHub write calls" with no caveat, but `ResolvePosters` is only reached inside the `if agent == nil` branch at line 134. `main.go:187` passes a non-nil `Agent` built by `CreateAgentProvider`, which wires real posters unconditionally (`pkg/factory/factory.go:237-238`). Any caller combining `Agent` with `SkipPost` gets a silently ignored safety flag and posts to GitHub. Not live today (only `cmd/run-task` sets `SkipPost`, and it leaves `Agent` nil), but this is a fail-open default on the one field whose failure mode is "wrote to a stranger's PR". Either honor `SkipPost` in the `agent != nil` path, or document the precondition on the field.

### run 3 — github-pr-review-agent#11 — pkg/skip_post_boundary_test.go:222

**`pkg/skip_post_boundary_test.go:222-224`** — case 4's stated negative signal cannot detect the regression its comment describes. `appendVerifyDiagnostic` (`pkg/steps_review.go:253`) is only called when `callVerifier` returns non-nil, i.e. when verification *fails* (`pkg/steps_review.go:120-121`). If the nil-interface contract broke and a live GitHub verification *succeeded*, no `ai_review verify:` line would be written and this assertion would still pass. The test does catch the regression incidentally (a typed-nil verifier would panic and trip `Expect(err).NotTo(HaveOccurred())`), but the documented assertion is inert — assert `verifier == nil` directly, or drop the diagnostics check and state the panic as the signal.

### run 3 — github-pr-review-agent#11 — README.md:33

**`README.md:33`** — the new `--skip-post` example is not runnable as written. `cmd/run-task/main.go:126-131` hard-errors unless `--app-id`, `--installation-id`, and one of `--pem-key-file`/`--pem-key` are all set. Since this line exists specifically to document the new flag, it should show the App-auth flags too.

### run 3 — github-pr-review-agent#11 — pkg/factory/runner.go:57

**`pkg/factory/runner.go:57`** — "suppresses all GitHub write calls" understates the blast radius: `ResolvePosters` also nils the `ReviewVerifier`, which does a read-only `GET /pulls/{n}/reviews`, so `--skip-post` disables post-verification as well. The same wording is duplicated in `cmd/run-task/main.go:76` (usage string) and `README.md:29`; all three should say writes *and* verification.

### run 3 — github-pr-review-agent#11 — pkg/steps_checkout_execution.go:316

**`pkg/steps_checkout_execution.go:316-321`** — the nil-poster early return skips `appendDiagnosticsSection`, so a `--skip-post` run leaves no `## Diagnostics` record at all, including whether the funnel fail-closed override at line 338 would have fired. For the flag's stated measurement/debug purpose, a synthetic `outcome=skipped reason=skip-post` block would preserve the audit trail.

### run 3 — github-pr-review-agent#11 — cmd/run-task/main.go:147

**`cmd/run-task/main.go:147`** — `glog.V(2).Infof` with a constant string and no format verbs; `glog.V(2).Info` is the right call. Separately, gating a safety-flag confirmation behind `V(2)` means an operator at default verbosity gets no signal that posting is suppressed.

### run 3 — github-pr-review-agent#11 — prompts/cancelled/001-run-task-skip-post-flag.md

**`prompts/cancelled/001-run-task-skip-post-flag.md`** — shares ID `001` with `prompts/completed/001-run-task-skip-post-flag.md`, which superseded it four minutes later (cancelled 21:09:15, re-queued 21:10:37). Committing both duplicates ~110 lines of near-identical prompt body.

### run 3 — github-pr-review-agent#11 — pkg/factory/runner.go:66

**`pkg/factory/runner.go:66`** — `ResolvePosters` puts a conditional inside `pkg/factory`, which `CLAUDE.md`'s "Factory functions are pure composition — no conditionals" forbids. Precedent exists (`ResolveBotLogin`, `pkg/factory/botlogin.go:12`), so `Resolve*` looks like the established escape hatch for the `Create*` purity rule — worth stating that in `CLAUDE.md` so the exception is intentional rather than inferred.

### run 3 — quant#109 — task/recurring-schedules/prod/check-bot-is-healthy.yaml:68

**`task/recurring-schedules/prod/check-bot-is-healthy.yaml:68`** — `due_date` is a **new frontmatter key with zero prior art anywhere in this repo** (before this PR, `grep -r due_date` returns nothing). The `recurring-task-creator` CRD lives in another repo, so nothing here proves `spec.template.frontmatter` accepts it. `make precommit` cannot catch this: `yamllint` checks syntax and `kustomize build` does no schema validation. The failure mode is **silent** — if `frontmatter` is a structural schema without `x-kubernetes-preserve-unknown-fields`, the API server prunes `due_date` on apply and 21 prod tasks ship with no due date, no error anywhere. Verify the CRD/controller accepts the key before `BRANCH=prod make apply`.

### run 3 — quant#109 — task/recurring-schedules/prod/plan-weekend.yaml:65

**`task/recurring-schedules/prod/plan-weekend.yaml:65`** — `due_date: "{{next_sun_date}}"` is due *after the window it plans*. The schedule fires Saturday, the body (line 18) says "Plan this weekend (Sat → Sun) **as the first thing Saturday morning**", and it carries `priority: 1` — the highest of any changed file. A weekend plan due Sunday evening is worthless by the time it's due. The six other same-day-actionable Saturday tasks correctly got `{{current_date}}`; this one should too.

### run 3 — quant#109 — task/recurring-schedules/prod/plan-next-week.yaml:40

**`task/recurring-schedules/prod/plan-next-week.yaml:40`** — the new pre-check gates `/plan-week` on the previous week's note having `reviewed: true`, which is produced by `/complete-week` — driven by `weekly-review.yaml`. Both fire Saturday, both now carry `due_date: "{{next_sun_date}}"` (`weekly-review.yaml:64`, `plan-next-week.yaml:67`) and both are `priority: 3`. The PR introduces a hard dependency but encodes no ordering in either due date or priority. Give `weekly-review` an earlier due date or a stronger priority.

### run 3 — quant#109 — task/recurring-schedules/prod/plan-next-week.yaml:29

**`task/recurring-schedules/prod/plan-next-week.yaml:29`** — "Month link present (target may not exist yet if the monthly plan is deferred)" is a success criterion with a built-in escape clause; it can't fail. Either drop the parenthetical or make it "month link present, dangling allowed".

### run 3 — quant#109 — task/recurring-schedules/prod/plan-next-week.yaml:28

**`task/recurring-schedules/prod/plan-next-week.yaml:28`** — "Deferrals stated explicitly (`# Explicitly Deferred`)" mandates the section unconditionally; a week with nothing deferred still needs an empty heading to tick the box.

### run 3 — quant#109 — task/recurring-schedules/README.md

**`task/recurring-schedules/README.md`** — the Saturday `current_date` vs `next_sun_date` split is a real convention but is nowhere written down. Two lines in the README ("same-day ops → current_date; work with a full-weekend window → next_sun_date") would keep the next 52 schedules consistent.

### run 3 — node-skeleton#2 — src/server.ts:31

**`src/server.ts:31`** — `export type CreateApp` is declared but never applied to the `createApp` implementation, so the two can diverge with `tsc --noEmit` fully green. Verified: renaming the implementation's option (`{ ready: isReady }`) while the declared type keeps `isReady` typechecks clean (exit 0), and at runtime `/readiness` returns **200 after `ready = false`** instead of 503 — silently defeating `node/lifecycle/readiness-fails-before-close`, the invariant this skeleton exists to protect. Consumers typecheck against the declaration, never the implementation. Fix: `const createApp: CreateApp = ({ isReady }: CreateAppOptions = {}) => {…}` so tsc checks both sides of the seam. *(rule: `node/lifecycle/readiness-fails-before-close`)*

### run 3 — node-skeleton#2 — src/handlers/health.ts:12

**`src/handlers/health.ts:12`** — same defect: `export type HealthRouter` is never applied to `healthRouter`. Annotate the implementation.

### run 3 — node-skeleton#2 — package.json:8

**`package.json:8`** — `"node": ">=22"` no longer matches what the code requires. Type stripping is only on by default from **v22.18.0**; on 22.0–22.17 `node src/index.ts` fails outright, which breaks the Dockerfile `ENTRYPOINT`, `make run`, and `npm start` simultaneously. `node:22-slim` is a floating tag that resolves to 22.22.x today, so CI masks this. Set `">=22.18"`.

### run 3 — node-skeleton#2 — CLAUDE.md:38

**`CLAUDE.md:38`** — the new `moduleResolution: "bundler"` invariant is factually wrong, in the file whose stated job is preserving load-bearing rationale. Two checks: (1) `module: "node16"` + `moduleResolution: "node16"` typechecks this repo with **exit 0 and zero errors**; (2) tsc never resolves `require()` string literals at all — they're a function call returning `any` — so the explicit `.ts`-extension `require()` calls cannot be what "fought" `node16`/`nodenext`. Either correct the rationale to what `bundler` actually buys (extensionless `import type` specifiers) or switch to `node16` and drop the bullet.

### run 3 — node-skeleton#2 — src/index.ts:8

**`src/index.ts:8-10`** — every module-boundary annotation is an unchecked assertion, because `require()` returns `any`. A typo'd path (`require('./cnofig.ts')`) typechecks clean and only fails at runtime — verified. This is the same escape hatch `as` provides, which `CLAUDE.md:11` bans; spelling it as an annotation doesn't make it checked. Annotating the implementations (findings 1–2) closes most of it; the path-typo class stays open and is worth a line in the invariants list.

### run 3 — node-skeleton#2 — src/config.ts:52

**`src/config.ts:52`** — `SHUTDOWN_TIMEOUT_MS=0` passes the new validation (`< 0` rejects only) and silently disables graceful drain. Verified against the exact shutdown shape: at `0` the unref'd timer fires on the next tick and forces `process.exit(1)` **before** an in-flight request completes; at `10000` the request drains and close is clean. Reject `< 1`, or floor it at a value that can actually drain.

### run 3 — node-skeleton#2 — src/config.ts:16

**`src/config.ts:16`** — `logLevel: string` where `src/log.ts` already defines the `LogLevel` union; `check()` also doesn't validate it, so `LOG_LEVEL=infoo` silently degrades to `info`. The guide's smell table prefers string-literal unions over bare `string`.

### run 3 — node-skeleton#2 — eslint.config.js:44

**`eslint.config.js:44`** — the `erasableSyntaxOnly` comment (enum, namespace, parameter properties) sits directly above `no-explicit-any` and `no-non-null-assertion`, which have nothing to do with erasable syntax. Nothing in the eslint config enforces the erasable-syntax claim — tsconfig does. Move or reword.

### run 3 — node-skeleton#2 — test/health.test.ts:22

**`test/health.test.ts:22`** — `port` falls back to `0` when `server.address()` isn't an object, converting a setup failure into a confusing connection error against port 0. Throw instead.

### run 3 — node-skeleton#2 — README.md:109

**`README.md:109`** — the new License section is unrelated to the TypeScript conversion and isn't covered by the single `feat:` CHANGELOG bullet.

### run 3 — python-skeleton#3 — Makefile.precommit:24

**`Makefile.precommit:24`** — `check` now hard-depends on the `trivy` binary, but trivy is not installed by `make sync` and is not listed anywhere as a prerequisite. On a fresh clone `make precommit` dies with `trivy: command not found` after lint/typecheck/audit have already run. The new README section describes what trivy does but never says to install it. Add trivy to a prerequisites block in the README Quick Start, or guard the target with a `command -v trivy` check that emits an install hint.

### run 3 — python-skeleton#3 — Makefile.precommit:47

**`Makefile.precommit:47`** — `uv export --all-extras` pulls the `dev` extra (pytest, ruff, mypy, httpx) into the audited set, but `Dockerfile:16` ships `uv sync --frozen --no-dev`. Combined with the deliberate zero-severity-threshold policy, a CVE in a dev-only tool that never reaches production blocks `make check` and all of CI, and the only escape is hand-editing a `--ignore-vuln` flag into the Makefile — there is no `.trivyignore`-style reason-carrying file for pip-audit. Consider auditing the shipped set (drop `--all-extras`) and running the dev set as a separate non-blocking target, or add a documented ignore file to match the `.trivyignore` ergonomics.

### run 3 — python-skeleton#3 — pyproject.toml:64

**`pyproject.toml:64-68`** — `S104` is ignored globally, but the justification ("binding `0.0.0.0` is correct for a containerised service") only covers the two intentional sites at `src/skeleton/__main__.py:33` and `src/skeleton/server.py:9`. A global ignore also silences any *future* accidental all-interfaces bind elsewhere in `src/`. Scoping it to those two files via `per-file-ignores`, or a `# noqa: S104` at each site, keeps the rule live where it still has value.

### run 3 — python-skeleton#3 — .github/workflows/ci.yml:33

**`.github/workflows/ci.yml:33`** — `$(lsb_release -sc)` pins the APT suite to the runner codename. `noble` exists in trivy-repo today (verified, HTTP 200), so this works now, but the repo also publishes a `generic` suite specifically to decouple from codenames — using it removes a silent break when `ubuntu-latest` advances to a codename Aqua has not published.

### run 3 — python-skeleton#3 — README.md:86

**`README.md:86`** — "to add a third advisory database over ground pip-audit and trivy already cover" is missing a word; read as "over ground **that** pip-audit and trivy already cover".

### run 3 — vault-cli#68 — CHANGELOG.md:13

**`CHANGELOG.md:13`** — entry is a four-clause paragraph carrying internal-only backstory (the fleet-wide `updater` tool injection, the `docs/dod.md` self-reference) that means nothing to a consumer running `go install`. The changelog guide sets no hard length cap and the repo has precedent for long entries, so this is style, not a rule breach. Tightening to the failure + fix and moving the backstory to the commit body would improve scanability.

### run 3 — backup#15 — Dockerfile:4

**`Dockerfile:4`** — the Go build runs `-ldflags "-s"` with no `-X` version wiring, and `Makefile:127` passes none of `BUILD_GIT_VERSION` / `BUILD_GIT_COMMIT` / `BUILD_DATE`; grep confirms no `buildGitVersion`/`buildGitCommit`/`buildDate` vars exist anywhere in the tree, so built images carry no provenance. **Pre-existing, not introduced by this PR** — flagged only because this PR edits that exact `docker build` line and the rule fired on it. Maintainer's call whether to fold in or defer. *(rule: `go-build-args/three-args-required`)*

### run 3 — backup#15 — Dockerfile:11

**`Dockerfile:11`** — `RUN npm set registry ${NPM_REGISTRY}` is unquoted. Harmless for the current value, but this PR is precisely what makes the value externally controlled; `"${NPM_REGISTRY}"` costs nothing.

### run 3 — backup#15 — Dockerfile:8

**`Dockerfile:8`** and **`Makefile:6`** — the default URL is hardcoded in both places. They agree today, so a bare `docker build .` behaves identically to `make build`; the cost is that a future host migration must touch two files or silently diverge.

### run 3 — backup#15 — Dockerfile:8

**`Dockerfile:8`** — `ARG` values persist in image metadata and are readable via `docker history`. Not an issue for the current credential-free URL, but the new override knob invites `https://user:token@host` for a private registry, which would bake the token into the image. Worth a one-line comment above the `ARG` warning against credentials-in-URL.

### run 3 — bw#41 — groups/meta/bundles.py:61

`sysctl` bundle reaches every node via `groups/meta/bundles.py:61` (`meta-bundles`, `member_patterns: .*`), so the metadata is not inert.

### run 3 — bw#41 — groups/meta/k3s.py:3

**`groups/meta/k3s.py:3`** — Membership is keyed on the node **name** regex `.*k3s.*`, not on `k3s.enabled`. `hm.hell` runs k3s (`nodes/hm.hell.py:66-67`, `'k3s': {'enabled': True}`) but its name contains no `k3s`, so the standalone hell cluster is the one k3s node in the repo that does **not** get the ARP fix and stays on the 128/512/1024 defaults that caused the incident. All 13 `hm.nuke-k3s-*` nodes are covered; hell is the sole miss.

### run 3 — bw#41 — groups/meta/k3s.py:20

**`groups/meta/k3s.py:20-26`** — Every other sysctl in this repo is delivered by a metadata reactor gated on its own `enabled` flag (`bundles/kvm-guest/metadata.py:26`, `bundles/intellij/metadata.py:20`, `bundles/kvm-host/metadata.py`, `bundles/netplan/metadata.py`). Putting this one in static group metadata breaks that convention and is the direct cause of the gap above. `bundles/k3s/metadata.py` already exists and already has a reactor gated on `metadata.get('k3s', {}).get('enabled', False)` (the `iptables` one at lines 1-20) — moving the block there fixes coverage and consistency in one step: ```python @metadata_reactor.provides( 'sysctl/options', ) def k3s_sysctl(metadata): if not metadata.get('k3s', {}).get('enabled', False): return {} return { 'sysctl': { 'options': { 'net.ipv4.neigh.default.gc_thresh1': '4096', 'net.ipv4.neigh.default.gc_thresh2': '8192', 'net.ipv4.neigh.default.gc_thresh3': '16384', } } } ``` Keep the incident comment verbatim — it is the most valuable part of the change.

### run 3 — bw#41 — groups/meta/k3s.py:22

**`groups/meta/k3s.py:22-24`** — IPv6 neighbour table (`net.ipv6.neigh.default.gc_thresh1/2/3`) left at kernel defaults. Repo evidence points to IPv4-only on these nodes (netplan `dhcp6: False`, `k3s.network` is an IPv4 CIDR), so this is not needed today — worth adding only if IPv6 is ever enabled on k3s nodes.

### run 3 — tts-mcp#16 — skills/voice/SKILL.md:87

**`skills/voice/SKILL.md:87`** — `"Anything at all when the user is clearly sitting there watching it scroll by"` sits in the *"Never speak, in any mode"* list and is unqualified, so it voids the attention-signal guarantee stated 8 lines above at `:76-79` (which explicitly names "completion / failure of background or long-running work"). For the commonest case — user present at the terminal while a build runs — the file gives two opposite instructions and an executing agent must guess. It also directly forbids what `:89` prescribes (user asks a question *and* turns voice on, i.e. sitting right there). Scope the bullet to trivially-fast actions the user watched happen, and say that mere presence does not suppress completion/failure/decision signals.

### run 3 — tts-mcp#16 — skills/voice/SKILL.md:69

**`skills/voice/SKILL.md:69-74`** — the table makes `narrate` and `interview` mutually exclusive (`narrate`: answers ✅ / every question ❌; `interview`: answers ❌ / every question ✅), but `:15` and `:16` both cite the *same* away-from-screen/driving scenario as their motivation, and `Args` takes one value at a time. A hands-free user who wants spoken answers **and** every question read aloud has no supported arg, and no guidance on which capability to drop. Either state that combining is unsupported and name the default (`interview` — a missed question blocks progress, a missed narration doesn't), or add a combined value.

### run 3 — tts-mcp#16 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** and **`skills/voice/SKILL.md:8`** — both assert unconditionally that there is *"no competing always-on rule in `~/.claude/CLAUDE.md`"*, while `:94` documents the exact procedure to make that false. `:3` is the frontmatter discovery signal — a point-in-time fact about a human-edited file outside this repo, baked in as if structural, with no instruction to check for a conflict. (Verified true on this machine: no voice/tts rule in `~/.claude/CLAUDE.md`.) Make both conditional and cross-reference the persistence note, or add a check-and-flag instruction. *(rule: `agent-cmd/single-source-of-truth`)*

### run 3 — tts-mcp#16 — skills/voice/SKILL.md:14

**`skills/voice/SKILL.md:14`** — `` `on` (default) `` collides verbally with `:94` *"No always-on default"* and `:8` *"Voice is off until someone invokes `/voice`"*. Recoverable from context, but the PR's stated purpose is removing exactly this ambiguity — say "default arg for bare `/voice`".

### run 3 — tts-mcp#16 — skills/voice/SKILL.md:69

**`skills/voice/SKILL.md:69-74`** — the `Every question` cells for `on`/`narrate` read `❌ decisions only`, pairing a negation with an affirmative and restating what the `Attention signals` ✅ column already covers. Plain ❌ plus a footnote scans faster.

### run 3 — tts-mcp#16 — skills/voice/SKILL.md:91

**`skills/voice/SKILL.md:91-95`** — persistence note documents adding a persistent default but not removing one, leaving the round trip asymmetric for a reader who added a rule and wants sole authority back.

### run 3 — tts-mcp#16 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — 616-char, ~5-clause description. No length cap in the skill-writing guide, so not a violation; the risk is a future edit silently dropping a clause.

### run 3 — tts-mcp#3 — skills/voice/SKILL.md:32

**`skills/voice/SKILL.md:32-33`** — the extraction created a bidirectional restart↔test reference: restart step 3 invokes `/tts-mcp:voice-test`, and `commands/voice-test.md:22` sends the failure path back to `/tts-mcp:voice restart`, re-entering step 3. The only loop guard is prose at `commands/voice-test.md:23` ("if it still fails after a restart"), which carries no state about a restart already performed by the caller. Pre-PR the handshake was inline, so the reference was one-directional within one file. Blast radius is real: `skills/voice/SKILL.md:35` notes a restart drops in-flight messages and "affects every session's relay". Suggest making `voice-test`'s restart bullet conditional on not having been entered from the restart flow.

### run 3 — tts-mcp#3 — commands/voice-test.md:1

**`commands/voice-test.md:1-3`** — frontmatter declares only `description`; no `allowed-tools`, so the command inherits unbounded session permissions while its body drives `mcp__tts__say`, `mcp__tts__get_voices`, and `mcp__tts__get_status` (lines 15, 19, 20). *(rule: `agent-cmd/command-frontmatter`)* Fix: add `allowed-tools:` scoped to those three MCP tools. Note: the owning auditor graded this **Critical** on the strength of the MUST-tier rule; I graded it down to Important because the command's actual surface is three read/speak MCP calls, not arbitrary execution. Take the stricter grade if you prefer rule-tier fidelity.

### run 3 — tts-mcp#3 — commands/voice-test.md:5

**`commands/voice-test.md:5,10,22`** — three references to the same skill use two spellings: `/voice on` (:5), `/voice restart` (:10), `/tts-mcp:voice restart` (:22). Pick one form; the plugin-prefixed one matches how `skills/voice/SKILL.md:24,32,33` refers to `/tts-mcp:voice-test`.

### run 3 — tts-mcp#3 — skills/voice/SKILL.md:3

**`skills/voice/SKILL.md:3`** — the appended clause ("Activation does not run a selftest — use /tts-mcp:voice-test…") is a post-activation caveat rather than a discovery trigger, and duplicates the body section at `:20-24`; it pushes the description to ~453 chars, diluting the actual match phrases. Counterpoint: frontmatter is always in context while the body is not, so keeping it may be deliberate — judgment call, not a defect.

### run 3 — tts-mcp#3 — commands/voice-test.md:16

**`commands/voice-test.md:16`** — the on-screen "Did you hear it? (y / no)" confirmation technically trips the no-user-prompt guideline. *(rule: `agent-cmd/no-user-prompts`)* Not a real defect — audio reachability cannot be verified non-interactively, and the command is explicitly gated to deliberate invocation. Worth one sentence documenting it as an intentional scoped exception so future audits don't re-derive it.

### run 3 — tts-mcp#3 — README.md

**`README.md`** — documents neither the `/voice` skill nor the new `/tts-mcp:voice-test` command. Pre-existing gap (no plugin artifact is documented there), so this PR only extends it.

### run 3 — distill#6 — hooks/deny-generated-file-edits.sh:46

**`hooks/deny-generated-file-edits.sh:46`** — the denial quotes the banner's `Regenerate:` line verbatim, but that line never contains `--title` (`pkg/distill/driver.go:315` formats only `--source`/`--output`). An agent that obeys the quoted command on a file generated *with* `--title` regenerates it **without the `# <heading>`** — silent content loss in the very file the hook exists to protect. This PR makes it live: `docs/adopting-distill.md:92` and the worked example at `:138` both prescribe `--title`, so anyone following the new runbook lands in exactly this state. Fix by including `--title` in the banner (driver.go) or by having the hook point at the Makefile `generate` target instead of the raw command.

### run 3 — distill#6 — hooks/deny-generated-file-edits.sh:36

**`hooks/deny-generated-file-edits.sh:36`** — `grep -oE 'Source:[[:space:]]*\S+'` stops at the first whitespace, so a source dir containing a space is truncated. Reproduced with `Source: /Users/me/My Notes/claude-md-rules` → the denial tells the agent to edit `/Users/me/My/<rule-id>.md`, a path that does not exist. Worse than no hint. `docs/adopting-distill.md:25` explicitly blesses arbitrary `--source` paths, and vault dirs with spaces are ordinary. Use the same `.+` capture already used for `regen` at line 37.

### run 3 — distill#6 — README.md:128

**`README.md:128`** — `cp hooks/deny-generated-file-edits.sh ~/.claude/hooks/` fails with `cannot create regular file … No such file or directory` when `~/.claude/hooks/` does not yet exist (verified against a fresh `HOME`). This is the first-run path for exactly the audience the section targets. Prefix with `mkdir -p ~/.claude/hooks`. Same snippet, same fix at **`docs/adopting-distill.md:110`**.

### run 3 — distill#6 — hooks/deny-generated-file-edits.sh:33

**`hooks/deny-generated-file-edits.sh:33`** — the 2 KB content scan will false-positive on any document that *describes* the banner near its top. distill's own `README.md` carries the literal at byte 2486 — only ~440 bytes of headroom; adding a couple of paragraphs above § Output shape makes the project's README self-protecting and un-editable by an agent. Anchoring the match to the leading `<!--` block (or the first line) removes the class entirely.

### run 3 — distill#6 — docs/adopting-distill.md:5

**`docs/adopting-distill.md:5`** — the three `[README.md § Section](../README.md)` links (also `:97`, `:107`) point at the file with the section named only in prose. All three targets exist as real headings (`## Source rule format`, `## Make integration`, `## Edit protection hook`), so anchors would work: `../README.md#source-rule-format` etc.

### run 3 — vault-ui#39 — src/vault_ui/activity.py:53

**`src/vault_ui/activity.py:53-55`** — `session_id` is interpolated raw into a path join (`project_dir / f"{session_id}.jsonl"`) with no validation, but this codebase already guards that exact operation: `cleanup.py:51` and `cleanup.py:148` reject `session_id` containing `/` or `\` *and* gate on `is_uuid()` before building the same path. The guard was dropped in the new module. It is reachable: `POST /api/tasks/{id}/session` stores an unresolved display name verbatim (`api/tasks.py:1737`), so `claude_session_id` is not guaranteed to be a UUID. A `../`-bearing value makes `Path.stat()` walk outside `~/.claude/projects/`, and the mtime of whatever it lands on is echoed back as `activity_date` in the API response — a file-existence + last-modified oracle driven by vault frontmatter. Fix: reuse the `cleanup.py` guard (`is_uuid` / reject separators) at the top of `transcript_mtime`.

### run 3 — vault-ui#39 — src/vault_ui/activity.py:61

**`src/vault_ui/activity.py:61`** — the fallback `root.glob(f"*/{filename}")` embeds `session_id` in a glob *pattern*, so `*`, `?`, or `[` in the value change matching semantics instead of being matched literally. The loop returns the first hit, so an unrelated session's transcript mtime can be reported as this card's activity date — silently wrong, no error surfaced. Same fix as above covers it.

### run 3 — vault-ui#39 — src/vault_ui/activity.py:59

**`src/vault_ui/activity.py:59-64`** (called from **`src/vault_ui/api/tasks.py:705`** and **`src/vault_ui/api/tasks.py:1794`**) — the fallback scan is uncached and runs once *per card, per response*. When the direct check misses, `glob` must enumerate every directory under `~/.claude/projects/` before concluding "not found". The module's own docstring (`activity.py:9-11`) names cloud/container sessions as a permanent, expected miss — so that path is guaranteed to scan exhaustively and guaranteed to fail, every call, forever. There is no memoization even *within* one request: `src/vault_ui/api/tasks.py:606` and `src/vault_ui/api/tasks.py:792-799` map every task/goal independently, and the existing `vault_task_cache` short-circuits the vault-cli subprocess but not this I/O. Fix: do one `projects_root.glob("*/*.jsonl")` pass per request indexed by filename stem, or cache negative lookups — a session that is not local now will not become local later.

### run 3 — vault-ui#39 — src/vault_ui/api/tasks.py:606

**`src/vault_ui/api/tasks.py:606`** — that mapping comprehension is synchronous blocking filesystem I/O executed inside `async def _process_vault`, so the per-card stat/glob runs on the event loop thread for the full N-card fan-out. It fires hardest exactly when the feature matters: each vault-cli file-change event pushes a websocket update that re-fetches the whole board, and during active agent work that is every few seconds. Fix: resolve activity dates once before mapping, or move the resolution off-loop via `asyncio.to_thread`.

### run 3 — vault-ui#39 — src/vault_ui/activity.py:21

**`src/vault_ui/activity.py:21`** — `_claude_projects_root()` reads `Path.home()` ambiently deep in a leaf function; the `projects_root` parameter exists but *no* production caller passes it (`api/tasks.py:705`, `api/tasks.py:1794`), only tests. This repo has an explicit composition root — `create_app()` in `src/vault_ui/factory.py:329` is docstringed as one and already wires request-independent state there (`app.state.vault_task_cache`). The closest sibling module, `session_resolver.py:23`, takes `project_dir` as a *required* parameter precisely to avoid this. Fix: resolve the root once in `factory.py` and thread it through, keeping the default only for CLI entry points. *(rule: `python-architecture/main-py-composition-root`)*

### run 3 — vault-ui#39 — tests/test_activity.py:44

**`tests/test_activity.py:44-56`** — `formatActivityAge` is verified by substring-matching a 900-character window of `app.js` source, so no unit boundary is actually exercised: 59s vs 60s, 23h vs 24h, 6d vs 7d, and the negative-delta case (a future `activity_date` from client/server clock skew, which currently renders `<1m`) are all untested. The pattern matches the pre-existing `test_card_render_unify.py` convention, so this is not a regression — but the new formatter has real branching that the old assertions did not.

### run 3 — vault-ui#39 — tests/test_activity.py

**`tests/test_activity.py`** — `test_transcript_found_in_project_dir`, `test_activity_prefers_live_session_over_stale_file`, and `test_activity_prefers_fresh_file_over_dead_session` assert against `datetime.now(tz=UTC)` at assertion time with tolerance windows. Margins are generous enough not to flake realistically, but `compute_activity_date` takes no clock parameter, so exact-value assertions are impossible. Injecting a clock would let these assert equality.

### run 3 — vault-ui#39 — src/vault_ui/vault_cli_client.py:316

**`src/vault_ui/vault_cli_client.py:316-319`** — the goal `modified_date` parse mirrors the task parse exactly, but nothing verifies that `vault-cli goal list --output json` actually *emits* `modified_date`. If it does not, session-less goals silently get `activity_date=None` and render no badge, and no test catches it. Worth one integration assertion against real vault-cli output.

### run 3 — recurring-task-creator#30 — pkg/schedule/task_definition.go:75

**`pkg/schedule/task_definition.go:75-76`** — The `PeriodOffset` field doc still says the publisher "applies the shift to the fire date before formatting the token." That is exactly the assumption this PR disproves — the shift is now applied to the first of the fire month. A reader trusting this doc could "simplify" the anchor away and reintroduce the bug. (The same comment also names `buildPeriodToken`, a symbol that does not exist; the method is `periodTokenBuilder.Build` — pre-existing, but worth fixing in the same pass.) File is outside the diff but made stale by it.

### run 3 — recurring-task-creator#30 — pkg/publisher/period_token_test.go:52

**`pkg/publisher/period_token_test.go:52`** — The block comment `// Discriminating entries — old code produces wrong token on these (overflow into next month)` covers four entries, but the fourth (`pkg/publisher/period_token_test.go:68-73`, `2026-08-31` / `offset: -1` / `2026-07`) is **not** discriminating: August and July both have 31 days, so `AddDate` hits a valid date and the old code produced `2026-07` too. The entry's own description ("July has 31 days but August target does too") contradicts the comment above it. The originating prompt explicitly classified this row as control/regression. Move it into the control block at line 36 or reword the comment — the assertion is right, the documented rationale is wrong, which misleads anyone later auditing regression coverage.

### run 3 — recurring-task-creator#30 — pkg/publisher/period_token.go:105

**`pkg/publisher/period_token.go:105`, `:109`, `:113`** — The three `firstOfMonth := time.Date(...)` lines carry no comment explaining *why* the clamp exists. Contrast the `RecurrenceOnDate` case at `:116-121`, which does explain its reasoning. This is precisely the kind of non-obvious, bug-motivated line that a future cleanup collapses back to `base.AddDate(...)`. One line of comment on the first case would prevent that.

### run 3 — recurring-task-creator#30 — pkg/publisher/period_token.go:105

**`pkg/publisher/period_token.go:105,109,113`** — `time.Date(base.Year(), base.Month(), 1, 0, 0, 0, 0, base.Location())` is duplicated verbatim three times. Extracting `firstOfMonth(base time.Time) time.Time` into `render.go` next to the existing `firstOfPreviousMonth` would collapse it to one definition and give four call sites.

### run 3 — recurring-task-creator#30 — pkg/publisher/render.go:54

**`pkg/publisher/render.go:54`** — `firstOfPreviousMonth` hardcodes `time.UTC` for the same day-1 clamp the new code does with `base.Location()`. Behaviorally identical today (`schedule.Date.Time()` always returns midnight UTC — confirmed in `pkg/schedule/date.go`), but it's a silent divergence between two code paths computing the same concept. The helper extraction above resolves it.

### run 3 — recurring-task-creator#30 — CHANGELOG.md:13

**`CHANGELOG.md:13`** — The `fix:` bullet is one 466-character sentence. Within this repo's convention (a `v0.10.0` bullet runs 572 chars), so not a violation, but splitting root cause from consequence would read better.

### run 3 — tts-mcp#10 — CHANGELOG.md:17

**`CHANGELOG.md:17`** — "The two remaining advisories are `@hono/node-server <2.0.5`" does not hold against this lockfile. A live `npm audit` on the PR's own tree reports **4** advisories: `fast-uri` 3.1.4 **high** (range `3.0.0 - 3.1.4`), `hono` 4.12.32 moderate (range `<=4.12.33`), plus `@hono/node-server` 1.19.15 and `@modelcontextprotocol/sdk` 1.29.0. The bump did move both packages (`fast-uri` 3.1.0→3.1.4, `hono` 4.12.9→4.12.32), so the claim was very likely true when written — the advisory ranges top out at exactly the versions installed here, the signature of advisories published afterwards. Not author error, but the entry now tells a reader that no high-severity finding remains, which is wrong. Reword to a point-in-time claim, or restate the current set.

### run 3 — tts-mcp#10 — Makefile.precommit:26

**`Makefile.precommit:26`** — `cd mcp && npm ci && npx tsc --noEmit` wipes and reinstalls `mcp/node_modules` on *every* `make check`. `check` was previously network-free and fast; it now needs a registry round-trip locally. Gate the install on lockfile mtime (`mcp/node_modules/.installed: mcp/package-lock.json`) and keep `npm ci` as the install command.

### run 3 — tts-mcp#10 — Makefile.precommit:23

**`Makefile.precommit:23-30`** — `mcp/` now has a type gate but still no lint/format/test target; style and dead-code drift in `tts-mcp.ts` remain ungated. Fine at single-file size, worth wiring if the relay grows. *(rule: `node/make/required-targets`)*

### run 3 — tts-mcp#10 — CHANGELOG.md:22

**`CHANGELOG.md:22`** — The new `### Changed` bullet is separated from the existing one by a blank line, making a loose list where the neighbouring `### Fixed` block is tight. Cosmetic.

### run 3 — discord-assistant#5 — shim/claude_openai_shim.py:110

**`shim/claude_openai_shim.py:110`** — the rewritten boolean parser fails **open**: anything not in the false-list is `True`. Verified live — `SHIM_UNSAFE=disabled`, `nope`, `n`, `None`, `2` all evaluate `True`, which removes the tool allowlist entirely (gates at `:1161`, `:1571`) and hands the Claude session unrestricted tools in a bot whose own `CLAUDE.md` notes it "can reach a session with vault and repository access." The sibling `flag()` this PR adds at `src/config.js:15` gets this right — unknown values fall back to the default. The changed line is the one place the asymmetry survives, and the PR's own rationale ("fails quietly in the safe-looking direction") is the argument against it. Mirror `flag()`: explicit true-list, explicit false-list, unknown → `default`. Pre-existing behaviour, but this line is rewritten here and the fix is two lines.

### run 3 — discord-assistant#5 — shim/claude_openai_shim.py:799

**`shim/claude_openai_shim.py:799-847`** — the new wake-phrase logic has **zero test coverage**. `make test` is `node --test`; there is no `test_*.py` anywhere in the repo. The JS mirror got four new tests; the Python side — which is the authority on whether a turn is answered at all — got none, along with the `setting()` change (`:110`), the `_PANEL` change (`:876`), and the bridge change (`:2016`). The changelog documents three separate live regressions in exactly this regex, one of them explicitly "passing every test written from imagined utterances." A `test/shim_wake_test.py` plus a `make test` hook is the cheapest guard against a fourth.

### run 3 — discord-assistant#5 — src/config.js:135

**`src/config.js:135-136`** — "If the two lists drift the cost is cosmetic — dots without an answer" is contradicted by `src/voice.js:569-574` in this same PR, which documents dots-without-an-answer as the state that hangs `answering` until `TYPING_MAX_MS` (`src/voice.js:56`, 5 min) and makes `speak()` refuse typed turns as busy. Worse, disagreement does not require drift: the bot evaluates a **single transcript event** (`src/voice.js:575`) while the shim evaluates the **accumulated turn** (`:1830`). I confirmed the shim returns `false` for an accumulation lacking sentence punctuation (`uh what's my task hey bot what's my task`); if the bot's per-event transcript is the clean `hey bot what's my task`, it arms `answering`, the endpoint answers with silence, and no `response.done` ever arrives to clear it. Either bound `answering` far below 5 minutes on the transcription path, or clear it when a turn ends without a response.

### run 3 — discord-assistant#5 — shim/claude_openai_shim.py:2016

**`shim/claude_openai_shim.py:2016`** — `strip_panels()` can reduce the answer to empty *after* the `if not answer` guard at `:1993` has already run on the unstripped text. For a typed turn `why` is unconditional (`:2003`), so a panel-only answer posts `{"text": ""}`, which `src/health.js:72` rejects with 400. Verified: `strip_panels()` on a panel-only answer returns `''`. The log then reads "posting — the question was typed" followed by "post failed" — the answer is lost and the reason is misleading. Strip first, then test for emptiness.

### run 3 — discord-assistant#5 — None

**`LICENSE`** — no LICENSE file at repo root. Pre-existing, not introduced by this diff. *(rule: `go-licensing/license-file-required`)*

### run 3 — discord-assistant#5 — shim/claude_openai_shim.py:876

**`shim/claude_openai_shim.py:876`** — the new 📌/🎯 alternative lacks the `(?:\*\*)?` tolerance its sibling `READY|DONE|…` alternative has. Verified: `**📌 Task:** [x](y)` survives stripping. The documented closer format is unbolded, so this is latent, but it is the same "half a panel removed, half posted" failure the alternative was added to fix.

### run 3 — discord-assistant#5 — src/config.js:180

**`src/config.js:180-185`** — a `new RegExp` is compiled per phrase per call, on every transcription event. The shim precompiles once at `:822`. Hoist to module scope.

### run 3 — discord-assistant#5 — shim/claude_openai_shim.py:1787

**`shim/claude_openai_shim.py:1787`** — `is_filler()` runs *before* `strip_wake_phrase()` at `:1838`, so `"Hey bot. Okay."` strips to `"Okay."` and reaches Claude as a real turn, while a bare `"Okay."` is silently skipped. The PR frames the wake gate as "the same silent path as filler"; re-checking filler after stripping would make that true.

### run 3 — discord-assistant#5 — shim/claude_openai_shim.py:1838

**`shim/claude_openai_shim.py:1838`** — dropping everything before the phrase also drops an explicit chat-post request made earlier in the same accumulated turn, before `_wants_chat_post(prompt)` reads it at `:2004`. Verified: `"Post that in chat please. Hey bot, what's my task?"` → `"what's my task?"`. Deliberate per the comment, but the interaction with the bridge trigger isn't noted.

### run 3 — tts-mcp#13 — src/server.py:353

**`src/server.py:353`** — `POST /say` queues `body.instruct` without validating it against the configured engine. With `engine: voxtral` (the value shipped in `config.yaml:1`), a request carrying `instruct` returns **202 Accepted**, then fails asynchronously in the worker when `VoxtralEngine.generate` raises. The caller only learns of it by polling `/status/{message_id}`. `voice` is already validated at this boundary (`src/server.py:333`, 400 on mismatch) — `instruct` should be rejected the same way. This contradicts the project's own fail-fast rule in `CLAUDE.md` ("Fail fast — never swallow errors… never continue past failures"). Independently flagged by the Python quality, Python architecture, and docs reviewers.

### run 3 — tts-mcp#13 — src/tts/engine.py:227

**`src/tts/engine.py:227`** — `streaming_interval` is static config parsed once from `config.yaml`, never varying per request, yet it's threaded as a runtime parameter through `generate_chunks` → `engine.generate` at four call sites. This PR already demonstrates the right pattern one parameter over: `Qwen3Engine.__init__` injects `language` at construction. `streaming_interval` is knowable at the same moment, inside `build_engine`. *(rule: `python-architecture/constructor-injection-only`)*

### run 3 — tts-mcp#13 — src/tts/engine.py:249

**`src/tts/engine.py:249`** — `cast(CustomVoiceModel, model)` is unchecked. Qwen3 models are carried through `ServerState.model`, `_generate_item`, `audio_worker`, and `generate_chunks` statically typed as `TTSModel` — a protocol requiring `generate(text, voice, …)` that Qwen3 models do not implement. Type-checking along the qwen3 path is fictional; correctness rests entirely on `validate_model` having run earlier in the model's lifecycle, a temporal coupling no checker enforces. mypy/pyright pass only because `load()` returns `Any` and the cast silences the rest. Consider a marker protocol or a union so the qwen3 path isn't statically lying.

### run 3 — tts-mcp#13 — README.md:273

**`README.md:273`, `CHANGELOG.md:13`** — Both claim the voxtral engine "fails the request rather than silently ignoring it." Verified against `src/server.py:320-360`: the request is accepted (202) and fails later in the background worker. "Fails the request" reads as synchronous 4xx rejection and could lead an API consumer to skip `/status` polling. Reword to describe the actual async behavior — or better, make the docs true by adding the boundary check.

### run 3 — tts-mcp#13 — tests/test_server.py

**`tests/test_server.py`** — No server-level coverage for `instruct`. `tests/test_engine.py:100` covers `VoxtralEngine.generate` rejecting it at the engine boundary, but nothing exercises `POST /say` with `instruct` end-to-end, which is exactly where both Critical findings live. The eight touched call sites only thread `instruct=None`.

### run 3 — tts-mcp#13 — src/server.py:118

**`src/server.py:118-140`** — The `ServerState` encapsulation refactor is applied inconsistently: `status_lock` got a private field plus a read-only property, but `work_queue`, `ready_queue`, and `statuses` are same-shaped (never reassigned, mutated in place) and stayed bare public attributes. `model` is correctly public since it genuinely is reassigned externally (`src/server.py:461`). Either extend the pattern or state the rule in the class docstring so the split reads as deliberate. Note the properties give shallow immutability only — `state.voices.append(...)` still works.

### run 3 — tts-mcp#13 — src/tts/config.py:70

**`src/tts/config.py:70`** — `discover_models` is engine-agnostic (globs any dir with `model.safetensors`), so the CLI model picker offers Qwen3 directories under `engine: voxtral`. Selecting one fails at `engine.discover_voices` with "No voice_embedding directory found" rather than naming the real mismatch. The engine already knows how to recognize its own models.

### run 3 — tts-mcp#13 — scripts/download-model.sh:110

**`scripts/download-model.sh:110-115`** — The qwen3 hint prints `language: English`, but the voxtral hint doesn't say to *remove* `language:`, which `build_engine` hard-rejects (`src/tts/engine.py:282`). Switching families back via the script leaves a config that won't start.

### run 3 — tts-mcp#13 — scripts/download-model.sh:83

**`scripts/download-model.sh:83-89`** — `$MODEL_DIR` is only created as a side effect of the subdirectory `mkdir -p`. Fine for both current families; an explicit `mkdir -p "$MODEL_DIR"` would keep a future flat-file engine from failing with a confusing curl error.

### run 3 — tts-mcp#13 — mcp/tts-mcp.ts:226

**`mcp/tts-mcp.ts:226`** — `instruct` is interpolated unbounded into the log line while `text` on the same statement is truncated at 80 chars. `(instruct ?? "-").slice(0, 80)` for consistency.

### run 3 — tts-mcp#13 — src/server.py:287

**`src/server.py:287-288`** — `audio_file` and `error` gained `= None` defaults, but both are always passed explicitly (`src/server.py:376-382`). Unrelated to the feature and undocumented in the CHANGELOG.

### run 3 — tts-mcp#13 — tests/test_server.py:92

**`tests/test_server.py:92-93`** — `_ImmediateAudioPlayer` renamed `sample_rate`/`lead_silence_ms` to `_sample_rate`/`_lead_silence_ms`; neither is read anywhere. Pure churn mirroring the `ServerState` refactor into a test stub.

### run 3 — tts-mcp#13 — src/tts/worker.py:57

**`src/tts/worker.py:57`** — The CLI path hardcodes `instruct=None`, so qwen3 emotion control is HTTP-only. Reasonable scope cut; worth a follow-up if CLI parity is expected.

### run 3 — tts-mcp#13 — CHANGELOG.md:12

**`CHANGELOG.md:12-26`** — Entries are multi-sentence prose paragraphs (~90 words for the first) rather than terse bullets, and use no conventional prefixes. Consistent with the file's own pre-existing Keep-a-Changelog style throughout `v0.2.0`/`v0.1.0` — a project convention that diverges from the guide, not a regression in this PR. *(rule: `changelog/conventional-prefix-required`)*

### run 3 — github-releaser-agent#8 — pkg/githubtags/tags.go:33

**`pkg/githubtags/tags.go:33-35`** — doc regression on a public interface: the `TagsFetcher` comment was reduced to one line, `LatestSemverTag` lost its doc comment entirely, and `// Implementations MUST be safe for concurrent use.` was deleted. Sibling interfaces `pkg/githubchangelog/fetcher.go` and `pkg/maintainerconfig/fetcher.go` still carry that sentence, so the concurrency contract is now inconsistent across the package set. The new `CommitSHAForTag` is documented well — restore the same standard for its neighbour. *(rule: `go-doc/exported-item-must-have-comment`)*

### run 3 — github-releaser-agent#8 — pkg/steps_planning_test.go:2256

**`pkg/steps_planning_test.go:2256-2705`** — untested interaction that can flip a terminal outcome: all 11 tests in the new block hardcode `tagsFetcher.LatestSemverTagReturns("v0.3.1", nil)`. Per `pkg/steps_planning.go:353-380`, when `LatestSemverTag` returns `ErrNoTags` or a transient error, `effectiveVersion` falls back to the frontmatter `snapshot` — so a *different tag* is handed to `CommitSHAForTag`, and a stale snapshot could match (or fail to match) a commit the live tag would not. A transient GitHub blip therefore changes whether a task auto-closes. Suggested test: `LatestSemverTagReturns("", githubtags.ErrNoTags)` + a P1 changelog, asserting via `CommitSHAForTagArgsForCall(0)` that the snapshot version was the tag actually queried.

### run 3 — github-releaser-agent#8 — pkg/steps_planning.go:759

**`pkg/steps_planning.go:759`** — the P1 path paginates `/tags` twice with no caching: `resolveCurrentVersion` → `LatestSemverTag` → `collectTags`, then `releaseTagCommit` → `CommitSHAForTag` → `collectTags`. `CHANGELOG.md:9`'s "shared `collectTags` pagination pass" describes shared *code*, not a shared *pass*. Threading the already-fetched tag list through, or having `LatestSemverTag` return the entry rather than the name, halves the API cost on a rate-limited endpoint.

### run 3 — github-releaser-agent#8 — pkg/steps_planning.go:86

**`pkg/steps_planning.go:86-91`** — `releaseTagVerdict` returns a `string` that unions a domain enum member (`PlanOutcomeNothingToRelease`) with an ad hoc sentinel (`releaseTagVerdictEscalate`, `pkg/steps_planning.go:39`), so the return type belongs to no coherent set. Returning `bool` and letting the caller map to the log token would drop the sentinel const entirely. *(rule: `go-enum-type/typed-constants-with-collection`)*

### run 3 — github-releaser-agent#8 — pkg/plan_output.go:111

**`pkg/plan_output.go:111`** — `PlanOutcomeNothingToRelease` joins the other three outcomes as an untyped `string` const with no named type and no `AllPlanOutcomes` collection, so nothing prevents an invalid outcome from being marshalled. Pre-existing pattern; this change extends it rather than introducing it. *(rule: `go-enum-type/typed-constants-with-collection`)*

### run 3 — github-releaser-agent#8 — specs/completed/002-planning-close-nothing-to-release.md:119

**`specs/completed/002-planning-close-nothing-to-release.md:119`** — the Security/Abuse claim "editing a task's `ref` cannot manufacture a match" is not accurate as written. `sameCommit` never verifies that `ref` corresponds to real git history — it compares the frontmatter value verbatim against the remote SHA. Anyone who can write the task frontmatter can paste the target repo's already-public tag commit SHA into `ref` and force a match with no grinding. The practical impact is nil (that same access lets you edit `status:` directly, and the worst case is a suppressed escalation, never a bad release), but the stated threat model should match the code.

### run 3 — github-releaser-agent#8 — pkg/steps_planning.go:761

**`pkg/steps_planning.go:761-763`** — the `release_tag_check` log line interpolates `ref` with `%s` and no sanitization (CWE-117). Spec 002 designates this line as the audit-of-record for the new auto-close path, so a `ref` containing newlines could forge a plausible-looking log entry for an operator reviewing after the fact. `%q` on `ref` closes it. Sibling log lines in the same file share the pattern, so this is not a regression — but this is the first one the spec makes load-bearing.

### run 3 — github-releaser-agent#8 — pkg/githubtags/tags.go:175

**`pkg/githubtags/tags.go:175-178`** — `fetchPage` declares named returns `(tags []tagResponse, nextURL string, err error)` but every return is explicit and the names are never used; the body even shadows them with local `tagEntries` / `next`. Drop the names or use them.

### run 3 — github-releaser-agent#8 — pkg/steps_planning_test.go:2298

**`pkg/steps_planning_test.go:2298-2307`** — the nothing_to_release assertions cover `Outcome`, `Reason`, and `PreconditionFailed` but never assert `plan.Bump` / `plan.NextVersion` are empty. `publishNothingToRelease` correctly leaves them unset; a future edit threading a stale value into that `PlanOutput{}` literal would regress silently.

### run 3 — github-releaser-agent#8 — CHANGELOG.md:11

**`CHANGELOG.md:11`** — `feat(planning):` prefixes a bullet whose own text describes corrected behaviour ("a release check re-fired against a release commit is nothing to release, **not** a malformed changelog"), which reads as `fix`. No practical bump impact — bullet 1 is a genuine `feat`, so minor is already forced. *(rule: `changelog/conventional-prefix-required`)*

### run 3 — discord-assistant#1 — src/index.js:272

**`src/index.js:272-278`** — the swallowed state has no escape hatch. `gatewayReady = false` can only return to `true` via `shardResume`/`shardReady` (`src/index.js:109-114`). On a non-resumable close — fatal close codes like 4004 (auth failed after token rotation) or 4014 (disallowed intents), whose `ws` message matches the `WebSocket was closed` alternative — discord.js stops retrying and those events never fire again. `/healthz` has no dependencies (`src/health.js:30-31`) and the k8s `livenessProbe` targets it (`k8s/discord-assistant-deploy.yaml:42-46`), so the pod stays alive with liveness green and readiness 503 indefinitely; `supervise.sh` never sees an exit either, because the process never exits. That is precisely the "alive, passing liveness, and permanently disconnected … strictly worse than exiting" state the comment at `src/index.js:254-258` argues against — correctly guarded before login, unguarded after it. Fix: a watchdog that calls `process.exit(1)` after N minutes of continuous `!gatewayReady`, letting `supervise.sh` and the Deployment do their job.

### run 3 — discord-assistant#1 — src/index.js:259

**`src/index.js:259-260`** — `RECOVERABLE_NET` classifies by message text inside a process-wide handler with no check of where the error came from, but the branch then asserts a gateway fact by setting `gatewayReady = false` (`src/index.js:276`). `ECONNRESET`, `ETIMEDOUT`, `socket hang up` and `EAI_AGAIN` are generic Node network errors. `src/voice.js:437-445` creates a `VoiceConnection` and attaches only a `stateChange` listener — no `'error'` listener — so an unhandled voice UDP/WS reset lands in this handler. The result is a voice-only fault draining *gateway* readiness, and because the gateway never actually dropped, no `shardResume`/`shardReady` will ever restore it: permanent 503 with a perfectly healthy gateway. Before this PR that error crashed the process visibly; now it is silent. Note also that `shardDisconnect` (`src/index.js:106-108`) already sets `gatewayReady = false` for genuine gateway drops — so line 276 is redundant when the assumption holds and harmful when it doesn't. Fix: gate on the error's origin (e.g. only inside a `client.on('error'/'shardError')` path), and drop the readiness write.

### run 3 — discord-assistant#1 — scripts/supervise.sh:19

**`scripts/supervise.sh:19-23`** — `stop()` kills the child and exits immediately without waiting for it. Verified empirically: the supervisor exits while the child is still handling SIGTERM, orphaning it. `src/index.js:199-228` deliberately spends ≥600 ms plus voice-leave time on shutdown specifically to avoid leaving ghost voice connections, so `make run` (and `make dev`, which foregrounds it at `scripts/dev.sh:91`) now returns to the prompt mid-drain, and a second `make run` can start a bot while the first still holds the gateway. Add `wait "$child"` before `exit 0`.

### run 3 — discord-assistant#1 — src/index.js:232

**`src/index.js:232-235`** — `unhandledRejection` is untouched and still exits unconditionally. The same class of transient network fault arriving as a promise rejection rather than a throw still kills the process, so "survive gateway network faults" is only half-covered. With `supervise.sh` in place this degrades to a restart rather than a silent death, which is defensible — but the asymmetry is undocumented right next to a block that documents its reasoning heavily.

### run 3 — discord-assistant#1 — src/index.js:259

**`src/index.js:259-281`** — the new classification logic cannot be unit-tested. The predicate is inline in `index.js`, which exports nothing and calls `client.login()` at require time (`src/index.js:283`). `test/` already covers config, health and llm, but the two things most likely to regress here — the `connectedOnce` gate and what `RECOVERABLE_NET` does and does not match — have no coverage. Extract `isRecoverableNetError(err, connectedOnce)` into its own module and test it alongside the existing suites.

### run 3 — discord-assistant#1 — scripts/supervise.sh:8

**`scripts/supervise.sh:8-9`** — the header states "Deliberately dumb. It does not know what it runs", but `scripts/supervise.sh:40-43` hardcodes `src/index.js`'s exit-code contract (2 = bad config). The contract is real and worth keeping; it is just undocumented at the other end — nothing at `src/index.js:23` says a supervisor depends on that 2. Note it at both sites, or the next person to renumber exit codes silently turns a give-up into a restart loop.

### run 3 — discord-assistant#1 — src/index.js:262

**`src/index.js:262-265`** — this is the second `client.once('clientReady')` listener; the one at `src/index.js:75-76` already runs on the same event and already sets `gatewayReady = true`. Set `connectedOnce` there, or drop the variable entirely in favour of `client.readyAt !== null`, which discord.js already tracks.

### run 3 — discord-assistant#1 — CHANGELOG.md:5

**`CHANGELOG.md:5`** — the new bullet has no conventional prefix. *(rule: `changelog/conventional-prefix-required`)* Every other entry in the file is narrative prose too, so this is house style rather than a regression — but `.maintainer.yaml` sets `autoRelease: true`, so the release bot infers the semver bump from these bullets. A `feat:` prefix would make the bump a read rather than a guess.

### run 3 — discord-assistant#1 — scripts/supervise.sh:12

**`scripts/supervise.sh:12-13`** — `SUPERVISE_DELAY_MIN`/`SUPERVISE_DELAY_MAX` are used unvalidated in arithmetic and `sleep`; a non-numeric value fails at line 50 rather than at the point of configuration.

### run 3 — discord-assistant#1 — Makefile.precommit

**`Makefile.precommit`** — `check` runs eslint, prettier, audit and trivy but no shell linting. This PR adds the third script under `scripts/` and the first in the `make run` critical path. It is shellcheck-clean today; a `shellcheck scripts/*.sh` line in `check` keeps it that way.

### run 3 — helm#3 — charts/github-pr-reviewer/values.yaml:51

**`charts/github-pr-reviewer/values.yaml:51`** — "routed to `human_review` … not a block" is not supported by this chart's own agent config: **`charts/github-pr-reviewer/values.yaml:84-87`** sets `triggerPhases: [planning, execution, ai_review]`, which excludes `human_review`. Even if the watcher does route untrusted PRs there, nothing in this unit consumes that phase, so within this chart the outcome is indistinguishable from a block. The reassuring framing understates the effect of leaving an author out of the list.

### run 3 — helm#3 — charts/github-pr-reviewer/values.yaml:54

**`charts/github-pr-reviewer/values.yaml:54-56`** — the trust rationale ("its PRs are mechanical … no code") is a property of the updater bot's *current* behavior, not something this chart enforces. `TRUSTED_AUTHORS` matches on login only; if the bot's scope ever widens, trust silently widens with it and nothing here fails closed. Worth one line noting the assumption so a future reader knows what invalidates it.

### run 3 — helm#3 — charts/github-pr-reviewer/Chart.yaml:9

**`charts/github-pr-reviewer/Chart.yaml:9`** — `0.1.0 → 0.2.0` (minor) for a default-value change. Repo precedent for a comparable change is patch: commit `cd6e677` bumped `github-dark-factory` `0.1.0 → 0.1.1` for a values-default change (agent resources). Minor is defensible here since the default alters runtime trust behavior rather than sizing — but the repo now has two conventions for the same class of change. No action needed if minor is the intended new rule; just be consistent going forward.
